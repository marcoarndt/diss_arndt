% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% %% Kapitel 5 - Entwurf %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Statistische Effektivität als Fundament effizienter Lebensdauertests} \label{chap:entwurf}

Nach der qualitativen Vorselektion relevanter Einflussgrößen (Kapitel~\ref{chap:screening}) fokussiert die nachfolgende Betrachtung nun die quantitative \ac{DoE}-Planung.
Die zentrale wissenschaftliche Zielsetzung besteht in der Entwicklung adäquater, multivariater Lebensdauertests, welche etablierte Standard-\acp{RSD} gezielt an die spezifischen Anforderungen der Lebensdauerprognose anpassen.
Das \ac{CCD} bildet hierbei das methodische Fundament, da es durch seine sequenzielle Augmentierbarkeit (Screening $\to$ lineares Modell $\to$ quadratische Modellterme) im Kontext kostenintensiver \ac{EoL}-Tests eine unübertroffene operative Flexibilität bietet (vgl. Abschnitt~\ref{sec:forschungsfragen}).

Um den Paradigmenwechsel von klassischen, starren \ac{DoE}-Ansätzen hin zu hochgradig effizienten Lebensdauertests methodisch abzusichern, gliedert sich dieses Kapitel in vier aufeinander aufbauende Schwerpunkte - ausgehend von einer idealisierten Theorie (Normalverteilung, \ac{OLS}) hin zur komplexen, gekoppelten Realität der Zuverlässigkeitstechnik (Weibull, \ac{MLE}, Prüfkosten):

Zunächst wird in Abschnitt \ref{sec:abweichungen} die statistische Sensitivität des ungestörten Designs evaluiert, um die Robustheit von Standard-Versuchsplänen gegenüber Praxisrestriktionen zu prüfen.
Unabhängig von einer Beschleunigung via \ac{ALT} wird quantifiziert, wie das \ac{CCD} auf typische, unbeabsichtigte Restriktionen - wie Anlagenlimits, asymmetrische Ausfälle oder Streuungen der Effekte - reagiert.
Mittels Monte-Carlo-Simulationen (\ac{MCS}) auf Basis eines fiktiv-physikalischen \textit{Ground-Truth}-Modells wird nachgewiesen, ab welchem Grad solche Orthogonalitätsabweichungen die Schätzpräzision und insbesondere die statistische Trennschärfe (\ac{power}) kompromittieren.
Das daraus gewonnene Bewusstsein über die erstaunliche Robustheit des \ac{CCD} legitimiert in einem zweiten Schritt (Abschnitt~\ref{sec:adaptive_planung}) den gezielten, asymmetrischen Eingriff in die Planmatrix im Sinne einer adaptiven Versuchsplanung.
Hierbei wird untersucht, wie durch den Verzicht redundanter Versuche "freigewordene" Prüfkapazitäten als strategische Brückenpunkte genutzt werden können, um die \ac{SPV} bei der Extrapolation in den realen Feldbereich zu minimieren.
Zur richtungsabhängigen Veranschaulichung dieser Extrapolationsabsicherung wird eine dedizierte visuelle Bewertungsmethodik, der \ac{EVG}, eingeführt.

Darauf aufbauend werden die bis dato unter Normalverteilungsannahme abgeleiteten Design-Modifikationen in Abschnitt~\ref{sec:weibull_transfer} auf multivariate Lebensdauermodelle übertragen.
Anhand lebensdauerverteilter Daten $\sym{t} \sim \sym{W}(\sym{T}, \sym{b})$ und der Parameteridentifikation via \ac{MLE} wird validiert, ob das \ac{CCD} auch mit adaptivem Brückenbau und unter erschwerten Bedingungen von Zensierungen und Lebensdauerskalierungen seine überlegene Prognosefähigkeit behält, welche maßgeblich über die resultierenden Konfidenzintervalle im Prognosebereich (Feldlast-Niveau) evaluiert wird.
Den Abschluss bildet in Abschnitt \ref{sec:kostenmodell} eine ökonomische Effizienz- und Kostenmodellierung, die das holistische Bild vervollständigt.
Indem die resultierenden Testlaufzeiten in reale Prüfstandskosten übersetzt werden, wird die statistische Testgüte - repräsentiert durch \ac{power} und \ac{SPV} - direkt mit dem monetären Aufwand verknüpft.
Nur durch diesen finalen Trade-off lässt sich die zentrale Kernfrage der Effizienz beantworten: \textit{Wie viel Trennschärfe ist finanzierbar?}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Effektivität von Versuchsplänen mit Orthogonalitätsabweichungen} \label{sec:abweichungen}
Generell können sich Orthogonalitätsabweichungen durch physikalische Restriktionen der technischen Versuchsausführung, die spezifische Performance und Funktionalität des Systems oder Produkts oder durch möglicherweise bewusste Manipulation des Versuchsraums im Kontext von \ac{ALT} zur Steigerung der Extrapolationsgüte ergeben.
Insbesondere bei Lebensdauerprognosen sind solche Eingriffe praxisrelevant und begründet: Die Verschiebung der Versuchspunkte in Richtung höherer Lastniveaus verkürzt die Prüfzeit und senkt die Kosten, während eine Verortung nahe dem Operationsbereich die Modellanpassung verbessert und die Extrapolationsunsicherheit reduziert.

Allerdings bergen solche Modifikationen das Risiko, die statistische Integrität des Designs zu beeinträchtigen.
Im Idealfall sind die Design-Matrix-Spalten eines \ac{CCD} orthogonal zueinander, was eine unabhängige Schätzung der Modellparameter ermöglicht - vgl. Abschnitt~\ref{subsubsec:rsm}.
Geometrische Verzerrungen führen zwangsläufig zu einem Verlust dieser Orthogonalität (vgl. Gleichung~\ref{eq:skalar}).
Besonders kritisch ist, dass die Auswirkungen peripherer Parameterstufen (\acp{SP}) im augmentierten Ansatz erst spät im sequenziellen Prozess sichtbar werden - nämlich dann, wenn das initial rein \textit{faktorielle} Design um die \acp{SP} erweitert wird.
Daher ist es essenziell, die Robustheit des \ac{CCD} gegenüber typischen Design-Abweichungen systematisch zu quantifizieren.
Entgegen bereits erfolgter Untersuchungen durch \textcite{Box.1963}, \textcite{Ahn.2015}, \textcite{Donev.2004}, \textcite{Zhang.2021} sowie \textcite{Jang.2002} oder vergleichbare Arbeiten liegt der Fokus hierbei allerdings nicht auf der reinen geometrischen Verzerrung und der Analyse sowie Bewertung durch geeignete Optimalitätskriterien (entsprechend Abschnitt~\ref{subsec:optimal}).
Die hier erwähnten Beiträge konzentrieren sich primär auf die geometrische Charakterisierung der Design-Abweichungen und deren Einfluss auf die Struktur der Design-Matrix bzw. geeigneter Optimalätskriterien.
Vielmehr soll eine Quantifizierung der Auswirkungen auf die Schätzpräzision und insbesondere die Trennschärfe im Kontext von Lebensdauerprognosen erfolgen.
Die Bewertung mittels Optimalitätskriterien wäre aus genannten Gründen ohnehin nicht zielführend, da im Umkehrschluss die Erfüllung der Kriterien im Planungsprozess erneut Unkenntnis über die tatsächlichen Auswirkungen auf die Schätzpräzision und Trennschärfe erzeugen würde.
Dies hat zur Folge, dass zunächst eine numerische Simulation erforderlich ist, um die Auswirkungen von Design-Abweichungen auf die statistische Güte quantifizieren zu können, bevor überhaupt eine Bewertung der Design-Alternativen erfolgen kann.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Methodik zur simulativen Ermittlung von Auswirkungen durch Orthogonalitätsabweichungen} \label{subsec:simulationsmethodik}
Um die Auswirkungen von Design-Abweichungen auf die statistische Güte quantifizieren zu können, bedarf es einer kontrollierten Untersuchungsumgebung, in der - im Gegensatz zu physischen Experimenten - die \textbf{wahre} Natur des Systems global bekannt ist.
Durch den Vergleich von beiderseits Modellierungs- als auch Analyse-Ergebnissen mit einer vorab definierten Wahrheit (engl. \textit{Ground Truth}) lassen sich \acp{KPI} wie die Schätzpräzision und allen voran die \ac{power} bestimmen.
Zu diesem Zweck wird für die Untersuchungen eine allgemeingültige Simulationsumgebung geschaffen, die auf einem scheinbar-physikalischen Modell basiert \cite{Arndt.2023,Arndt.2022c}.\\

\textbf{Pseudo-physikalisches Simulationsmodell} \\
Das zugrundeliegende Systemverhalten im Sinne einer \textit{Ground Truth} wird durch ein polynomiales Regressionsmodell zweiter Ordnung approximiert.
Dies entspricht exakt der Struktur, die als Ergebnis einer \ac{RSM}-Auswertung (vgl. Gleichung~\ref{eq:quadric_std_model}) erwartet wird und welche die Basis für die Lebensdauer- bzw. Zuverlässigkeitsmodellierung eines technischen Produktes bildet.
Um die Anforderungen an eine multivariate Untersuchung ($\sym{k} \ge 2$) zu erfüllen und die Komplexität handhabbar zu halten, wird im Basisszenario von zwei Faktoren ($\sym{x}_1, \sym{x}_2$) ausgegangen.
Die Begrenzung auf $\sym{k}=2$ Faktoren stellt zugleich eine bewusste, konservative \textit{Worst-Case}-Betrachtung dar, da sich die negativen Auswirkungen einzelner geometrischer Störfälle mit zunehmender Dimensionalität des Versuchsplans signifikant verdünnen, weil die Informationsmatrix durch eine absolut höhere Anzahl intakter Stützpunkte robuster abgestützt werden würde \cite{Arndt.2022}.
Das Modell berücksichtigt neben den Haupteffekten explizit die für die Lebensdaueranalyse kritischen Wechselwirkungen ersten Grades sowie quadratische Terme zur Abbildung von Nichtlinearitäten:

\begin{equation} \label{eq:simulation_model}
    \sym{y}_{\sym{idx_sim}} = \sym{beta}_{0} + \sym{beta}_{1}\sym{x}_{1} + \sym{beta}_{2}\sym{x}_{2} + \sym{beta}_{12}\sym{x}_{1}\sym{x}_{2} + \sym{beta}_{11}\sym{x}_{1}^2 + \sym{beta}_{22}\sym{x}_{2}^2 + \sym{epsilon}.
\end{equation}

Um zunächst ein vollständig stochastisches Verhalten abbilden zu können, sind die Modellparameter nicht deterministisch fixiert.
Den Regressionskoeffizienten $\symsub{beta}{idx_j}$ werden zwar nominale Startwerte zugewiesen (vgl. Tabelle~\ref{tab:simulation_parameters}), diese werden jedoch in jedem Simulationslauf mit einer definierten Streuung überlagert.
Zusätzlich wird die resultierende Systemantwort $\sym{y}_{\sym{idx_sim}}$ mit einer Reststreuung $\sym{epsilon}$ beaufschlagt, um die inhärente \textbf{aleatorische Unsicherheit} des Systems (Messfehler, unbekannte Einflüsse) abzubilden.
Mathematisch wird dies proportional durch normalverteilte Zufallsvariablen modelliert, sodass für den Fehlerterm sowie für die fluktuierenden Modellparameter um ihre nominalen Startwerte ($\sym{beta}_{\sym{idx_j}, \sym{idx_nom}}$) gilt:
\begin{equation} \label{eq:simulation_stochastics}
    \sym{epsilon} \sim \mathcal{N}(0, \symsub{sigma}{idx_sim}^2) \quad \text{und} \quad \symsub{beta}{idx_j} \sim \mathcal{N}(\sym{beta}_{\sym{idx_j}, \sym{idx_nom}}, \symsub{sigma}{idx_sim}^2).
\end{equation}

Die bewusste Wahl der Normalverteilung als zugrundeliegende Stochastik ist für diese isolierte Designuntersuchung methodisch zwingend und in einem ersten Schritt zweckdienlich:
Sie entspricht der elementaren Grundannahme klassischer Regressionsmodelle (Gauß-Markow-Theorem) und stellt sicher, dass die nachfolgende statistische Inferenz (Berechnung von $\sym{p-Wert}$ und \acp{CI}) im ungestörten Basis-Design ideal und erwartungstreu abläuft.
So wird gewährleistet, dass später beobachtete Schätzfehler oder $\sym{power}$-Verluste ausschließlich auf die induzierten geometrischen Orthogonalitätsabweichungen (Design-Penalties) zurückzuführen sind und nicht durch systematische, verteilungsbedingte Residuenfehler (wie beispielsweise durch Form- und Lageparameter der Weibull-Verteilung \cite{Meeker.2022}) verfälscht werden.

\begin{table}[htbp]
    \centering
    \caption{Nominale Startwerte und Streuparameter des pseudo-physikalischen Simulationsmodells}
    \label{tab:simulation_parameters}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{@{}lr@{}}
        \toprule
        \textbf{Modellparameter}                                                           & \textbf{Default-Wert} \\
        \midrule
        Regressionskonstante $\sym{beta}_{0}$                                              & $30$                  \\
        Regressionskoeffizienten $\symsub{beta}{idx_j}$ (für $j \in \{1, 2, 12, 11, 22\}$) & $10$                  \\
        Standardabweichung der Systemantwort $\symsub{sigma}{idx_sim}$                     & $1$                   \\
        \bottomrule
    \end{tabular}
\end{table}

\textbf{Simulationsalgorithmus und Monte-Carlo-Ansatz} \\
Das Modell der \textit{Ground Truth} wird zunächst mit den zu untersuchenden Versuchsplänen gekoppelt.
Für jeden Stützpunkt des Versuchsplans (die Versuchspunkte) werden die korrespondierenden Systemantworten simuliert.
Anschließend erfolgt die Rückrechnung des Regressionsmodells mittels Parameterschätzverfahren (\ac{OLS}, \ac{MLE}), entsprechend der ausgeführten Methodik aus Abschnitt~\ref{subsubsec:mle}.
Um statistisch belastbare Aussagen zu treffen und \textbf{epistemische Unsicherheiten} auf einen minimalen numerischen Approximationsfehler zu reduzieren, wird das Verfahren in eine \ac{MCS} mit $\symsub{n}{idx_MC} = 1\times10^4$ Iterationen eingebettet - vergleiche Abschnitt~\ref{subsec:optimal} für entsprechende Hintergründe.
Dieser Ansatz erlaubt die Bewertung zweier zentraler Kriterien:
\begin{enumerate}
    \item \textbf{Präzision der Parameterschätzung:} Durch den (Mittelwert-) Vergleich der geschätzten Parameter $\hat{\sym{Beta}}$ mit den \textit{wahren} Koeffizienten $\sym{Beta}_{\text{true}}$ lassen sich systematischer Schätzfehler (\textbf{Bias}) und Varianz (\textbf{\ac{MSE}}) der Schätzung ermitteln.
    \item \textbf{Bestimmung der Trennschärfe $\sym{power}$:} Für jeden Simulationslauf wird geprüft, ob die signifikanten Effekte korrekt erkannt wurden ($\sym{p-Wert} < \sym{alpha}$). Das Verhältnis dieser Treffer zur Gesamtzahl der Simulationen ergibt die empirische \ac{power} (vgl. Gleichung~\ref{eq:power}).
\end{enumerate}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Sensitivitätsanalyse geometrischer und stochastischer Einflussgrößen} \label{subsec:sensitivitaetsanalyse}
Um zunächst die Robustheit des methodischen Ansatzes gegenüber den eingangs erwähnten Einflüsse zu bewerten, wird eine Sensitivitätsanalyse durchgeführt, welche einem \textbf{\ac{DoE} on Experimental Design}-Ansatz folgt:
Die Eigenschaften des Versuchsplans sowie die der \textit{Ground Truth} werden als Faktoren eines übergeordneten Meta-Designs derart variiert, sodass deren Einfluss auf die Effektivität der Schätzung und damit auf insbesondere die Trennschärfe der Effekte im \ac{GLM} quantifiziert werden kann \cite{Arndt.2023}.
Hierfür wird eine aus praxisnahen Überlegungen abgeleitete Auswahl geometrischer Manipulationen, stochastischer Einflüsse sowie methodischer Parameter herangezogen:
\begin{enumerate}
    \setlength{\itemsep}{0pt}
    \setlength{\parskip}{0pt}
    \item Manipulation von \acp{SP} über deren axialen Abstand;
    \item Manipulation von \acp{SP} durch co-faktorielle Korrelation;
    \item Variation der Anzahl der \acp{ZP};
    \item Variation der Anzahl der \acp{SP};
    \item Variation der Standardabweichung der Effektstärken (Effekt-Rauschen);
    \item Änderung des relativen Verhältnisses von Effektstärken untereinander;
    \item Variation der Standardabweichung der Systemantwort (System-Rauschen, auch \ac{SNR});
    \item Variation des Signifikanzniveaus.
\end{enumerate}
Insbesondere die Berücksichtigung der Verhältnismäßigkeit von Effektstärken sowie der Rauschparameter ist von zentraler Bedeutung, da sie die \ac{SNR} im Modell direkt beeinflussen und damit die Detektionswahrscheinlichkeit der Effekte maßgeblich steuern, gleichzeitig aber eine gegen unendlich tendierende Variationsmöglichkeit bieten, die in einem reinen Screening-Ansatz nicht abgedeckt werden könnte.
Zur formalen Beschreibung des Untersuchungsraums werden diese acht Einflussgrößen im Vektor $\sym{xi_vek} = [\symsub{xi_factor}{idx_fac_a},\dots, \symsub{xi_factor}{idx_fac_h}]^\top$ zusammengefasst und in Tabelle~\ref{tab:sensitivitaetsfaktoren} definiert.
Die Integration dieser Faktoren in das Meta-Design erfolgt dabei methodisch differenziert: Während die kontinuierlichen Stör- und Geometrieparameter ($\symsub{xi_factor}{idx_fac_a}, \symsub{xi_factor}{idx_fac_b}, \symsub{xi_factor}{idx_fac_e}, \symsub{xi_factor}{idx_fac_f}, \symsub{xi_factor}{idx_fac_g}, \symsub{xi_factor}{idx_fac_h}$) den Versuchsraum eines \ac{CCD} aufspannen, um stetige Sensitivitäten (Regressionskoeffizienten) zu schätzen, werden die strukturellen Designgrößen der Stützstellenanzahl ($\symsub{xi_factor}{idx_fac_c}, \symsub{xi_factor}{idx_fac_d}$) als kategoriale Blockfaktoren über drei diskrete Stufen behandelt.
Diese Trennung ist zwingend geboten, da Variationen in der Stützstellenanzahl die topologische Struktur der Design-Matrix $\sym{X}$ und deren Freiheitsgrade diskontinuierlich verändern.
Ein rein kontinuierliches Meta-Modell würde hier zu unzulässigen Interpolationen über strukturelle Singularitäten hinweg führen - beispielsweise beim vollständigen Verlust der Schätzbarkeit quadratischer Terme bei extremer Ausdünnung.
Folglich werden die kontinuierlichen Sensitivitäten der Rauschparameter stets \textit{innerhalb} konsistenter topologischer Design-Strukturen ermittelt, während der Haupteffekt der Stützstellenreduktion separat über die diskreten Mittelwertverschiebungen (Level-Shifts) der \ac{power} zwischen den Blöcken quantifiziert wird.
Weiter erfüllt der \ac{DoE}-Ansatz mittels \ac{CCD} den Anspruch, für die Sensitivitätsanalyse eine effiziente und notwendige statistische Inferenz und Kausalität zu identifizieren, wohingegen alternative Ansätze wie die reine Korrelationsanalyse, \textit{Space-Filling Designs} (wie \textit{Latin Hypercube Sampling} oder \textit{Sobol}-Sequenzen) oder die Anwendung von \ac{ML}-Modellen zur Schätzung von Sensitivitäten darüber hinaus Assoziationen für explorative Prädiktionen liefern würden \cite{Zio.2013,Kleppmann.2020}.

Etwaige Ergänzungen wie die Variation der Anzahl der Faktoren, die Manipulation der faktoriellen Versuchspunkte oder die Einführung von Lebensdauermodellen werden dabei bewusst ausgeschlossen, um die Komplexität des Meta-Designs handhabbar zu halten und die Interpretation der Ergebnisse durch eine pragmatische Auswahl und im Fokus der in Kapitel~\ref{chap:ansatz} postulierten Zielsetzung zu erleichtern.
\begin{table}[htbp]
    \centering
    \caption{Definition und Parametrisierung der Untersuchungsfaktoren für die Sensitivitätsanalyse}
    \label{tab:sensitivitaetsfaktoren}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{@{}l p{0.45\textwidth} l l@{}}
        \toprule
         & \textbf{Beschreibung}                                                  & \textbf{Wertebereich} & \textbf{Charakteristik} \\
        \midrule

        $\symsub{xi_factor}{idx_fac_a}$
         & \textbf{\ac{SP}-Abstand ($\symsub{alpha}{idx_D}$)} \newline
        Variation des Abstands der \acp{SP} zum Zentrum (z.B. flächenzentriert, rotierbar und extendiert).
         & $[1,00;\, 2\cdot\symsub{alpha}{idx_D}]$
         & Kont. (\ac{CCD})                                                                                                         \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_b}$
         & \textbf{\ac{SP}-Shift} \newline
        Asymmetrische / Co-Faktorielle Verschiebung eines \ac{SP}s zur Simulation geometrischer Verzerrungen und Faktoren-Korrelation.
         & $[0,10;\, \phantom{0}\phantom{0}\symsub{alpha}{idx_D}]$
         & Kont. (\ac{CCD})                                                                                                         \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_c}$
         & \textbf{\acp{ZP} ($\symsub{n}{idx_C}$)} \newline
        Variation der Anzahl der \acp{ZP} $\symsub{n}{idx_C}>0$.
         & $[1,00;\, \phantom{0}8,00]$
         & Diskret (Faktoriell)                                                                                                     \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_d}$
         & \textbf{\acp{SP} ($\symsub{n}{idx_S}$)} \newline
        Variation durch Entfall von maximal $50\,\%$ der \acp{SP} (z.B. durch Zensierung oder Funktionalitätsverlust).
         & $[2,00;\, \phantom{0}4,00]$
         & Diskret (Faktoriell)                                                                                                     \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_e}$
         & \textbf{Effekt-Rauschen} \newline
        Verhältnis zwischen pseudo-physikalischem Modellkoeffizienten und Systemabweichung bzw. Effektstreuung $\symsub{beta}{idx_i}/\sym{sigma}$.
         & $[0,80;\,\phantom{0} 1,20]$
         & Kont. (\ac{CCD})                                                                                                         \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_f}$
         & \textbf{Effekt-Verhältnis ($\sym{beta}_1/\sym{beta}_2$)} \newline
        Verhältnis zweier Haupteffekte zueinander: zwischen gleichwertiger Effektstärke oder einem Anteil von $10\,\%$.
         & $[0,10;\,\phantom{0} 1,00]$
         & Kont. (\ac{CCD})                                                                                                         \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_g}$
         & \textbf{Mess-Rauschen ($\sym{snr}$: $\sym{y}/\sym{epsilon}$)} \newline
        Verhältnis zwischen den Modellkoeffizienten und dem Wert des Streuparameters der Systemantwort.
         & $[0,80;\, \phantom{0}1,20]$
         & Kont. (\ac{CCD})                                                                                                         \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_h}$
         & \textbf{Signifikanzniveau ($\sym{alpha}$)} \newline
        Das gewählte Signifikanzniveau.
         & $[0,02;\, \phantom{0}0,08]$
         & Kont. (\ac{CCD})                                                                                                         \\

        \bottomrule
    \end{tabular}
\end{table}

So werden schließlich Haupteffekte, Interaktionen sowie quadratische Effekte auf die Trennschärfe als wesentlicher Performance-\ac{KPI} und damit Metrik zur Effektivität ermittelt.

\subsection{Ergebnisse der Sensitivitätsanalyse und Quantifizierung der Trennschärfe} \label{subsec:ergebnisse_sensitivitaet}


Die simulative Untersuchung der Trennschärfe (\ac{power}) im Meta-Design offenbart eine signifikante Dichotomie zwischen aktiven, \textbf{dispositiven Designfaktoren} (Geometrie, Stützstellenanzahl) und unveränderlichen, \textbf{externen Systemrestriktionen} (Rauschen, Signifikanzniveau).
Tabelle~\ref{tab:summary_power_sensitivity} fasst die Ergebnisse der \ac{ANOVA} zusammen, wobei die signifikante Effektstärke $\sym{Eff}$ als Maß für die Dominanz eines Faktors dient (Detailauswertungen siehe Anhang~\ref{app:c}).

Wird zunächst die globale \textbf{Modell-Power} ($\sym{power}_{\sym{idx_M}}$) betrachtet, zeigt sich eine extrem hohe Robustheit gegenüber allen Variationen mit einer mittleren Trennschärfe von $\overline{\sym{power}} > 99{,}7\,\%$.
Diese pauschale Metrik verdeckt jedoch, dass das Verhalten spezifischer Einzel-Effekte - insbesondere die Identifikation der für die Lebensdauerprognose kritischen quadratischen Terme - deutlich sensitiver ausfällt.
Wie Tabelle~\ref{tab:summary_power_sensitivity} zeigt, liegt die mittlere Trennschärfe für quadratische Terme ($\sym{power}_{\sym{beta}_{11}}, \sym{power}_{\sym{beta}_{22}}$) nur noch im Bereich von $70\,\%$ bis $79\,\%$.
Auch die Trennschärfe der linearen Haupteffekte wird durch die Variation der Einflussgrößen maßgeblich gesteuert, was sich in einem breiten Wertebereich von $37{,}3\,\%$ bis $90{,}8\,\%$ widerspiegelt.
Die entscheidende Erkenntnis liefert jedoch der Blick auf die \textbf{Sensitivitäts-Indizes} (Spalte „Dominante Sensitivität“).
Für die Mehrheit der untersuchten Koeffizienten ($\sym{power}_{\sym{idx_M}}$, $\sym{power}_{\sym{beta}_1}$, $\sym{power}_{\sym{beta}_{12}}$, $\sym{power}_{\sym{beta}_{11}}$) ist nicht etwa eine unzureichende Versuchspunktanzahl der limitierende Faktor, sondern das \textbf{Effektstärke-Verhältnis ($\symsub{xi_factor}{idx_fac_f}$)} sowie das \textbf{Messrauschen ($\symsub{xi_factor}{idx_fac_g}$)}.
Mit einem Sensitivitätsindex von bis zu $\sym{Eff}=2{,}53$ dominiert das physikalische Systemverhalten die Erfolgswahrscheinlichkeit des Tests.
Da das Rauschen sowie das Signifikanzniveau ($\symsub{xi_factor}{idx_fac_h}$) bei \ac{EoL}-Tests an bestehenden Prüfständen zumeist feste Randbedingungen darstellen, definieren sie eine natürliche Obergrenze der erreichbaren Trennschärfe, die auch durch noch so ausgedehnte Versuchspläne nicht durchbrochen werden kann.

\begin{table}[htbp]
    \centering
    \caption{Zusammenfassende Statistik der Trennschärfe und Identifikation der zugehörigen, jeweils dominierenden Einflussfaktoren. Die Ergebnisse demonstrieren, dass die Power maßgeblich durch unveränderliche Systemgrößen (Rauschen) und nur im Ausnahmefall durch modifizierbare Designgrößen limitiert wird.}
    \label{tab:summary_power_sensitivity}
    \renewcommand{\arraystretch}{1.2}
    \small
    \begin{tabular}{@{}l r r c r@{}}
        \toprule
        \textbf{Koeffizient}            & \textbf{Mittelwert}      & \textbf{Wertebereich}          & \multicolumn{2}{c}{\textbf{Zugehörig dominierende Sensitivität}}                        \\
        \cmidrule(lr){2-3} \cmidrule(lr){4-5}
                                        & $\overline{\sym{power}}$ & $[\text{min}; \text{max}]\,\%$ & \textbf{Einflussfaktor $\symsub{xi_factor}{idx_i}$ (Kategorie)}  & \textbf{$\sym{Eff}$} \\
        \midrule
        \multicolumn{5}{c}{\textit{Globale Modellgüte:}}                                                                                                                                      \\
        $\sym{power}_{\sym{idx_M}}$     & $99,7\,\%$               & $[94,5; 100,0]$                & $\symsub{xi_factor}{idx_fac_g}$ (System)                         & $0,10$               \\
        \addlinespace
        \midrule
        \multicolumn{5}{c}{\textit{Spezifische Modelleffekte:}}                                                                                                                               \\
        $\sym{power}_{\sym{beta}_0}$    & $97,9\,\%$               & $[76,9; 100,0]$                & $\symsub{xi_factor}{idx_fac_e}$ (System)                         & $0,52$               \\
        $\sym{power}_{\sym{beta}_1}$    & $78,6\,\%$               & $[60,0; \phantom{0}90,8]$      & $\symsub{xi_factor}{idx_fac_g}$ (System)                         & $1,50$               \\
        $\sym{power}_{\sym{beta}_2}$    & $66,0\,\%$               & $[37,3; \phantom{0}84,1]$      & $\symsub{xi_factor}{idx_fac_f}$ (System)                         & $2,53$               \\
        $\sym{power}_{\sym{beta}_{12}}$ & $72,6\,\%$               & $[52,5; \phantom{0}85,9]$      & $\symsub{xi_factor}{idx_fac_g}$ (System)                         & $1,93$               \\
        $\sym{power}_{\sym{beta}_{11}}$ & $70,9\,\%$               & $[32,6; \phantom{0}86,8]$      & $\symsub{xi_factor}{idx_fac_g}$ (System)                         & $1,99$               \\
        $\sym{power}_{\sym{beta}_{22}}$ & $79,3\,\%$               & $[33,4; 100,0]$                & $\symsub{xi_factor}{idx_fac_a}$ \textbf{(Design)}                & $3,72$               \\
        \bottomrule
        \multicolumn{5}{@{}l}{\footnotesize $\sym{Eff}$: Absoluter Effekt des Faktors auf die Power (aus ANOVA zum Meta-Design).}
    \end{tabular}
\end{table}

Aus dieser Gegenüberstellung kristallisiert sich eine fundamentale Erkenntnis für die Versuchsökonomie heraus: Wenn die Nachweiswahrscheinlichkeit der linearen Haupteffekte ohnehin durch nicht-dispositive Systemrestriktionen limitiert wird, ist zu prüfen, ob eine gezielte Ausdünnung des Versuchsplans (Reduktion der Stützstellen $\symsub{xi_factor}{idx_fac_c}$ und $\symsub{xi_factor}{idx_fac_d}$) die statistische Aussagekraft in der Praxis überhaupt signifikant kompromittiert.
Tabelle~\ref{tab:impact_cd} quantifiziert diesen passiven „\textit{Design-Penalty}“.

\begin{table}[htbp]
    \centering
    \caption{Systematischer Trennschärfe-Verlust ($\Delta \sym{power}$) bei Reduktion der Versuchsstützpunkte. Während lineare Haupteffekte relativ robust gegenüber einer Design-Ausdünnung bleiben, zeigt sich der primäre \textit{Design-Penalty} in der Schätzung der quadratischen Terme.}
    \label{tab:impact_cd}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{@{}l rr@{}}
        \toprule
        \textbf{Koeffizient}            & \multicolumn{2}{c}{\textbf{Durchschnittlicher Trennschärfe-Verlust ($\Delta \sym{power}$)}}                                                                              \\
        \cmidrule(l){2-3}
                                        & \textbf{bei $\symsub{xi_factor}{idx_fac_c}$ ($\symsub{n}{idx_C}=8 \to 1$)}                  & \textbf{bei $\symsub{xi_factor}{idx_fac_d}$ ($\symsub{n}{idx_S}=4 \to 2$)} \\
        \midrule
        \multicolumn{3}{@{}c}{\textit{Konstante, Haupt- und Interaktionseffekte}}                                                                                                                                  \\
        $\sym{power}_{\sym{beta}_0}$    & $-4,5\,\%$                                                                                  & $-0,3\,\%$                                                                 \\
        $\sym{power}_{\sym{beta}_1}$    & $-2,4\,\%$                                                                                  & $-6,0\,\%$                                                                 \\
        $\sym{power}_{\sym{beta}_2}$    & $+1,1\,\%$\rlap{$^*$}                                                                       & $-8,0\,\%$                                                                 \\
        $\sym{power}_{\sym{beta}_{12}}$ & $-1,2\,\%$                                                                                  & $-0,9\,\%$                                                                 \\
        \addlinespace
        \midrule
        \multicolumn{3}{@{}c}{\textit{Quadratische Effekte}}                                                                                                                                                       \\
        $\sym{power}_{\sym{beta}_{11}}$ & $-13,9\,\%$                                                                                 & $-12,8\,\%$                                                                \\
        $\sym{power}_{\sym{beta}_{22}}$ & n.\,a.\rlap{$^{**}$}                                                                        & $-6,1\,\%$                                                                 \\
        \bottomrule
        \multicolumn{3}{l}{\footnotesize $^*$ \phantom{$^*$}Positiver Wert resultiert aus Varianzverschiebungen bei instabilen Designs.}                                                                           \\
        \multicolumn{3}{l}{\footnotesize $^{**}$ Nicht auswertbar, da Singularität in quadratischem Term bei $\symsub{n}{idx_C}$.}
    \end{tabular}
\end{table}

Die numerische Bilanzierung belegt eine erstaunliche Robustheit des \ac{CCD}-Skeletts: Der Verzicht auf redundante Zentralpunkte oder die Ausdünnung der Sternpunkte zieht für die Haupteffekte und Interaktionen lediglich einen marginalen Trennschärfe-Verlust nach sich (z.\,B. $\Delta \sym{power} = -2{,}4\,\%$ bis $-8{,}0\,\%$).
Der signifikante Schwachpunkt dieser Reduktion entlädt sich fast ausschließlich in den quadratischen Termen (bis zu $-13{,}9\,\%$ Einbruch bei $\sym{power}_{\sym{beta}_{11}}$).

Hierin verbirgt sich die zentrale mathematische Rechtfertigung für die im weiteren Studienverlauf verfolgte Strategie gezielter Design-Manipulationen:
Einerseits zeigt Tabelle~\ref{tab:impact_cd}, dass die Reduktion klassischer Stützstellen die Detektion von Haupteffekten kaum gefährdet.
Dies erzeugt ein ungenutztes \textbf{geometrisches Gestaltungsbudget}, da diese Punkte für die reine Effektidentifikation hochgradig ineffizient sind.
Andererseits belegt Tabelle~\ref{tab:summary_power_sensitivity} eindrücklich, dass die Geometrie (speziell der Sternpunktabstand $\symsub{xi_factor}{idx_fac_a}$) mit einem herausragenden Index von $\sym{Eff}=3{,}72$ den massivsten Hebel auf exakt jene quadratischen Terme besitzt, die bei passiver Stützstellenreduktion am stärksten leiden.
Dieses ungenutzte Budget sollte folglich nicht zur reinen Kostensenkung gestrichen, sondern durch proaktive Design-Manipulationen (asymmetrische Ankerpunkte) umgewidmet werden. Dies gilt umso mehr, als das physikalische Systemverhalten (Effektstärken und Rauschniveau) in realen \ac{ALT}-Szenarien naturgemäß als unveränderliche Randbedingung zu akzeptieren ist. Nur so lässt sich der enorme geometrische Hebel nutzen, um die quadratische Modellkrümmung abzustützen und die Extrapolationsvarianz im Feldbereich gezielt zu minimieren.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Generische Auswirkungen relevanter Orthogonalitätsabweichungen} \label{subsec:ergebnisse_generic}

Die Ergebnisse der vorangegangenen Sensitivitätsanalyse (siehe Abschnitt~\ref{subsec:ergebnisse_sensitivitaet}) zeigen, dass das \ac{CCD} im Hinblick auf die statistische Trennschärfe ein robustes Verhalten gegenüber Modifikationen seiner Stützstellenanzahl aufweist, da im Regelfall das experimentelle System- und Messrauschen den limitierenden Faktor der Effektivität darstellt (vgl. Tabellen~\ref{tab:summary_power_sensitivity} und~\ref{tab:impact_cd}).
Für die praktische \ac{EoL}-Versuchsplanung greift dieser generalisierte, kombinatorische Ansatz jedoch noch zu kurz.
In der Realität am Prüfstand treten Abweichungen von der idealen Design-Matrix technisch bedingt oft nicht als geplante, systematische Ausdünnung auf, sondern als unvorhergesehene, asymmetrische und isolierte Einzelphänomene, erzwungen durch physikalische oder normative Restriktionen \cite{Arndt.2022,Mell.2022,Box.1963,Ahn.2015}.

Gleichzeitig eröffnet das Bewusstsein über die erstaunlich geringen \textit{Design-Penalties} aus Abschnitt~\ref{subsec:ergebnisse_sensitivitaet} die Chance für eine gezielte adaptive Planung:
Wenn Design-Modifikationen die Effektivität des Nachweises ohnehin nur moderat beeinträchtigen und insbesondere für Terme höherer Ordnung durch die Platzierung von \acp{SP} vorteilhaft gestaltbar sind (vergleiche Faktor $\symsub{xi_factor}{idx_fac_a}$ in Tabelle~\ref{tab:summary_power_sensitivity}), ist zu prüfen, inwiefern geometrische Manipulationen zur Kostenoptimierung, Prüfzeitverkürzung oder zur Vermeidung irrelevanter Ausfallmechanismen nicht sogar bewusst induziert werden sollten.

Um diese singulären Praxisphänomene systematisch bewerten zu können, ist die kombinatorische Betrachtung des Meta-Designs nicht mehr zielführend.
Zwar ist der Parameterraum an denkbaren geometrischen Abweichungen und deren Kombinationen in der Realität nahezu unendlich und hochgradig abhängig von produktspezifischen, technologischen sowie modellspezifischen Randbedingungen. Ein allgemeingültiger, analytischer oder erschöpfend numerischer Beweis für sämtliche Permutationen ist folglich weder möglich noch methodisch erstrebenswert.
Der wissenschaftliche Anspruch an dieser Stelle liegt vielmehr darin, anhand isolierter, archetypischer Störfälle die fundamentalen Wirkmechanismen zwischen den objektiven Design-Metriken (Optimalitäten) und der resultierenden Trennschärfe aufzudecken.
Aus dem Verständnis dieser kausalen Zusammenhänge lassen sich schließlich generalisierbare, methodische Anreize für die \textit{Entwicklung effizienter multivariater Lebensdauerversuche} ableiten.
Dementsprechend werden basierend auf der Simulationsmethodik aus Abschnitt~\ref{subsec:simulationsmethodik} vier spezifische, voneinander isolierte Störszenarien definiert, welche typische Einzelrestriktionen in multivariaten Lebensdauerversuchen abbilden \cite{Arndt.2022,Mell.2022}:

\begin{itemize}
    \item \textbf{Fall I (Axialer Shift):} \acp{SP} ($\symsub{n}{idx_S}\geq 1$) können aufgrund von Anlagenrestriktionen nicht auf dem nominellen Abstand $\symsub{alpha}{idx_D}$ gefahren werden oder würden einen durch \ac{ALT} induzierten Wechsel des Ausfallmechanismus provozieren.
          Zur methodischen Einordnung wird dieser Fall in drei Stufen simuliert: eine axiale Stauchung auf $80\,\%$, eine Streckung auf $120\,\%$ sowie der Rückzug auf die Nennlasten des rein faktoriellen Würfels (\textit{\ac{FC}}, $\symsub{alpha}{idx_D}=1$).
    \item \textbf{Fall II (Co-faktorielle Korrelation):} Ein \ac{SP} ist nicht mehr achsenrein isoliert realisierbar. Spezifische, physikalische Systemfunktionalitäten (beispielsweise kombinierte Einflüsse durch Temperatur und mechanische Kraft) könnten die simultane (parasitäre) Aussteuerung eines zweiten Faktors erzwingen, wodurch eine asymmetrische geometrische Korrelation im ansonsten orthogonalen Raum entsteht.
          Dieser Fall wird in einer leichten und einer starken Ausprägung untersucht, indem der Sternpunkt der $\sym{x}_1$-Achse durch einen parasitären Stellgrößenanteil von $\sym{x}_2 = 0{,}1$ sowie $\sym{x}_2 = 1{,}0$ überlagert wird.
    \item \textbf{Fall III (Stellgrößen-Scatter):} Die Aktorik des Prüfstands erlaubt keine exakte Ansteuerung der Zielkoordinaten, sodass die \acp{SP} einer unbeabsichtigten stochastischen Streuung (uniformes Rauschen von $\pm 10\,\%$ auf der Eingangsvariablen $\symsub{x}{idx_i}$) unterliegen.
    \item \textbf{Fall IV (Früh-Ausfall / Zensierung):} Ein Versuchspunkt führt zu einem Frühausfall (ggf. konkurrierender Schadensmechanismus), einem Durchläufer oder kann aus Zeitgründen nicht abgeschlossen werden.
          Der Versuch entfällt somit vollständig aus der finalen Design-Matrix.
          Da die topologische Bedeutung der Punkte stark variiert, wird dieses Szenario zur methodischen Differenzierung in zwei Unterkategorien aufgeschlüsselt:
          \begin{itemize}
              \item \textbf{Fall IV: \ac{ZP}}: Eliminierung geometrisch redundanter \acp{ZP}.
              \item \textbf{Fall IV: \ac{SP}}: Asymmetrische Eliminierung extremer \acp{SP} - hier simuliert am nördlichen \ac{SP} auf der Achse von Faktor $\sym{x}_1$.
          \end{itemize}
\end{itemize}

Wird als Vergleichsbasis zusätzlich zu den Design-Variationen der \textbf{Fälle I-IV} das standardmäßige \textit{Basis \ac{CCD}} berücksichtigt, ergeben sich insgesamt \textbf{neun} Szenarien.
Für diese werden die Auswirkungen der jeweiligen Praxisrestriktion auf die Effektivität der Parameterschätzung und die $\sym{power}$ der Effekte isoliert quantifiziert und verglichen.
Es gilt zu quantifizieren, inwiefern der Verlust der Orthogonalität - messbar über den globalen $\sym{A2}$-Indikator - zu Multikollinearität und folglich zu einer schädlichen Varianzinflation (\ac{VIF} beziehungsweise \ac{MSE}) oder gar zu systematischen Verzerrungen der Koeffizientenschätzung (\textit{Bias}) führt (vgl. Abschnitt~\ref{subsec:optimal}).
Die analytische Differenzierung dieser Fehlerarten ist von entscheidender Bedeutung, da insbesondere ein signifikanter Bias die Validität der gesamten Lebensdauerprognose unbemerkt kompromittieren würde, während eine reine Varianzinflation primär die Konfidenzintervalle aufweitet.

\subsubsection{Kalibrierung der Signal-to-Noise Ratio und analytische Justierung des Arbeitspunkts}

Wie in der vorangegangenen Sensitivitätsanalyse (vgl. Abschnitt~\ref{subsec:ergebnisse_sensitivitaet}) dargelegt, konvergiert die Trennschärfe bei dominierenden physikalischen Effekten (hohes $\sym{snr} \gg 1$) in einen asymptotischen Sättigungsbereich nahe $100\,\%$.
Unter derart idealen Bedingungen ist das Regressionsmodell so robust, dass selbst signifikante geometrische Verzerrungen des Versuchsplans den Nachweis eines Effekts kaum gefährden.
Für die Evaluation subtiler topologischer Schwachstellen ist ein solches Setup folglich inadäquat.
Zudem stellen derart dominante Einflussgrößen in der Praxis der Zuverlässigkeitserprobung ohnehin selten die primäre Herausforderung dar. Vielmehr gilt es zumeist, schwache Degradationserscheinungen und sensible Interaktionen zu identifizieren, die sich nur knapp vom Systemrauschen abheben.

Um den isolierten Einfluss von Orthogonalitätsabweichungen - den spezifischen \textit{Design-Penalty} - überhaupt quantifizierbar zu machen, wird der Arbeitspunkt der Simulation gezielt in einen kritischen Bereich verschoben und an den empirischen Grenzen der Sensitivitätsanalyse ausgerichtet.
Da die Auswertung des Meta-Designs dem Messrauschen ($\symsub{xi_factor}{idx_fac_g}$) einen dominierenden Einfluss auf die Modell-Power ($\sym{Eff}=1{,}99$) attestiert, wird das Basismodell am kritischen oberen Rand dieses Intervalls verankert:
Bei einer fixierten Standardabweichung der Systemantwort von $\sym{sigma} = 5{,}0$ wird das Signal-Rausch-Verhältnis für die schätztechnisch anfälligen Terme zweiter Ordnung auf exakt $\sym{snr} = 1{,}20$ justiert ($|\sym{beta}_{11}| = |\sym{beta}_{22}| = |\sym{beta}_{12}| = 6{,}0$).
Diese Parametrisierung zwingt das ideale Referenz-Design für die quadratischen Effekte und Wechselwirkungen exakt auf die steilste Flanke der sigmoidalen Trennschärfe-Funktion \cite{Cohen.1988,Montgomery.2020}, woraus eine empirische Basis-Trennschärfe von rund $70\,\%$ bis $80\,\%$ resultiert.

Gleichzeitig belegen die Screening-Ergebnisse, dass das relative Verhältnis der Effektstärken ($\symsub{xi_factor}{idx_fac_f}$) die Trennschärfe hochgradig sensitiv steuert ($\sym{Eff}=2{,}53$).
Unter Vorgriff auf das empirisch belegte \textit{Sparsity of Effects Principle} \cite{Montgomery.2020} wird die \textit{analytische Lupe} folglich hierarchisch asymmetrisch aufgeteilt:
Während die Terme zweiter Ordnung den kritischen Schwellenwert ($\sym{snr}$) definieren, fließen die linearen Haupteffekte vom Betrag her proportional dominanter mit $\sym{beta}_1 = \sym{beta}_2 = 12{,}0$ ($\sym{snr} = 2{,}40$) in das Modell ein (vgl. Tabelle~\ref{tab:simulation_parameters_normal}).
Der Durchschnittswert der Konstanten ($\sym{beta}_0 = 24{,}0$) hebt die Systemantworten hierbei in einen anwendungsrelevanten positiven Bereich ($[0; 50]$).
Die Wahl positiver Haupteffekte in Kombination mit negativen Krümmungstermen ist gezielt getroffen: Sie modelliert das in der \ac{RSM} klassische Verhalten eines zu maximierenden Leistungsoptimums (analog zur Zuverlässigkeitstechnik z.\,B. Ausbeute oder Wirkungsgrad), welches durch Sättigungseffekte und antagonistische Wechselwirkungen bei extremen Faktorkombinationen physikalisch begrenzt wird.

Auf eine zusätzliche, stochastische Streuung der wahren Effektkoeffizienten (\textit{Random Effects}) wird im Weiteren bewusst verzichtet.
Wie die Sensitivitätsanalyse belegt, beeinflusst diese die Entdeckungswahrscheinlichkeit ohnehin nur marginal ($\sym{Eff}=0{,}52$).
Eine methodische Integration würde die Untersuchung unnötig in einen \textit{Random-Effects}-Ansatz überführen und die isolierte Evaluation rein geometrischer Design-Eigenschaften mathematisch verfälschen \cite{Montgomery.2020,McCulloch.2001}.

In der Synthese eliminiert die Fixierung dieses künstlichen Arbeitspunkts jegliche statistischen Sättigungseffekte.
Um bei der vergleichenden Gegenüberstellung der Design-Szenarien jedoch nicht nur die relative Abweichung der Trennschärfe ($\Delta \sym{power}$) zu quantifizieren, sondern gleichzeitig den systematischen Schätzfehler (Bias) sowie die empirische Schätzvarianz (\ac{MSE}) der Koeffizienten $\hat{\sym{beta}}$ valide zu beurteilen, ist eine iterative Auswertung zwingend erforderlich.
Hierfür wird ein Monte-Carlo-Ansatz genutzt, der mittels einer strikten \textit{Seed}-Fixierung deterministisch initialisiert wird \cite{Zio.2013}.
Die Anwendung solch identischer Zufallszahlen (\textit{Common Random Numbers}) über alle Szenarien hinweg stellt die notwendige methodische Kontrolle sicher: Sie eliminiert das stochastische Schwankungsrauschen zwischen den Simulationsläufen. Erst dadurch wird gewährleistet, dass jede beobachtete Varianzinflation (\ac{VIF}) und jede messbare Abweichung in der Schätzgüte unmittelbar und unverfälscht auf den induzierten Orthogonalitätsverlust der jeweiligen Design-Matrix zurückzuführen ist.

\begin{table}[htbp]
    \centering
    \caption{Kalibrierte Startwerte und statistische Eigenschaften des additiven Normalverteilungs-Basismodells}
    \label{tab:simulation_parameters_normal}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{@{}lccccc@{}}
        \toprule
        \textbf{Koeffizient} & $\sym{beta}_{0}$ & $\sym{beta}_{1}, \sym{beta}_{2}$ & $\sym{beta}_{11}, \sym{beta}_{22}$ & $\sym{beta}_{12}$ & $\sym{sigma}$ \\
        \midrule
        Wahrer Wert          & $24{,}0$         & $12{,}0$                         & $-6{,}0$                           & $-6{,}0$          & $5{,}0$       \\
        \sym{snr}            & --               & $2{,}40$                         & $1{,}20$                           & $1{,}20$          & --            \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Quantitative Auswertung und explorative Optimalitäts-Analyse}

Die Auswertung der definierten Praxis-Szenarien (\textbf{Fälle I-IV}) am kalibrierten, kritischen Arbeitspunkt ($\sym{snr} = 1{,}20$) offenbart eine hochgradig differenzierte Reaktion der klassischen Design-Metriken (Tabelle~\ref{tab:results_ortho_metrics}) und der numerisch ermittelten Schätzgüte.
Zur detaillierten Evaluation wird diese Schätzgüte nachfolgend auf Basis der kombinierten Schätzvarianz (\textit{\ac{VIF}} in Tabelle~\ref{tab:results_ortho_metrics} bzw. \textit{\ac{MSE}} in Tabelle~\ref{tab:results_ortho_mse} und Abbildung~\ref{fig:ma_abb5.1.4_msedev}), dem systematischen Schätzfehler (\textit{Bias} in Tabelle~\ref{tab:results_ortho_bias}) sowie der resultierenden Trennschärfe-Variation ($\Delta \sym{power}$ in Tabelle~\ref{tab:results_ortho_power}) aufgeschlüsselt.

Ein erster Blick auf die topologischen und informationsbasierten Metriken aus Tabelle~\ref{tab:results_ortho_metrics} offenbart physikalisch wie geometrisch plausible Muster:
\begin{itemize}
    \item \textbf{Volumeneffekte und Prädiktionsgüte:} Kriterien wie die \textit{I-} und \textit{D-Effizienz} skalieren direkt mit der räumlichen Ausdehnung des Versuchsraums.
          So erzielt der nach außen gestreckte Shift (\textit{Fall I}, $+20\,\%$) Bestwerte in der integrierten Prädiktionsvarianz mit $\sym{eff_I}=85{,}1\,\%$ (Tabelle~\ref{tab:results_ortho_metrics}).
          Demgegenüber provozieren ungeplante Ausfälle periphere Informationsverluste.
          Bemerkenswert ist hierbei jedoch die topologische Sensitivität: Während der asymmetrische Entfall eines extremen Sternpunktes (\textit{Fall IV: \ac{SP}}) einen signifikanten Kollaps der \textit{I}-Effizienz auf $\sym{eff_I}=69{,}0\,\%$ provoziert, dämpft der Ausfall eines redundanten Zentralpunktes (\textit{Fall IV: \ac{ZP}}) die Vorhersagegüte keineswegs.
          Vielmehr steigt die \ac{SPV}-normierte $\sym{eff_I}$ aufgrund des reduzierten Stichprobenumfangs $\sym{N}$ rechnerisch sogar leicht auf $85{,}7\,\%$ an.
          Dies belegt eindrücklich die statistische Ineffizienz reiner Basis-\acp{ZP} für den Bereich der fernen Extrapolation.

    \item \textbf{Topologische Warnsignale und blinde Flecken:} Eine starke \textit{co-faktorielle Korrelation} (\textit{Fall II, stark}) erzeugt einen statistischen blinden Fleck.
          Obwohl das globale Design-Volumen bei dieser Modifikation weitestgehend erhalten bleibt, schießt der $\sym{A2}$-Indikator als Maß für die Nicht-Orthogonalität auf seinen Spitzenwert von $0{,}352$.
          Die durch die parasitäre Abhängigkeit entstehenden Lücken im Parameterraum bestraft die an \textit{Worst-Case}-Szenarien orientierte \textit{G-Effizienz} mit einem drastischen Einbruch, während sich der reale Informationsverlust term-spezifisch in die betroffenen Koeffizienten verlagert.

    \item \textbf{Geometrische Selektivität der Varianzinflation:} Der \ac{VIF} bildet den Orthogonalitätsverlust nicht global, sondern hochgradig \textit{term-spezifisch} ab.
          Beim asymmetrischen Sternpunkt-Ausfall (\textit{Fall IV: \ac{SP}}) greift die stringente Mathematik der isolierten Achsen: Da der zensierte \ac{SP} exakt auf der $\sym{x}_1$-Achse liegt, leiden ausschließlich der zugehörige Haupteffekt $\hat{\sym{beta}}_1$ sowie der quadratische Term $\hat{\sym{beta}}_{11}$ unter spürbarer Varianzinflation.
          Die orthogonal dazu stehende $\sym{x}_2$-Achse bleibt von diesem topologischen Bruch vollkommen isoliert und unbeschadet.
\end{itemize}

\input{tables/tab_results_ortho_metrics.tex}

Der \ac{VIF} schlägt die direkte Brücke zur absoluten Schätzvarianz, welche in Abbildung~\ref{fig:ma_abb5.1.4_msedev} über den \ac{MSE} quantifiziert wird.
Die Visualisierung zeigt das absolute Ausmaß der resultierenden Prognoseunsicherheit.
Dabei fällt zunächst auf, dass der Wechselwirkungsterm $\hat{\sym{beta}}_{12}$ bereits im ungestörten Basisdesign (\ac{CCD}) eine strukturell höhere Schätzvarianz aufweist ($\sym{MSE} \approx 6{,}17$) als die rein quadratischen Terme ($\sym{MSE} \approx 3{,}13$).
Dies ist eine unvermeidbare topologische Eigenheit des \ac{CCD}: Da die \acp{SP} strikt auf den Koordinatenachsen liegen (wobei der jeweils andere Faktor exakt Null ist), tragen sie systembedingt keinerlei Information zur Schätzung der Interaktion bei.
Diese stützt sich stattdessen ausschließlich auf den faktoriellen Kern des Designs.

Die tatsächlichen, störungsinduzierten Ausreißer manifestieren sich jedoch exakt an den zuvor durch den \ac{VIF} prognostizierten Schwachstellen.
Der direkte Vergleich der Ausfallszenarien zeichnet ein für die Planungspraxis essenzielles Bild:
Während die Zensierung des Zentralpunktes (\textit{Fall IV: \ac{ZP}}) mangels struktureller Varianzerhöhung quasi unbemerkt verpufft (der \ac{MSE} von $\hat{\sym{beta}}_{11}$ steigt lediglich marginal von $3{,}13$ auf $3{,}24$), treibt der asymmetrische Freiheitsgradverlust in \textit{Fall IV: \ac{SP}} den \ac{MSE} massiv in die Höhe.
Für den betroffenen Haupteffekt $\hat{\sym{beta}}_1$ ($3{,}10 \to 5{,}29$) und die Krümmung $\hat{\sym{beta}}_{11}$ ($3{,}13 \to 5{,}46$) entspricht dies einem drastischen Varianzanstieg von rund $70\,\%$ bis $75\,\%$ gegenüber dem fehlerfreien Referenz-Design.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_msedev.tex}
    \caption{Güte der Schätzer (\ac{MSE}) am Arbeitspunkt $\sym{snr}=1{,}20$. Geometrische Verzerrungen entladen sich primär in der Schätzvarianz, insbesondere beim Wegfall von Rand-Freiheitsgraden durch asymmetrische Ausfälle (\textbf{\textit{Fall IV: \ac{SP}}}).}
    \label{fig:ma_abb5.1.4_msedev}
\end{figure}

Andererseits belegt die Auswertung die fundamentale Erwartungstreue des angewandten \ac{OLS}-Schätzers: Trotz teils gravierender struktureller Abweichungen im Versuchsraum verharrt der relative systematische Schätzfehler (Bias) über alle Störszenarien und Koeffizienten hinweg im absoluten Promillebereich (vgl. Tabelle~\ref{tab:results_ortho_bias}).
Eine geometrische Verzerrung im \ac{CCD} führt demnach nicht zu einer inhärent falschen, verschobenen Effekterkennung.
Die Schätzungen der wahren physikalischen Koeffizienten bleiben im Mittel absolut korrekt und stabil (vgl. Abbildung~\ref{fig:ma_abb5.1.4_betadev}), wenngleich ihre Vertrauensbereiche anwachsen.

\input{tables/tab_results_ortho_bias.tex}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_betadev.tex}
    \caption{Absolute Schätzwerte der Modellkoeffizienten für den Arbeitspunkt $\sym{snr}=1{,}20$. Der Bias bleibt marginal; die \ac{OLS}-Schätzung ist fundamental erwartungstreu.}
    \label{fig:ma_abb5.1.4_betadev}
\end{figure}

Die hinreichende Dimensionierung von $\symsub{n}{idx_MC} = 1\times10^4$ Simulationsläufen zur Eliminierung des stochastischen Approximationsrauschens und zur Gewährleistung einer strikt reproduzierbaren Konvergenz wird dabei exemplarisch für den im Störfall orthogonal isolierten Koeffizienten $\hat{\sym{beta}}_2$ in Abbildung~\ref{fig:ma_abb5.1.4_betakonv} visuell belegt. Entsprechende Konvergenzplots der infizierten, stark streuenden Koeffizienten finden sich vergleichend im Anhang~\ref{app:c}.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_betakonv.tex}
    \caption{Numerische Konvergenz am Beispiel des Schätzwertes zu $\hat{\sym{beta}}_2$ über die \ac{MCS}-Iterationen.}
    \label{fig:ma_abb5.1.4_betakonv}
\end{figure}

Der resultierende \textbf{Einfluss auf die Trennschärfe} - in Tabelle~\ref{tab:results_ortho_power} quantifiziert und in Abbildung~\ref{fig:ma_abb5.1.4_powerdev} detailliert veranschaulicht - summiert die Effekte aus marginalem Bias und gestiegener Schätzvarianz (\ac{MSE}) probabilistisch auf.
Es resultiert folgendes praxisrelevantes Bild:

\input{tables/tab_results_ortho_power.tex}

\begin{enumerate}
    \item \textbf{Gutmütige Restriktionen (Scatter und \ac{ZP}):} Eine ungenaue, stochastisch verzerrte Ansteuerung der Nenn-Koordinaten (\textit{Fall III}) sowie der ungeplante Entfall eines Zentralpunktes (\textit{Fall IV: \ac{ZP}}) erzeugen kaum messbare Leistungseinbußen über alle Koeffizienten hinweg.
          Der punktuelle Trennschärfe-Verlust verbleibt hier zumeist auf dem Niveau des numerischen Rauschens ($\le 1{,}3$ Prozentpunkte).
    \item \textbf{Kritische Restriktionen für Terme höherer Ordnung (\ac{FC} und asymmetrischer Ausfall):} Sobald die periphere Geometrie des Versuchsraums kompromittiert wird, leiden die quadratischen Terme signifikant.
          Selbst das noch symmetrische Zurückziehen aller \acp{SP} auf das physikalische Randniveau der Faktoren (\textit{\ac{FC}}, \textit{Fall I}) generiert bereits einen Detektionsverlust von $6{,}7$ Prozentpunkten der absoluten $\sym{power}$ bei den quadratischen Termen, wenngleich es die Haupt- und Interaktionseffekte nahezu unbeeindruckt belässt.
          Der vollständige, asymmetrische Entfall eines Sternpunktes (\textit{Fall IV: \ac{SP}}) führt unweigerlich zu den gravierendsten Schäden in den isolierten Störszenarien.
          Die Varianzerhöhung an den Rändern des Parameterraums resultiert im stärksten Einbruch, welcher spezifisch zur Dimension $\hat{\sym{beta}}_{11}$ einen Verlust von $10{,}6$ Trennschärfe-Punkten sowie $\hat{\sym{beta}}_1$ einen Abzug von $4{,}4$ Prozentpunkten kostet.
          Dies korrespondiert konsistent mit der bereits in Tabelle~\ref{tab:impact_cd} nachgewiesenen Sensitivität: Eine systematische Ausdünnung peripherer Stützstellen provoziert primär bei den Krümmungseffekten (bis zu $13{,}9$ Prozentpunkte Verlust) einen fundamentalen Zusammenbruch der Detektionswahrscheinlichkeit.
\end{enumerate}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_powerdev.tex}
    \caption{Trennschärfe mit $95\,\%$-Konfidenzintervallen in Abhängigkeit der simulierten Störszenarien ($\sym{snr}=1{,}20$).}
    \label{fig:ma_abb5.1.4_powerdev}
\end{figure}

Zur Verifikation der methodischen Transparenz ist das korrespondierende numerische Konvergenzverhalten der empirischen Trennschärfe über die Simulationsläufe - wiederum exemplarisch für $\sym{beta}_2$ - in Abbildung~\ref{fig:ma_abb5.1.4_powerkonv} dokumentiert.
Ergänzend sind die resultierenden Wahrscheinlichkeitsdichten der Signifikanztests (\sym{p-Wert}) für alle Fälle in Abbildung~\ref{fig:ma_abb5.1.4_phisto} grafisch aufbereitet.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_powerkonv.tex}
    \caption{Numerische Konvergenz der Trennschärfe über die Simulationsläufe am Beispiel von $\sym{beta}_2$.}
    \label{fig:ma_abb5.1.4_powerkonv}
\end{figure}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_phisto.tex}
    \caption{$\sym{p-Wert}$ Histogramm für den Signifikanztest am Beispiel von $\sym{beta}_2$. Die kumulierte Häufigkeit links der $\sym{alpha}=0{,}05$-Schranke entspricht der berechneten $\sym{power}$.}
    \label{fig:ma_abb5.1.4_phisto}
\end{figure}

Zur abschließenden Synthese wird die Korrelation zwischen den \textit{a priori} Design-Metriken und dem dynamisch simulierten Trennschärfe-Verlust ($\Delta \sym{power}$) analysiert.
Explizit herangezogen werden hierfür der topologische Indikator $\sym{A2}$, die prädiktive $\sym{eff_I}$ sowie die koeffizienten-spezifische Schätzvarianz $\ac{MSE}$.
Metriken wie der Bias scheiden aufgrund ihrer nachgewiesenen Unempfindlichkeit als Prädiktoren aus.
Die \textit{D}-Optimalität bleibt ebenfalls unberücksichtigt, da sie als reines Volumenmaß die entscheidende interne Strukturverschiebung (Varianz-Hebelwirkung) unzureichend abbildet.

Abbildung~\ref{fig:ma_abb5.1.4_korrelation} fasst diese korrelativen Zusammenhänge zusammen.
Während der rein algebraische $\sym{A2}$-Indikator aufgrund der oftmals nichtlinearen Klippeneffekte von Trennschärfe-Verlusten nur ein extrem schwaches Bestimmtheitsmaß als Prädiktor liefert, erweist sich die \textbf{I-Effizienz ($\sym{eff_I}$)} als hochgradig verlässliches und robustes globales Maß.
Da sie die \ac{SPV} über den gesamten Versuchsraum integriert, bewertet sie direkt die durchschnittliche Auswirkung der Varianzinflation auf die Prognosegüte.
Der \textbf{\ac{MSE}} wiederum fungiert infolge der zugrundeliegenden Prüfmathematik (in welcher die Teststatistik invers proportional zu $1/\sqrt{\ac{MSE}}$ skaliert) als nahezu perfekter und deterministischer Prädiktor für den Trennschärfe-Erhalt isolierter Koeffizienten.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{4cm}
    \input{plots/ma_abb5.1.4_korrelation.tex}
    \caption{Korrelation zwischen a priori Design-Metriken und dem resultierenden Trennschärfe-Verlust in den Störszenarien: Der \ac{MSE} übersetzt den geometrischen Fehler am direktesten in die probabilistische Trennschärfe.}
    \label{fig:ma_abb5.1.4_korrelation}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Fazit: Statistische Effektivität als Basis der explorativen Versuchsplanung} \label{subsec:fazit_sensitivitaet}

Die simulativen Analysen unterstreichen eine ausgeprägte, jedoch hochgradig term-spezifische Resilienz des \ac{CCD} gegenüber geometrischen und anlagenbedingten Abweichungen.
Während massive topologische Brüche - insbesondere der asymmetrische Wegfall von Extremstellen (\textit{Fall IV: \ac{SP}}) - eine eindeutig messbare Aufweitung der Schätzvarianz (\ac{MSE}) provozieren, offenbart sich eine für die Versuchsplanung elementare Dichotomie:
Die Detektion linearer Haupteffekte erweist sich gegenüber geometrischen Störungen als erstaunlich robust, wohingegen die für die Modellkrümmung essenziellen quadratischen Terme hochsensibel auf periphere Informationsverluste reagieren.
Die Trennschärfe-Einbußen bei den Haupteffekten verbleiben über fast alle isolierten Störszenarien hinweg auf einem absolut tolerierbaren Niveau (zumeist deutlich unter $5$ Prozentpunkten), während die quadratischen Effekte signifikante Einbrüche (bis zu $13{,}9$ Prozentpunkte bei systematischer Ausdünnung, vgl. Tabelle~\ref{tab:impact_cd}) verzeichnen.

In Kombination mit der nachgewiesenen fundamentalen Erwartungstreue des \ac{OLS}-Schätzers (vernachlässigbarer \textit{Bias}) ergibt sich genau aus dieser term-spezifischen Robustheit ein entscheidender planerischer Spielraum:
Die bewusste Reduktion oder Inkaufnahme moderater Einbußen in der Orthogonalität gefährdet die grundlegende Identifikation der primären Einflüsse nicht.
Sie schafft stattdessen ein wertvolles \textbf{geometrisches Gestaltungsbudget}, da redundante Stützstellen zur operativen Optimierung von Prüfzeiten und Ressourcen freigegeben werden können.

Dass der Fokus im weiteren methodischen Verlauf dieser Arbeit konsequent von der reinen Detektionswahrscheinlichkeit (\ac{power}) der Haupteffekte hin zur räumlichen Verteilung der Schätzpräzision in der Peripherie verschoben wird, legitimiert sich durch exakt diese Erkenntnis.
Die algorithmische Güte eines \ac{EoL}-Tests ist untrennbar mit der Struktur der Prädiktionsvarianz im Versuchsraum verknüpft, welche maßgeblich durch die prädiktiven Metriken ($\sym{eff_I}$ und \ac{MSE}) abgebildet wird.
Im Kontext der \ac{ALT} gewinnt dies eine existenzielle Bedeutung:
Da Lebensdauermodelle naturgemäß auf die weite Extrapolation in den späteren Einsatzbereich (\textit{Use-Space} bzw. \textit{Field-Space}) angewiesen sind, wirkt die im Versuchsraum (\textit{Design-Space}) vorliegende Varianz der Prädiktion - insbesondere getrieben durch unsichere Krümmungseffekte - als geometrischer Verstärker für Unsicherheiten in der Ferne.

Für die holistische Effizienzbewertung multivariater Lebensdauertests bedeutet dies: Sobald durch die fundamentale topologische Robustheit des Designs die grundlegende Identifikation der Haupteffekte gesichert ist, wird die zielgerichtete Kontrolle der \ac{SPV}-Struktur zum primären Stellhebel der Versuchsökonomie.
Die Minimierung der Prognoseunsicherheit auf dem Extrapolationspfad ist kein rein akademisches Gütemaß, sondern die unabdingbare Voraussetzung für eine belastbare und wirtschaftlich vertretbare Lebensdauerprognose durch \ac{ALT}.
Im Folgenden wird das aus topologisch unkritischen Zentral- und Sternpunkten freigewordene Gestaltungsbudget daher proaktiv genutzt, um diese Stützstellen gezielt in Richtung des anvisierten Feldbereichs zu verschieben.
Diese asymmetrische, zielgerichtete Versuchsplanungsstrategie wird im weiteren Verlauf unter dem Akronym \textbf{\ac{EmALT}} eingeführt.
Positionsoptimierte Ankerpunkte investieren das gewonnene Budget exakt dort, wo es zur Stützung der kritischen Modellkrümmung und der Extrapolationsgüte gefordert wird, während die Grundstruktur des \ac{CCD} als stabiler Identifikationskern erhalten bleibt.
Ein iterativer, datengetriebener Optimierungsprozess erlaubt es, die Stützstellen sukzessive in Richtung des anvisierten Feldbereichs zu verschieben und die \ac{SPV} entlang des Extrapolationspfads gezielt zu minimieren wie in nachfolgendem Abschnitt~\ref{sec:adaptive_planung} beschrieben.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Adaptive Versuchsplanung zur Extrapolationsabsicherung} \label{sec:adaptive_planung}

In der Praxis werden \acp{ALT} naturgemäß im Bereich hoher Belastungen durchgeführt - etwa in den nordöstlichen Quadranten des Parameterraums -, um Ausfälle in vertretbarer Prüfzeit zu erzwingen.
Das eigentliche Ziel der Modellbildung bleibt jedoch die verlässliche Prognose unter moderaten Einsatzbedingungen im Feldbereich, der meist im südwestlichen Quadranten angesiedelt ist.
Anstatt das in Abschnitt~\ref{sec:abweichungen} erarbeitete geometrische Budget rein zur passiven Kostensenkung zu nutzen, wird dieses Gestaltungsbudget proaktiv verwertet:
Die durch die nachgewiesene Robustheit zur Disposition stehenden Stützstellen - etwa substituierbare \acp{ZP} oder für den Schädigungsmechanismus irrelevante Sternpunkte (\acp{SP}) - werden gezielt in Richtung des anvisierten Feldbereichs verlagert.
Dieser strategische \textit{Brückenbau im Parameterraum} erzeugt asymmetrische \acp{AP}, die das Regressionsmodell auf dem Extrapolationspfad empirisch absichern, während der Gesamtversuchsumfang $\sym{N}$ des \ac{CCD} unverändert bleibt \cite{Arndt.2024,Arndt.2025b,Arndt.2025}.
Das gewählte \ac{RSD} bleibt dabei als stabiler Kern erhalten und gewährleistet weiterhin die notwendige Identifikationsgüte für die Haupteffekte und Interaktionen.
Zudem erfolgt nach Vorbild der sequenziellen Versuchsplanung aus Abschnitt~\ref{subsec:strategie} eine Erweiterung der Augmentierung sowie eine iterative, datengetriebene Verfeinerung der Stützstellenpositionen, um die \ac{SPV} im kritischen Extrapolationspfad sukzessive zu optimieren \cite{Arndt.2025}.
Um den Leistungsgewinn dieser gerichteten Design-Manipulationen objektiv bewerten zu können, wird die \ac{SPV} aus ihrer globalen Integralbetrachtung gelöst.
Es bedarf ihrer Anwendung als lokale Maßzahl, die die Vorhersageunsicherheit gemäß Gleichung~\ref{eq:pred_variance_scaled} punktgenau im kritischen Betriebspunkt quantifiziert.

\subsection{Die Scaled Prediction Variance als Prognosemetrik} \label{subsec:spv_metrik}
Zur Quantifizierung der ortsabhängigen Prognosegüte dienen sowohl die \ac{SPV} für \textit{effizienzbasierte} Varianzbewertungen als auch die \ac{UPV} für die Bestimmung der \textit{absoluten} Vorhersageunsicherheit.
Da bei einer reinen geometrischen Umverteilung der Stützstellen der Stichprobenumfang $\sym{N}$ konstant bleibt, verhalten sich \ac{SPV} und \ac{UPV} streng proportional zueinander.
Eine topologische Optimierung der Effizienzmetrik (\ac{SPV}) führt in diesem Szenario zwangsläufig zu einer formgleichen Verbesserung der absoluten physikalischen Vorhersagepräzision (\ac{UPV}).

Um die Auswirkungen von Design-Modifikationen speziell auf die Extrapolationsfähigkeit zu bewerten, erweist sich die Untersuchung mittels \ac{VDG} und \ac{FDS} jedoch als nur bedingt geeignet.
Dies lässt sich anhand der Zentralpunkt-Reduktion (\textit{Fall IV: \ac{ZP}}) belegen:
werden die \acp{ZP} von $\symsub{n}{idx_C} = 5$ auf beispielsweise $\symsub{n}{idx_C} = 3$ reduziert, ist im Zentrum $(0 ; 0)$ lediglich ein marginaler Varianzanstieg zu verzeichnen.
Gleichzeitig wirkt sich diese Modifikation nicht negativ auf die Peripherie aus.
Wie Abbildung~\ref{fig:ma_abb5.2.1_fdsvdg_nc} zeigt, bleibt die \ac{SPV} im Vergleich zum Basis-\ac{CCD} im Extrapolationsraum nahezu unverändert oder sinkt aufgrund der Normierung mit dem geringeren $\sym{N}$ sogar ab.
In der äußeren Sphäre ($\sym{r}=\sqrt{2}$) reduziert sich die \ac{SPV} dadurch um rund $15\,\%$ von $8{,}125$ auf $6{,}875$. Entgegengesetzt dazu führt eine Erhöhung der \acp{ZP} ($\symsub{n}{idx_C} > 5$) zu einem unvorteilhaften Anstieg der skalierten Varianz in der Peripherie.
\begin{figure}[htbp]
    \centering
    \setlength\figureheight{5cm}
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \setlength\figurewidth{0.8\linewidth}
        \input{plots/ma_abb5.2.1_fds_nc.tex}
        \caption{Fraction of Design Space (\ac{FDS})}
        \label{fig:ma_abb5.2.1_fds_nc}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \setlength\figurewidth{0.8\linewidth}
        \input{plots/ma_abb5.2.1_vdg_nc.tex}
        \caption{Variance Dispersion Graph (\ac{VDG})}
        \label{fig:ma_abb5.2.1_vdg_nc}
    \end{subfigure}
    \caption{Vorhersagegüte bei Variation der \ac{ZP}-Anzahl $\sym{n}_{\sym{idx_C}}$ für einen \ac{CCD} mit $\sym{k}=2$ Faktoren.}
    \label{fig:ma_abb5.2.1_fdsvdg_nc}
\end{figure}
Die Ineffizienz redundanter Zentralpunkte in dieser Hinsicht lässt sich am Grenzwert der \ac{SPV} im Zentrum analytisch beweisen.
Da die Schätzvarianz im Ursprung durch den Mittelwert der \acp{ZP} dominiert wird ($\sym{pred_var}\approx \sym{sigma}^2 / \symsub{n}{idx_C}$), ergibt sich für ein zweidimensionales \ac{CCD} mit einer Kernstruktur aus $\symsub{n}{idx_F} + \symsub{n}{idx_S} = 8$ Basis-Versuchspunkten:
\begin{equation} \label{eq:spv_limit_center}
    \lim_{\symsub{n}{idx_C} \to \infty} \sym{pred_var}= \lim_{\symsub{n}{idx_C} \to \infty} \frac{(\symsub{n}{idx_F} + \symsub{n}{idx_S} + \symsub{n}{idx_C}) \cdot \frac{\sym{sigma}^2}{\symsub{n}{idx_C}}}{\sym{sigma}^2} = \lim_{\symsub{n}{idx_C} \to \infty} \left( \frac{8}{\symsub{n}{idx_C}} + 1 \right) = 1
\end{equation}
Dieses Konvergenzverhalten verdeutlicht den abnehmenden Grenznutzen:
Die \ac{SPV} stagniert am absoluten Minimum von $1$.
Eine Erhöhung über $\symsub{n}{idx_C} \approx 3$ hinaus leistet somit keinen Beitrag für die ferne Extrapolation und legitimiert die Freigabe dieser Ressourcen für das Gestaltungsbudget.

Demgegenüber nehmen die Sternpunkte (\textit{Fall IV: \ac{SP}}) eine kritischere Rolle ein, da sie die Varianztopologie in der Peripherie dominieren.
Wird ein Sternpunkt zensiert oder ausgelassen (hier exemplarisch im Hochlastbereich), wächst die \ac{SPV} in dieser Region massiv an.
Infolgedessen wird ein wesentlicher Teil des Versuchsraums durch unverhältnismäßig große Varianzwerte dominiert (vgl. \ac{FDS} in Abbildung~\ref{fig:ma_abb5.2.1_fds_omitted}).
\begin{figure}[htbp]
    \centering
    \setlength\figureheight{5cm}
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \setlength\figurewidth{0.8\linewidth}
        \input{plots/ma_abb5.2.1_fds_omitted.tex}
        \caption{Fraction of Design Space (\ac{FDS})}
        \label{fig:ma_abb5.2.1_fds_omitted}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \setlength\figurewidth{0.8\linewidth}
        \input{plots/ma_abb5.2.1_vdg_omitted.tex}
        \caption{Variance Dispersion Graph (\ac{VDG})}
        \label{fig:ma_abb5.2.1_vdg_omitted}
    \end{subfigure}
    \caption{Vorhersagegüte bei Variation der \ac{SP}-Anzahl $\sym{n}_{\sym{idx_S}}$ für einen \ac{CCD} mit $\sym{k}=2$ Faktoren. \ac{SPV}-Werte für verschiedene Varianten entfallener Sternpunkte. Abbildung \ref{fig:ma_abb5.2.1_vdg_omitted} zeigt den Fall des vollständigen Entfalls der Sternpunkte $(\symsub{alpha}{idx_D};0)$ und $(0;\symsub{alpha}{idx_D})$ im Hochlastbereich.}
    \label{fig:ma_abb5.2.1_fdsvdg_omitted}
\end{figure}
Gleichzeitig führt diese asymmetrische Störung zu einer komplexen, nicht mehr radialsymmetrischen Varianzstruktur, wodurch die \ac{SPV} über den \ac{VDG} ihre Interpretierbarkeit verliert.
Abbildung~\ref{fig:ma_abb5.2.1_vdg_omitted} verdeutlicht diesen Informationsverlust:
Zwar ist eine starke Aufweitung des radialen Streubereichs erkennbar, die exakte geometrische Ausrichtung dieser Varianzspitzen bleibt jedoch verborgen.
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/ma_abb5.2.1_contour_spv.pdf}
    \caption{Dreidimensionale Darstellung der \ac{SPV} und zugehöriger Konturverlauf für ein beispielhaft asymmetrisch manipuliertes Versuchsdesign: es entfallen $(0;\symsub{alpha}{idx_D})$ \textit{und} $(\symsub{alpha}{idx_D};0)$ eines \ac{CCD} mit $\sym{k}=2$ Faktoren.}
    \label{fig:ma_abb5.2.1_contour_spv}
\end{figure}
Die Ursache dieser Anisotropie offenbart erst der dreidimensionale Konturverlauf in Abbildung~\ref{fig:ma_abb5.2.1_contour_spv}.
Aus ingenieurwissenschaftlicher Perspektive ist diese Richtungsabhängigkeit hochgradig kritisch, da sie die Vorhersagegüte entlang des Extrapolationspfads massiv beeinträchtigt.
Für die Versuchsplanung folgen daraus drei essenzielle Erkenntnisse:
\begin{enumerate}
    \item Lokale Fehlstellen in der Versuchsmatrix prägen sich unmittelbar als stark divergierende Varianzspitzen (\textit{Peaks}) in exakt jener Himmelsrichtung der fehlenden Stützstelle aus.
    \item Die Prognosegüte in fernen Feldbereichen verhält sich hochgradig richtungssensitiv; pauschale Aussagen über die radiale Extrapolationsfähigkeit sind bei asymmetrischen Modifikationen durch \ac{VDG} und \ac{FDS} unzulässig.
    \item Rotationssymmetrisch aggregierende Visualisierungen wie der \ac{VDG} maskieren die erfolgskritische Richtungsabhängigkeit und deuten lediglich auf das Vorhandensein extremer Streuungen hin.
\end{enumerate}
Für die effiziente Auslegung multivariater \ac{ALT}-Szenarien resultiert hieraus die zwingende Notwendigkeit eines neuen Bewertungswerkzeugs, welches die Richtungsabhängigkeit explizit erfasst, um eine Interpretationsfähigkeit der Prognosesicherheit im realen Betriebspunkt zu garantieren.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Richtungsabhängige Bewertung: Der Extrapolation Variance Graph (EVG)} \label{subsec:evg_plot}
Wie aus den vorherigen Überlegungen hervorgeht, verliert die \ac{SPV}-Topologie durch asymmetrische Modifikationen (wie die Verschiebung oder Streichung einzelner Versuchspunkte aus \textit{Fall I}, \textit{\ac{SP}} und \textit{\ac{ZP}}) unweigerlich ihre konzentrische Rotationssymmetrie.
Die Prädiktionsvarianz (\ac{SPV} sowie \ac{UPV}) divergiert folglich richtungsabhängig.
Für die gezielte Extrapolation einer Lebensdauer- bzw. Zuverlässigkeitsprognose auf einen fernen Betriebspunkt im Feldbereich (\textit{Field-Space}) ist daher nicht die durchschnittliche Varianz über alle Winkel entscheidend, sondern exakt die Entwicklung entlang des Vektors, der das Design-Zentrum mit diesem Zielpunkt verbindet.
Da klassische Visualisierungswerkzeuge wie der \ac{VDG} (vgl. Abbildung~\ref{fig:ma_abb5.2.1_vdg_omitted}) lediglich sphärische Mittel- und Extremwerte berechnen, bleiben sie für diese tatsächliche geometrische Ausrichtung der Varianz blind.

Um diesen \textit{Blind Spot} in der Interpretationsfähigkeit der Prädiktionsvarianz und somit der Effektivität des Versuchsplans aufzulösen und asymmetrische Anpassungen gezielt quantifizieren zu können, entwickelten \textcite{Arndt.2025} den \textbf{\ac{EVG}}.
Dieser transformiert die Fragestellung: Anstatt die \ac{SPV} konzentrisch zu mitteln, wird sie ausschließlich entlang eines spezifischen, normierten Richtungsvektors $\sym{e_vec}$ in Abhängigkeit des radialen Abstands $\sym{r}$ analysiert.
Mathematisch wird diese richtungsabhängige Prädiktionsvarianz - im Folgenden als $\nu(\sym{r}, \sym{e_vec})$ bezeichnet - wie folgt definiert:
\begin{equation} \label{eq:evg_definition}
    \nu(\sym{r}, \sym{e_vec}) = \sym{N} \cdot \left[ \symsub{x_loc}{idx_0}(\sym{r}, \sym{e_vec}) \right]' \sym{M}^{-1} \left[ \symsub{x_loc}{idx_0}(\sym{r}, \sym{e_vec}) \right]
\end{equation}
Die Auswertungsfunktion $\symsub{x_loc}{idx_0}(\sym{r}, \sym{e_vec})$ transformiert den abgetasteten Koordinatenpunkt $\sym{r} \cdot \sym{e_vec}$ in den korrespondierenden Modellvektor, welcher exakt auf die Polynomstruktur der Informationsmatrix $\sym{M}$ expandiert wird.
Dadurch wird die Varianz gezielt entlang der kritischen Extrapolationsachse $\sym{e_vec}$ hin zum Prognosepunkt auswertbar.
Gleichzeitig bleibt die Methodik flexibel anwendbar für ingenieurstechnisch relevante Stellen im Parameterraum, etwa zur Untersuchung spezifischer Randbereiche des Betriebsraums.
Dieser mathematische Zusammenhang befähigt zu einer weitreichenden Optimierungsstrategie:
Um die Varianzdivergenz auf der kritischen Extrapolationsachse abzustützen, wird das geometrische Gestaltungsbudget gezielt umgewidmet. Dieses setzt sich aus obsoleten Stützstellen zusammen - beispielsweise einem substituierbaren \ac{ZP} ($\sym{n}_{\sym{idx_C}}=1$) sowie entfallenen Sternpunkten aus dem Hochlastbereich, wie $(0 ; \symsub{alpha}{idx_D})$ und $(\symsub{alpha}{idx_D} ; 0)$.
Diese Versuchspunkte fungieren fortan als Extrapolations-\acp{AP}.
Soll die skalierte Vorhersagevarianz $\nu(\sym{r}_{\textnormal{fix}}, \sym{e_vec})$ in einem spezifischen Prognosepunkt minimiert werden, lässt sich die Ermittlung der optimalen \ac{AP}-Allokation algorithmisch als diskretes Suchproblem formulieren.
Für eine gegebene Anzahl freier Stützstellen $\sym{n}_{\sym{idx_AP}}$ (im vorliegenden Visualisierungsbeispiel $\sym{n}_{\sym{idx_AP}} = 3$) wird die Designmatrix um die Vektoren potenzieller \acp{AP} aus der Kandidatenmenge $\sym{cand_set}$ erweitert.
Da Prüfstandsrestriktionen eine punktgenaue Ansteuerung des theoretischen Optimums in der Praxis oft verhindern, identifiziert der Algorithmus die gesamte Menge aller \ac{AP}-Kombinationen, deren resultierende Varianz einen tolerierbaren Zielbereich nicht überschreitet. Dieser wird über eine relative Toleranzmarge $\sym{q}$ (beispielsweise ein Aufschlag von $5\,\%$ auf das absolute Minimum, $\sym{q}=0{,}05$) definiert:
\begin{equation} \label{eq:anchor_exposure}
    \sym{X_exposure} = \left\{ \sym{x}_{\sym{idx_AP}, \sym{idx_i}} \in \sym{cand_set} \;\middle|\; \nu(\sym{r}_{\textnormal{fix}}, \sym{e_vec}) \le \min(\nu) \cdot (1 + \sym{q}) \right\}
\end{equation}
Die Menge dieser zulässigen Koordinaten spannt die sogenannte \textbf{Anker-Exposition} (\textit{Exposure-Area}) $\sym{X_exposure}$ auf.
Jede physikalisch realisierbare Kombination der drei umgewidmeten Versuchspunkte innerhalb dieses Toleranzfeldes garantiert dem Versuchsplaner, dass das Regressionsmodell auf dem Extrapolationspfad empirisch hochgradig effizient verankert wird. Gleichzeitig bleibt ausreichend Flexibilität für die technische Umsetzung am Prüfstand gewahrt.
Abbildung~\ref{fig:ma_abb5.2.2_exposure_scatter} illustriert diese Methodik exemplarisch: Das unveränderte Basis-\ac{CCD} wird um drei strategisch investierte Extrapolations-\acp{AP} im südwestlichen Quadranten erweitert.
Die grau hinterlegte Anker-Exposition markiert dabei exakt jenen Parameterbereich, in dem eine beliebige Platzierung dieser \acp{AP} die Prädiktionsvarianz im anvisierten Prognosepunkt $(-0{,}7 ; -1{,}9)$ zuverlässig auf den definierten Schwellenwert limitiert.
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.6\textwidth]{plots/ma_abb5.2.2_exposure_scatter.pdf}
    \caption{Versuchsraum-Topologie: Das Basis-Design (\ac{CCD}) wird durch strategische Extrapolations-\acp{AP} (innerhalb der \textit{Exposure-Area}) in Richtung des fernen Feld-Betriebspunktes (Ziel-Vektor) erweitert.}
    \label{fig:ma_abb5.2.2_exposure_scatter}
\end{figure}
Werden, wie in diesem Beispiel, konkrete Ankerpunkte identifiziert und fixiert, lässt sich das resultierende asymmetrische Design zur Überprüfung in einem 3D-Konturplot visualisieren (vgl. Abbildung~\ref{fig:ma_abb5.2.2_contour}).
Beispielhaft zu dieser Stelle sowie in den nachfolgenden Abschnitten werden hier die \acp{AP} auf die Koordinaten $(-0{,}85 ; -2{,}60)$, $(-1{,}10 ; -2{,}35)$ und $(-0{,}85 ; -2{,}35)$ fixiert und als und das Design-Layout \ac{EmALT} evaluiert.
Hierbei wird deutlich, wie die gesetzten \acp{AP} die Varianztopologie im Zielquadranten eindrücken und den divergierenden Anstieg der \ac{SPV} gezielt dämpfen.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{plots/ma_abb5.2.2_contour.pdf}
    \caption{Dreidimensionale Darstellung der \ac{SPV} und zugehöriger Konturverlauf für ein asymmetrisch manipuliertes Versuchsdesign: Das modifizierte \ac{CCD} ($\sym{k}=2$) beinhaltet drei \acp{AP} zur Stützung des südwestlichen Quadranten.}
    \label{fig:ma_abb5.2.2_contour}
\end{figure}

Der quantitative Nutzen dieser gezielten Manipulation manifestiert sich abschließend in der direkten Gegenüberstellung von klassischem \ac{VDG} und dem neuen \ac{EVG} (vgl. Abbildung~\ref{fig:ma_abb5.2.2_evg}).
Wird das asymmetrisch modifizierte Design konventionell über den \ac{VDG} evaluiert, zeigt sich im Vergleich zum ungestörten Basis-\ac{CCD} lediglich eine marginale, durchschnittliche Erhöhung der Varianz.
Der \ac{VDG} maskiert den lokalen Informationsgewinn der gesetzten \acp{AP} durch seine sphärische Mittelung somit nahezu vollständig.

Der \ac{EVG} offenbart hingegen den tatsächlichen, zielgerichteten Erfolg der Modifikation:
Durch die explizite Auswertung exakt entlang des Extrapolationsvektors $\vec{e}$ wird die \ac{SPV} richtungsabhängig und korrekt bis in den fernen Prognosepunkt quantifiziert.
Hierbei wird ersichtlich, dass die strategisch platzierten \acp{AP} einen messbaren Sattelpunkt im radialen Anstieg der Varianz erzwingen.
Diese topologische Stützung drückt die Vorhersageunsicherheit auf dem Extrapolationspfad im Vergleich zum starren Standard-Design massiv nach unten.
Insbesondere für die industrielle Lebensdauerversuchsplanung erweist sich diese präzise, zielgerichtete Varianzbestimmung als essenzielles Werkzeug:
Sie liefert dem versuchsplanenden Ingenieur den belastbaren Nachweis der Extrapolationsgüte exakt in jenem Parameterbereich, in dem die Zuverlässigkeitsaussage für den Feldbetrieb gefordert ist.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.2.2_evg.tex}
    \caption{Der klassische \ac{VDG} verschleiert den lokalen Design-Nutzen des um \acp{AP} modifizierten \ac{CCD} durch radiale Mittelung. Der \ac{EVG} belegt hingegen die signifikante \ac{SPV}-Reduktion zielgerichtet entlang des Extrapolationsvektors zum Prognosepunkt.}
    \label{fig:ma_abb5.2.2_evg}
\end{figure}
Diese richtungsabhängige Reduktion der Vorhersagevarianz belegt eindrücklich, dass eine Abkehr von der reinen Symmetrie und Orthogonalität in \ac{ALT}-Szenarien hochgradig effizient ist.
Das gewonnene geometrische Budget wird nicht verschwendet, sondern wertsteigernd investiert.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Kostenstruktur der Lebensdauererprobung} \label{sec:kostenmodell}

Die vorangegangenen Abschnitte~\ref{sec:abweichungen} und \ref{sec:adaptive_planung} zeigen, dass eine adaptive Manipulation der Orthogonalität eines Versuchsplans ein wertvolles geometrisches Gestaltungsbudget freisetzen kann.
Die bisherige Evaluierung fokussierte sich dabei primär auf die \textbf{statistische Effektivität} der Versuchsdesigns - also auf deren fundamentale methodische Fähigkeit, die wahren physikalischen Zusammenhänge unverfälscht (\textit{Bias}, \ac{MSE}, Optimalitäten) und mit der geforderten Nachweissicherheit (\ac{power}) zu identifizieren.
Statistische Effektivität bildet jedoch lediglich die unabdingbare Grundvoraussetzung für die industrielle \ac{EoL}-Versuchsplanung.
Ein effektiver Versuchsplan wird in der Praxis erst dann zu einem \textbf{effizienten} Versuchsplan, wenn er die geforderten statistischen Zielvorgaben unter minimalem Ressourceneinsatz und zwingend innerhalb eines limitierten Zielbudgets erreicht.
Effizienz definiert sich folglich direkt über das Verhältnis zwischen der algorithmischen Leistungsfähigkeit (statistische \acp{KPI}) und den real verursachten experimentellen Aufwänden.

Um die ganzheitliche Leistungsfähigkeit adaptiver Versuchsplan-Modifikationen bewerten zu können, bedarf es demnach zwingend einer komplementären ökonomischen Betrachtung.
Nur wenn die methodischen Gewinne der Extrapolationsabsicherung mit den tatsächlichen Prüfkosten ins Verhältnis gesetzt werden, lässt sich die übergeordnete Forschungsfrage objektiv beantworten: \textit{Wie viel Prädiktionsgüte ist finanzierbar?}

Zu diesem Zweck wird nachfolgend die Struktur eines Modells eingeführt, welches die monetären Aufwände der experimentellen Durchführung im \ac{DoE} systematisch erfasst und bilanziert.
Die methodische Grundlage hierfür bildet das in \textcite{Arndt.2025} publizierte Kostenmodell für multivariate Lebensdauertests.
An dieser Stelle werden zunächst die zugrundeliegenden Kostenpositionen, deren funktionale Zuordnung sowie die strukturellen Interdependenzen des Modells grundlegend definiert.
Die detaillierte quantitative Auswertung dieses Modells erfolgt im Rahmen der Gesamtbewertung in Abschnitt~\ref{sec:weibull_transfer}, sobald die stochastischen Eigenschaften realer Lebensdauerdaten in die Betrachtung integriert sind.

Auch bei einer vorab fest definierten Anzahl an Versuchspunkten ($\sym{N}$) und vorgegebenen Replikationen hängen die realen Versuchskosten maßgeblich von den individuellen Laufzeiten $\sym{t}_{\sym{idx_i}}$ der Prüflinge ab.
Dies gilt insbesondere für \ac{EoL}-Un\-ter\-such\-ung\-en, bei denen Prüflinge oftmals sequenziell und bis zum Ausfallereignis unter stark variierenden Lastniveaus getestet werden.
Um diese Aufwände generisch und unabhängig vom spezifischen Prüflingstyp bilanzieren zu können, werden die Gesamtkosten $\sym{C}_{\sym{idx_cost_total}}$ in drei wesentliche Hauptdomänen unterteilt \cite{Arndt.2025}:

\begin{enumerate}
    \item \textbf{Umgebungskosten $\sym{C}_{\sym{idx_cost_ambient}}$ (\textit{Ambient Conditions}):} Diese statische Domäne bündelt die standort- und laborbezogenen Bereitstellungskosten $\sym{C}_{\sym{idx_cost_labor}}$, wie beispielsweise allgemeine Aufwände für die Laborinfrastruktur, den Flächenbedarf oder die grundlegende Raumklimatisierung.
    \item \textbf{Prüfstandskosten $\sym{C}_{\sym{idx_cost_test_eq}}$ (\textit{Test Equipment}):} Hierunter fallen die anlagenspezifischen Ausrüstungs- und Investitionskosten.
          Diese setzen sich primär aus der grundlegenden Prüfstandshardware $\sym{C}_{\sym{idx_cost_base_eq}}$ sowie der geforderten Güte der eingesetzten Messtechnik $\sym{C}_{\sym{idx_cost_instr}}$ und Steuerungstechnik $\sym{C}_{\sym{idx_cost_ctrl}}$ zusammen.
          Ein strategisches Absenken der geforderten Ansteuergenauigkeit (und die damit verbundene Inkaufnahme von Orthogonalitätsabweichungen, vgl. in Abschnitt~\ref{subsec:ergebnisse_generic} Scatter-Effekte in Fall III) würde sich hier direkt in signifikant reduzierten Hardware- und Investitionskosten niederschlagen.
    \item \textbf{Laufende Durchführungskosten $\sym{C}_{\sym{idx_cost_size}}$ (\textit{Size of Testing}):} Diese hochgradig variable Kostengruppe skaliert direkt mit der zeitlichen Dauer und dem physikalischen Umfang der Versuchsreihe.
          Sie umfasst die kontinuierlichen Personalkosten $\sym{C}_{\sym{idx_cost_op}}$, die Aufwände für Hilfs- und Betriebsstoffe $\sym{C}_{\sym{idx_cost_help}}$, den lastabhängigen Energieverbrauch $\sym{C}_{\sym{idx_cost_ener}}$ sowie die belastungs- und zeitproportionalen Instandhaltungskosten $\sym{C}_{\sym{idx_cost_maint}}$.
\end{enumerate}

Das zugrundeliegende strukturelle Wirknetz dieser Kostenpositionen ist in Abbildung~\ref{fig:cost_model_structure} visualisiert.
Es verdeutlicht die Interdependenzen der Domänen und zeigt auf, wie die einzelnen Faktoren methodisch aggregiert werden, um die designspezifischen Gesamtkosten $\sym{C}_{\sym{idx_cost_total}}$ abzuleiten.

\begin{figure}[htbp]
    \centering
    % \includegraphics[width=0.7\textwidth]{plots/ma_abb_cost_model.pdf} % Pfad bitte anpassen
    \caption{\colorbox{yellow}{Struktur} des Kostenmodells zur Berechnung der Gesamtkosten $\sym{C}_{\sym{idx_cost_total}}$ als Funktion der Versuchsplan-Implementierung, identifizierter Kostenfaktoren sowie zufallsbehafteter Versuchslaufzeiten $\sym{t}_{\sym{idx_i}}$ (in Anlehnung an \cite{Arndt.2025}).}
    \label{fig:cost_model_structure}
\end{figure}

Um die theoretischen Relationen dieser Kostendomänen in einen ingenieurspraktischen Kontext zu setzen, können für typische \ac{EoL}-Prüfszenarien im Maschinen- und Automobilbau charakteristische Annahmen getroffen werden.
Die Investitionskosten für derartige Anlagen ($\sym{C}_{\sym{idx_cost_test_eq}}$) belaufen sich branchenüblich auf mittlere bis hohe sechsstellige Beträge, welche kalkulatorisch zumeist über eine Nutzungsdauer von sechs bis acht Jahren abgeschrieben werden \cite{AfA.2000}.
Trotz dieser substanziellen Fixkosten verdeutlicht die praxisnahe Aufschlüsselung die absolute Dominanz der laufenden Durchführungskosten ($\sym{C}_{\sym{idx_cost_size}}$) bei \ac{EoL}-Untersuchungen:
Durch die inhärente Auslegung auf Dauerlauf -- oftmals im 24/7-Betrieb mit Auslastungen von weit über 4.000 Betriebsstunden pro Jahr -- degeneriert der Anteil der reinen Anlagenabschreibung pro Betriebsstunde auf einen marginalen Bruchteil der Investitionssumme.

Die tatsächliche Kostendynamik wird stattdessen von zwei operativen Hebeln determiniert:
Erstens skaliert der lastabhängige Energieverbrauch $\sym{C}_{\sym{idx_cost_ener}}$ proportional mit den Laufzeiten $\sym{t}_{\sym{idx_i}}$.
Insbesondere bei kombinierten Umwelt- und Lastkollektiven (beispielsweise bei zyklischer thermischer Konditionierung gepaart mit hohen Antriebsleistungen) übersteigen die stündlichen Energiekosten die kalkulatorische Anlagenabschreibung oftmals signifikant.
Zweitens definieren die Personalkosten $\sym{C}_{\sym{idx_cost_op}}$ eine starke Asymmetrie: Während hochgradig automatisierte Dauerläufe personell lediglich für das initiale Rüsten der Prüflinge ins Gewicht fallen, treiben kurze, interaktionsintensive Versuchsläufe den effektiven Stundensatz durch den hohen Rüst- und Betreuungsaufwand massiv in die Höhe.

Neben diesen primären Treibern vervollständigen drei weitere Positionen die Bilanzierung, deren Charakteristika für das Gesamtmodell essenziell sind:
Die Instandhaltungskosten ($\sym{C}_{\sym{idx_cost_maint}}$) setzen sich typischerweise aus einem fixen Präventivanteil für zyklische Kalibrierungen und einem stark beanspruchungsabhängigen Verschleißanteil (beispielsweise für adaptive Sensorik oder mechanische Vorrichtungen) zusammen \cite{VDI2884, Ehrlenspiel.2017}.
Hilfs- und Betriebsstoffe ($\sym{C}_{\sym{idx_cost_help}}$), wie Schmier- und Kühlmittel, verhalten sich zumeist linear zur Versuchsdauer, nehmen jedoch monetär oft eine untergeordnete Rolle ein.
Die Umgebungskosten ($\sym{C}_{\sym{idx_cost_ambient}}$) hingegen bilden den statischen Sockelbetrag: Sie werden in der Regel nach dem Verursacherprinzip über den Flächenbedarf des Prüfstandes umgelegt und verhalten sich unabhängig vom aktuellen Rüstzustand oder der Auslastung \cite{VDI3258}.

Tabelle~\ref{tab:cost_relations_capex} fasst diese ingenieurspraktischen Größenordnungen zusammen.
Sie ordnet die identifizierten Kostenpositionen indikativ den gesamten \ac{CapEx} des Prüfstandes zu.
Diese normierten Verhältnisangaben verdeutlichen das Spannungsfeld zwischen statischer Anlagenbereitstellung und dynamischer Laufzeit und dienen als Basis für die spätere Parametrierung der stochastischen Simulation.

\begin{table}[htbp]
    \centering
    \small
    \caption{Indikative Relationen der Kostenpositionen in Bezug auf die Gesamtinvestition $\sym{C}_{\sym{idx_cost_test_eq}}$ für beispielhafte, typische Komponentenprüfstände im Maschinenbau.}
    \label{tab:cost_relations_capex}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{@{}llcl@{}}
        \toprule
        \textbf{Kostendomäne} & \textbf{Kostenposition}                           & \textbf{Pos.}                                          & \begin{tabular}[b]{@{}l@{}}\textbf{Relation} \textbf{zu $\sym{C}_{\sym{idx_cost_test_eq}}$}\end{tabular} \\
        \midrule
        Prüfstandskosten      & Grundlegende Hardware (AfA)\textsuperscript{a}    & $\sym{C}_{\sym{idx_cost_base_eq}}$                     & $\approx 12{,}5\,\% - 16{,}7\,\%$ p.\,a.                                                                                  \\
                              & Mess- und Steuerungstechnik                       & $\sym{C}_{\sym{idx_cost_instr},{\sym{idx_cost_ctrl}}}$ & \textit{in $\sym{C}_{\sym{idx_cost_test_eq}}$ inkludiert}                                                                 \\
        Umgebungskosten       & Laborinfrastruktur\textsuperscript{b}             & $\sym{C}_{\sym{idx_cost_labor}}$                       & $\approx 1{,}0\,\% - 3{,}0\,\%$ p.\,a.                                                                                    \\
        Durchführung          & Instandhaltung \& Kalibrierung\textsuperscript{c} & $\sym{C}_{\sym{idx_cost_maint}}$                       & $\approx 3{,}0\,\% - 5{,}0\,\%$ p.\,a.                                                                                    \\
        \midrule
        Durchführung          & Maschinenstundensatz (rein)\textsuperscript{b}    & --                                                     & $\approx 0{,}01\,\% - 0{,}02\,\%$ p.\,Bh                                                                                  \\
        Durchführung          & Lastabhängige Energie\textsuperscript{b}          & $\sym{C}_{\sym{idx_cost_ener}}$                        & $\approx 0{,}005\,\% - 0{,}02\,\%$ p.\,Bh                                                                                 \\
        Durchführung          & Hilfs- und Betriebsstoffe                         & $\sym{C}_{\sym{idx_cost_help}}$                        & $\ll 0{,}005\,\%$                                                                                                         \\
        Durchführung          & Rüst- und Bedienpersonal                          & $\sym{C}_{\sym{idx_cost_op}}$                          & $\approx 0{,}015\,\% - 0{,}025\,\%$ p.\,Ah                                                                                \\
        \bottomrule
        \multicolumn{4}{@{}p{\linewidth}@{}}{\footnotesize \textsuperscript{a} bei 6--8 Jahren Nutzungsdauer \cite{AfA.2000}; \textsuperscript{b} in Anlehnung an VDI 3258 \cite{VDI3258}; \textsuperscript{c} gem. VDI 2884 \cite{VDI2884}.}                          \\
        \multicolumn{4}{@{}p{\linewidth}@{}}{\footnotesize \textit{Anmerkung:} Bh = Betriebsstunde (Anlage aktiv), Ah = Arbeitsstunde (Personal aktiv).}
    \end{tabular}
\end{table}

Während die fixen Bereitstellungsblöcke für Labor ($\sym{C}_{\sym{idx_cost_ambient}}$) und Basis-Prüfstand ($\sym{C}_{\sym{idx_cost_test_eq}}$) weitestgehend statisch und additiv in die Gesamtbilanz einfließen, verhalten sich die laufenden Durchführungskosten $\sym{C}_{\sym{idx_cost_size}}$ extrem dynamisch.
Da Lebensdauerversuche stochastischen Ausfallprozessen unterliegen und die asymmetrische Verteilung unterschiedlicher Belastungsstufen (Faktorkombinationen im \ac{DoE}) zu stark variierenden Testdauern führt, determinieren erst die individuellen, zufallsbehafteten Versuchslaufzeiten $\sym{t}_{\sym{idx_i}}$ den tatsächlichen Ressourcenverbrauch (Personal, Energie, Material, Anlagenverschleiß).

Im Rahmen der späteren Modellierung müssen diese dynamischen Kostentreiber folglich in einem iterativen Prozess evaluiert werden.
Die Einbettung in eine Monte-Carlo-Simulation erlaubt es, die Verteilung der stochastischen Ausfallzeiten zu aggregieren, um einen belastbaren Erwartungswert für die Durchführungskosten $\bar{\sym{C}}_{\sym{idx_cost_size}}$ zu ermitteln.
In der Summe stellt dieses Kostenmodell ein unabdingbares Instrument dar, um topologischen und statistischen Modifikationen eines Versuchsplans ein direktes ökonomisches Preisschild zuzuordnen.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Übertragung auf multivariate Lebensdauermodelle} \label{sec:weibull_transfer}

Die vorangegangenen Abschnitte dieses Kapitels bauen die methodischen Grundlagen einer adaptiven Versuchsplanung für die Lebensdauererprobung sukzessive auf:
In Abschnitt~\ref{sec:abweichungen} evaluieren Sensitivitätsanalysen generelle Einflüsse von Orthogonalitätsabweichungen auf die Variabilität von Trennschärfe (\ac{power}) und Schätzfehler (\textit{Bias}).
Diese Betrachtung gipfelt in Abschnitt~\ref{subsec:ergebnisse_generic} in der Erkenntnis, dass praxismotivierte Designmodifikationen hinsichtlich ihrer Auswirkungen auf die statistische Effektivität oftmals tolerabel bleiben.
Abschnitt~\ref{sec:adaptive_planung} greift diese Erkenntnis auf und bewertet sie aus der Perspektive einer für \acp{ALT} motivierten Optimierung der Prädiktionsvarianz.
Es zeigt sich, wie freigewordene Versuchspunkte - das identifizierte geometrische Gestaltungsbudget - proaktiv genutzt werden, um die Extrapolationsvarianz für die multidimensionale und beschleunigte Lebensdauererprobung gezielt zu minimieren.
Abschnitt~\ref{sec:kostenmodell} versieht diese topologischen Aspekte schließlich mit einem monetären Preisschild. Das eingeführte Kostenmodell dient der objektiven Quantifizierung ökonomischer Vergünstigungen, welche aus der asymmetrischen Re-Allokation der Versuchspunkte resultieren.

Der vorliegende Abschnitt bildet abschließend die Brücke zu lebensdauerverteilten Daten.
Für die industrielle \ac{EoL}-Versuchsplanung greift das bisherige, idealisierte Setup aus Sicht der spezifischen Datenlage noch zu kurz, da reale Ausfallzeiten streng positiv, oftmals rechtsschief verteilt und durch Prüfzeitverkürzungen zensiert sind \cite{Meeker.2022}.
In einem finalen Syntheseschritt erfährt die Methodik der Extrapolationsabsicherung (\textbf{EmALT}) daher den Transfer in die komplexe stochastische Realität der Zuverlässigkeitstechnik.
Das gewählte \ac{EmALT} orientiert sich wie gehabt an dem in Abschnitt~\ref{subsec:evg_plot} beispielhaft gewählten Set an \acp{AP} im südwestlichen Quadranten, um die Prädiktionsvarianz entlang des Extrapolationsvektors zum realen Feldbetriebspunkt zu minimieren.
Dazu transformiert die Methodik das zugrundeliegende Regressionsmodell in ein log-lineares \ac{AFT}-Modell auf Basis einer Weibull-verteilten Lebensdauer $\sym{t} \sim \sym{W}(\sym{T}, \sym{b})$ und bettet dieses in die etablierte Monte-Carlo-Simulationsumgebung ein.

Um die Effektivität der adaptiven Extrapolationsabsicherung abschließend zu quantifizieren, evaluiert die nachfolgende \ac{MCS} drei spezifische Design-Konfigurationen gegeneinander.
Alle Konfigurationen verfügen über den exakt gleichen Ressourcenaufwand von $\sym{N} = 13$ Versuchspunkten:
\begin{enumerate}
    \item \textbf{Basis \ac{CCD} (Referenz):} Der klassische rotationssymmetrische Versuchsplan, bestehend aus dem faktoriellen Kern ($\sym{n}_{\sym{idx_F}}=4$), vier axialen \acp{SP} ($\sym{n}_{\sym{idx_S}}=4$, mit $\symsub{alpha}{idx_D}=\sqrt{2}$) sowie fünf redundanten \acp{ZP} ($\sym{n}_{\sym{idx_C}}=5$).
    \item \textbf{Passiv ausgedünntes Design ($\textnormal{\ac{CCD}}_{-\textnormal{\ac{AP}}}$):} Die aus Abschnitt~\ref{subsec:spv_metrik} als ineffizient identifizierten Stützstellen entfallen ersatzlos.
          Dies betrifft einen redundanten Zentralpunkt sowie die beiden für die Extrapolation unkritischen Sternpunkte im Bereich der höchsten Belastung (Nordosten: $(0; \symsub{alpha}{idx_D})$ und $(\symsub{alpha}{idx_D};0)$).
          Dieses künstliche Design dient primär als methodischer Zwischenschritt, um den isolierten Trennschärfe-Verlust dieser spezifischen Fehlstellen im Lebensdauer-Raum zu messen.
    \item \textbf{\ac{EmALT} (Proaktive Extrapolationsabsicherung):} Das erarbeitete geometrische Budget aus den drei freigewordenen Stützstellen erfährt eine Reinvestition.
          Gemäß der in Abschnitt~\ref{subsec:evg_plot} beschriebenen \textit{Exposure-Area}-Methode werden drei Versuchspunkte als asymmetrische Ankerpunkte (\acp{AP}) in den südwestlichen Quadranten des Basis-Szenarios versetzt, in welchem sich üblicherweise ein realer Feldbetriebspunkt befindet, vgl. Abbildung~\ref{fig:ma_abb5.2.2_exposure_scatter}.
          Eine realitätsnahe Allokation, die auch Prüfstandsrestriktionen an den absoluten Belastungsgrenzen berücksichtigt, platziert diese Anker in einem stützenden Layout entsprechend Abschnitt~\ref{subsec:evg_plot} bei $(-0{,}85 ; -2{,}60)$, $(-1{,}10 ; -2{,}35)$ und $(-0{,}85 ; -2{,}35)$ als beispielhafte Variante zur Minimierung der Prädiktionsvarianz.
\end{enumerate}
Entsprechend der Vorgehensweise in Abschnitt~\ref{subsec:ergebnisse_generic} evaluiert die \ac{MCS} die drei Design-Varianten auf Basis von $\symsub{n}{idx_MC} = 1\times10^4$ Iterationen.

\subsection{Signal-Rausch-Kalibrierung unter dem Sparsity of Effects Principle} \label{subsec:weibull_kalibrierung}

Um die in der linearen Vorstudie (Abschnitt~\ref{sec:abweichungen}) etablierte methodische Vergleichbarkeit der Trennschärfe-Verluste aufrechtzuerhalten, erfährt das Referenz-Design für die Weibull-Simulation eine Sensibilisierung auf exakt denselben kritischen Arbeitspunkt $\sym{snr} = 1{,}20$ (vgl. Abschnitt~\ref{subsec:ergebnisse_generic}).
Da absolute Regressionskoeffizienten in $\sym{Beta}$ zwischen dem additiven Normalverteilungsraum und Lebensdauermodellen nicht translationsinvariant sind, erfordert dieser Transfer die Ausnutzung der zugrundeliegenden Verteilungsmechanik.

Beim Übergang zum \ac{AFT}-Modell ändert sich die Interpretation des Modells grundlegend:
Aufgrund der strengen Positivität und der ausgeprägten Wertebereichsdynamik realer Ausfallzeiten modelliert die Regression nicht die Lebensdauer $\sym{t}$ selbst, sondern deren natürlichen Logarithmus $\ln(\sym{t})$ als Zielgröße.
Unterliegt die physikalische Lebensdauer $\sym{t}$ einer Weibull-Verteilung, folgt ihr logarithmierter Wert $\ln(\sym{t})$ axiomatisch einer \ac{SEV} -- in der Literatur äquivalent als Gumbel-Verteilung für Minima bezeichnet \cite{Meeker.2022}.
Das \ac{AFT}-Modell formuliert sich in diesem logarithmischen Raum als lineares Regressionsmodell:
\begin{equation}
    \ln(\sym{t}) = \sym{x}'\sym{beta} + \sym{sigma}_{\textnormal{SEV}} \cdot \sym{epsilon} \quad \text{mit} \quad \sym{epsilon} \sim \textnormal{SEV}(0, 1)
\end{equation}

Die für die $\sym{power}$ entscheidende Rauschkomponente ($\sym{sigma}_{\textnormal{SEV}}$) stellt im Weibull-Fall jedoch keine freie Variable dar. Sie entspricht exakt dem Skalenparameter der \ac{SEV}, welcher über das Inverse des Weibull-Formparameters $\sym{b}$ intrinsisch an die Werkstoffphysik gekoppelt ist \cite{Meeker.2022}:
\begin{equation}
    \sym{sigma}_{\textnormal{SEV}} = \frac{1}{\sym{b}}
\end{equation}
Unter der werkstoffmechanisch plausiblen Annahme eines progressiven Verschleiß-/Ermüdungsverhaltens fixiert sich dieser Formparameter repräsentativ auf $\sym{b} = 3{,}0$ \cite{Nelson.1990}, was den Skalenparameter deterministisch auf $\sym{sigma}_{\textnormal{SEV}} = 1/3 \approx 0{,}33$ beschränkt.

Für die methodische Konsistenz bleibt zu betonen, dass $\sym{sigma}_{\textnormal{SEV}}$ den rein mathematischen Skalenparameter und nicht die tatsächliche Standardabweichung der rechtsschiefen \ac{SEV} abbildet.
Letztere definiert sich über die Eulersche Konstante und skaliert proportional über den Faktor $\pi / \sqrt{6}$ zu $\sym{sigma}_{\textnormal{total, SEV}} \approx 1{,}28 \cdot \sym{sigma}_{\textnormal{SEV}}$.
In der mathematischen Statistik der Zuverlässigkeitstechnik etabliert sich jedoch die gängige Praxis, das \ac{SNR} invariant auf den Skalenparameter zu beziehen, da dieser als direkter Skalierungsfaktor in die Fisher-Informationsmatrix eingeht und somit die Asymptotik der Likelihood-basierten Prüfmathematik unmittelbar bestimmt \cite{Meeker.2022}.

Während die Normalverteilungs-Studie das Streuungsniveau künstlich auf $\sym{sigma} = 5{,}0$ skaliert, fixiert der \ac{AFT}-Ansatz das Systemrauschen folglich intrinsisch.
Um den kritischen Arbeitspunkt von $\sym{snr} = 1{,}20$ in den Weibull-Raum zu überführen, kalibrieren sich die vorzugebenden Effekte in $\sym{Beta}$ exakt auf diese \ac{SEV}-Streuung: $\sym{beta} = \sym{snr} \cdot \sym{sigma}_{\textnormal{SEV}}$.
Für die quadratischen Modellkomponenten resultiert daraus eine exakte Ziel-Effektstärke von $\sym{beta}_{11} = \sym{beta}_{22} = \sym{beta}_{12} = \left\lvert 1{,}20 \cdot 0{,}33\right\rvert  = \left\lvert 0{,}40\right\rvert $.

Eine physikalisch repräsentative Modellierung von \acp{ALT} erfordert darüber hinaus eine funktionale Differenzierung:
Ein Anstieg des Stressniveaus induziert im Erwartungsfall eine Beschleunigung der Schädigungsmechanismen und damit eine Reduktion der Lebensdauer.
Die Modellstruktur bildet dies durch strikt \textit{negative} Vorzeichen der Effekte ab.
Die relativen Effektstärken staffeln sich hierbei hierarchisch gemäß dem empirisch belegten \textit{Sparsity of Effects Principle} \cite{Montgomery.2020}.
Daraus leitet sich die Parametrisierung für die nachfolgende \ac{MCS} ab:

\begin{itemize}
    \item \textbf{Interaktionen und quadratische Effekte:} Diese Terme höherer Ordnung determinieren die Krümmung der Antwortfläche (vgl. Abbildung \ref{fig:ma_abb5.4.3_response_surface}) und kalibrieren sich betragsmäßig auf den informationstheoretisch sensiblen Schwellenwert von $\vert{}\sym{beta}_{ij}\vert{} = 0{,}40$.
          Dies gewährleistet im orthogonalen Basis-Design eine hinreichende statistische Referenz-$\sym{power}$ ($\approx 70-80\,\%$).
          Die methodische Vorzeichenwahl ist hierbei physikalisch determiniert: Um eine valide Extrapolation in den Bereich extrem milder Feldlasten (negative Faktorstufen) zu ermöglichen, erzwingen positive quadratische Terme ($\sym{beta}_{11} = \sym{beta}_{22} = -0{,}40$) ein streng monotones Ansteigen der Systemlebensdauer im \ac{AFT}-Ansatz.
          Der synergistische Interaktionsterm verbleibt positiv ($\sym{beta}_{12} = 0{,}40$), um antagonistische Kompensations-Effekte bei kombinierter Extrembelastung auszuschließen.
    \item \textbf{Haupteffekte:} Entsprechend dem Sparsity-Prinzip fungieren sie als dominante, lebensdauermindernde Stressoren und parametrisieren sich mit einer betragsmäßig doppelt so hohen Effektamplitude: $\sym{beta}_1 = \sym{beta}_2 = -0{,}80$.
          Das korrespondierende \ac{SNR} von $2{,}40$ verleiht diesen Termen eine ausgeprägte statistische Robustheit.
          Im Kontext des multiplikativen \ac{AFT}-Ansatzes impliziert dies eine Verkürzung der zu erwartenden Lebensdauer um den konstanten Beschleunigungsfaktor $r = \exp(0{,}80) \approx 2{,}22$ pro normierter Stressstufe.
    \item \textbf{Intercept:} Die Regressionskonstante $\sym{beta}_0$ dimensioniert sich analytisch derart, dass die charakteristische Basislebensdauer $\sym{T}$ im Koordinatenursprung des Versuchsraums ein anwendungsnahes Referenzniveau von ca. $100.000$ Lastwechseln abbildet ($\ln(10^5) \approx 11{,}51$).
\end{itemize}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{plots/ma_abb5.4.3_response_surface.pdf}
    \caption{Antwortfläche der charakteristischen Systemlebensdauer $\sym{T}$ in Abhängigkeit der Faktoren $\sym{x}_{1}$ und $\sym{x}_{2}$. Die Projektion verdeutlicht die massive Extrapolationsdistanz zwischen dem \ac{DoE}-Zentrum und den geforderten Feld-Bedingungen.}
    \label{fig:ma_abb5.4.3_response_surface}
\end{figure}

\begin{table}[htbp]
    \centering
    \caption{Kalibrierte Startwerte und statistische Eigenschaften des log-linearen Weibull-Basismodells (\ac{AFT}-Ansatz)}
    \label{tab:simulation_parameters_weibull}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{@{}lccccc@{}}
        \toprule
        \textbf{Koeffizient} & $\sym{beta}_{0}$ & $\sym{beta}_{1}, \sym{beta}_{2}$ & $\sym{beta}_{11}, \sym{beta}_{22}$ & $\sym{beta}_{12}$ & $\sym{sigma}_{\textnormal{SEV}}$ \\
        \midrule
        Wahrer Wert          & $11{,}51$        & $-0{,}80$                        & $-0{,}40$                          & $0{,}40$          & $0{,}33$                         \\
        \sym{snr}            & --               & $2{,}40$                         & $1{,}20$                           & $1{,}20$          & --                               \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Generische Auswirkungen asymmetrischer Versuchsplanung} \label{subsec:spvbias}

Analog zur Methodik aus Abschnitt~\ref{subsec:ergebnisse_generic} erfolgt die Auswertung der drei Design-Konfigurationen in einer stringenten zweistufigen Sequenz: Zunächst quantifizieren \textit{a priori} Design-Metriken den topologischen Orthogonalitätsverlust, bevor die probabilistische \ac{MCS} die tatsächlichen Auswirkungen auf Schätzpräzision und Entdeckungswahrscheinlichkeit beziffert.

Die algebraische Auswertung entlang Tabelle~\ref{tab:results_emalt_metrics_ols} bestätigt das erwartete Spannungsfeld:
Der proaktive asymmetrische Eingriff bei Designmodifikationen nach \ac{EmALT} erzwingt den partiellen Verlust der Orthogonalität - messbar im Anstieg der Werte von $\sym{A2}$ und $\ac{VIF}$.
Im direkten Vergleich zum rein passiv ausgedünnten Design $\textnormal{\ac{CCD}}_{-\textnormal{\ac{AP}}}$ belegt die \textit{I-Effizienz} jedoch den strukturellen Mehrwert einer nach Abschnitt~\ref{sec:adaptive_planung} motivierten Reallokation von Versuchspunkten zur Optimierung der Prädiktionsvarianzen im Feldbereich:
Das geometrische Budget verpufft nicht ersatzlos, sondern stützt die integrierte Vorhersagevarianz signifikant ab.
\input{tables/tab_results_emalt_metrics_ols.tex}
Für die ingenieurwissenschaftliche Lebensdauerprognose manifestiert sich hier ein elementarer Paradigmenwechsel: Die vollkommen isolierte, dekorrelierte Schätzung einzelner Systemparameter - das höchste Paradigma klassischer Screening-Designs - tritt zugunsten einer zielgerichteten, makroskopischen Prädiktionsschärfe in den Hintergrund.

Die stochastische Auswertung validiert diese topologischen Indikatoren probabilistisch.
Die fundamentale Erwartungstreue des \ac{OLS}-Schätzers bleibt unter asymmetrischen Bedingungen unbeschadet gewahrt; der systematische Schätzfehler (\textit{Bias}) verharrt über alle Design-Varianten hinweg auf einem vernachlässigbaren Niveau marginal nahe Null: vgl. Tabelle~\ref{tab:results_emalt_bias_ols} sowie Abbildung~\ref{fig:ma_abb5.4.2_betadev_ols}.
Flankierend weisen die aggregierten Wahrscheinlichkeitsdichten der Signifikanztests - Abbildung~\ref{fig:ma_abb5.4.2_phisto_all_ols} - einen theoretisch konformen Verlauf auf, was numerische Artefakte infolge der Asymmetrie ausschließt.
\input{tables/tab_results_emalt_bias_ols.tex}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.4.2_betadev_ols.tex}
    \caption{Absolute Schätzwerte der Modellkoeffizienten für die asymmetrischen Designvarianten am Arbeitspunkt $\sym{snr}=1{,}20$. Der Bias bleibt marginal; die Erwartungstreue des \ac{OLS}-Schätzers ist gewahrt.}
    \label{fig:ma_abb5.4.2_betadev_ols}
\end{figure}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{5cm}
    \input{plots/ma_abb5.4.2_phisto_all_ols.tex}
    \caption{Aggregierte $\sym{p-Wert}$ Histogramme der verglichenen Designvarianten. Der konforme Verlauf belegt die Abwesenheit systematischer Verzerrungen in der Signifikanzprüfung.}
    \label{fig:ma_abb5.4.2_phisto_all_ols}
\end{figure}

Der geometrische Eingriff entlädt sich folglich primär in der Schätzvarianz - siehe \ac{MSE}, Tabelle~\ref{tab:results_emalt_mse_ols}.
Zwar weitet die topologisch induzierte Multikollinearität die Vertrauensbereiche auf, doch erweist sich das \ac{EmALT}-Design der ersatzlosen Ausdünnung als deutlich überlegen:
Die reinvestierten Extrapolations-\acp{AP} dämpfen den Varianzanstieg im direkten Vergleich spürbar für alle Regressionskoeffizienten in $\sym{Beta}$.
Die durch die Asymmetrie gezielt induzierte Kovarianzstruktur der Schätzer fungiert hierbei nicht als methodischer Makel, sondern als das exakte mathematische Vehikel, welches die \ac{SPV} auf dem Extrapolationspfad stützt.

\input{tables/tab_results_emalt_mse_ols.tex}

Dieser dämpfende Effekt schlägt sich unmittelbar in der Erhaltung der $\sym{power}$ nieder, vgl. Tabelle~\ref{tab:results_emalt_power_ols} und Abbildung~\ref{fig:ma_abb5.4.2_powerdev_ols}.
Während das passiv ausgedünnte Design ($\textnormal{\ac{CCD}}_{-\textnormal{\ac{AP}}}$) einen massiven Einbruch der Entdeckungswahrscheinlichkeit - insbesondere bei den für die Krümmung essenziellen quadratischen Termen - verzeichnet, fängt das \ac{EmALT}-Design diesen Verlust hochgradig effektiv ab.
Es übertrifft die reduzierte Designvariante substanziell und unterschreitet das Baseline $\sym{power}$-Niveau des orthogonalen Basis-\ac{CCD} lediglich marginal.

\input{tables/tab_results_emalt_power_ols.tex}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{5cm}
    \input{plots/ma_abb5.4.2_powerdev_ols.tex}
    \caption{Vergleichende Analyse der absoluten statistischen Trennschärfe. Die asymmetrische Extrapolationsabsicherung (\ac{EmALT}) stellt die Detektionsrate im Vergleich zur reinen Ausdünnung nahezu vollständig wieder her.}
    \label{fig:ma_abb5.4.2_powerdev_ols}
\end{figure}

Jegliche Variation der Modellgüte im additiven Normalverteilungsraum lässt sich somit deterministisch auf den variablen \ac{VIF} der modifizierten Design-Topologie zurückführen.
Alle im nachfolgenden Transfer auf Lebensdauermodelle auftretenden Effekte ordnen sich damit unmissverständlich der stochastischen Natur der Weibull-Verteilung und der iterativen \ac{MLE}-Approximation zu.

\subsection{Effizientes \ac{L-DoE} und der Economic Extrapolation Power Index} \label{sec:ldoe}

Schließlich vollzieht der nun vorliegende Abschnitt den finalen Übertrag in die stochastische und ökonomische Realität der Lebensdaueranalyse.
Auf Basis Weibull-verteilter Ausfallzeiten und einer log-linearen \ac{AFT}-Modellierung findet die Parameteridentifikation hier nun iterativ via \ac{MLE} statt.
Dieser Wechsel der mathematischen Domäne impliziert jedoch weit mehr als den Austausch des Schätzalgorithmus:
Er aktiviert zwingend die in Abschnitt~\ref{sec:kostenmodell} eingeführte dynamische Kostenstruktur der Lebensdauererprobung.

In klassischen, landwirtschaftlichen oder chemischen Versuchsplänen entsprechen die experimentellen Kosten zumeist linear der bloßen Anzahl an Versuchen: $\sym{C} \propto \sym{N}$.
In der Lebensdauererprobung erweist sich eine solche Entkopplung jedoch als physikalisch unzulässig, da die Zielgröße selbst - die Ausfallzeit $\sym{t}_{\sym{idx_i}}$ - als der primäre Kostentreiber fungiert \cite{VDI3258}.
Die Designmatrix $\sym{X}$ und die resultierenden Versuchskosten $\sym{C}_{\sym{idx_cost_total}}$ sind dadurch stochastisch hart gekoppelt, siehe auch Abschnitt~\ref{sec:kostenmodell}.
Dieser Umstand zwingt die Versuchsplanung in einen fundamentalen Zielkonflikt:
Das \ac{EmALT}-Design verschiebt Extrapolations-\acp{AP} in Richtung geringerer Stressniveaus (i.d.R. Südwesten des Parameterraums), um die Prädiktionsvarianz im Feld zu stützen.
Geringerer Stress erzwingt physikalisch jedoch exponentiell längere Testlaufzeiten, wodurch der Kostenerwartungswert $\sym{Er}(\sym{C}_{\sym{idx_cost_total}})$ massiv ansteigt.
Gleichzeitig minimiert das Design jedoch die Extrapolationsvarianz \ac{SPV} am beispielhaft anvisierten Prognosepunkt $(-0{,}7 ; -1{,}9)$ um ein Vielfaches gegenüber dem standardisierten Basis-\ac{CCD} und bewahrt die Detektionssicherheit sensibler Krümmungseffekte.

Um diesen inhärenten Konflikt zwischen statistischer Prädiktionssicherheit und realem Ressourcenverbrauch messbar zu machen und eine objektive Entscheidungsgrundlage zu schaffen, bedarf es einer adäquaten Effizienz-Metrik.
Da im Kontext der Lebensdauererprobung die zielgerichtete Extrapolation im Fokus steht, entspricht der relevante Informationsgehalt exakt dem Kehrwert der prädiktiven Varianz am fernen Prognosepunkt ($1 / \sym{pred_var}$).
Unter Einbezug der Notwendigkeit, die zugrundeliegenden Modellparameter zuverlässig zu identifizieren, wird dieser Ansatz generisch für einen beliebigen Regressionskoeffizienten $\hat{\sym{beta}}_i$ zum \textbf{\ac{EPI}} erweitert:
\begin{equation} \label{eq:epi_eco}
    \sym{epi} = \frac{\sym{power}(\hat{\sym{beta}}_{i})}{\sym{pred_var} \cdot \sym{Er}(\sym{C}_{\sym{idx_cost_total}})} \times 10^3
\end{equation}
Dieser Index überführt den multidimensionalen Zielkonflikt aus topologischer Geometrie, Inferenzstatistik und Ökonomie in ein skalares Optimierungskriterium:
Die Metrik skaliert proportional zur statistischen Detektionssicherheit, während sowohl die Prädiktionsvarianz im Extrapolationsraum als auch der monetäre Ressourceneinsatz als inverse Faktoren fungieren.
Um den Nachweis für die absolute Notwendigkeit dieses Trade-offs zu erbringen, erweitert das nachfolgende \ac{MCS}-Setup hier die bisherige Evaluierung um ein begleitendes, viertes Referenz-Design: das Feld-\ac{CCD}.
Diese Konfiguration translatiert die orthogonale Designmatrix unskaliert auf die Koordinaten des fernen Prognosepunktes - den Feldbetriebsbereich.
Methodisch fungiert dieses Konstrukt als analytischer Grenzwert, der das stochastische Ideal einer varianzarmen Interpolation unter Feldbedingungen mit der resultierenden, physikalisch determinierten Kostenfunktion kontrastiert.

Die probabilistische Auswertung der \ac{MCS} belegt unter diesen Prämissen zunächst die grundlegende numerische Stabilität der iterativen \ac{MLE} unter den Bedingungen stark eingeschränkter Stichprobenumfänge ($\sym{N}=13$).
Beim Übergang von der idealisierten \ac{OLS}-Regression zur \ac{MLE} rechtsschiefer \ac{SEV}-Residuen tritt theoriekonform der Effekt des Stichprobenbias kleiner Stichproben (\textit{Small-Sample Bias}) auf \cite{Meeker.2022}.
Da die asymptotische Erwartungstreue des \ac{MLE}-Schätzers mathematisch erst für $N \to \infty$ konvergiert, induziert der Stichprobenumfang von $\sym{N}=13$ eine systematische Varianz- und Parameterverschiebung, welche in Tabelle~\ref{tab:results_emalt_bias_wbl} quantifiziert wird.

\input{tables/tab_results_emalt_bias_wbl.tex}

Die detaillierte Analyse des relativen systematischen Schätzfehlers offenbart dabei zwei wesentliche stochastische Effekte:
Erstens verharren die Regressionskonstanten $\hat{\sym{beta}}_0$ über alle drei Design-Szenarien hinweg auf einem vernachlässigbaren Niveau nahezu perfekter Erwartungstreue (im \ac{EmALT} sogar exakt $-0{,}00\,\%$).
Die fundamentale Skalierung der charakteristischen Basislebensdauer im Koordinatenursprung bleibt somit durch die geometrische Umstrukturierung vollkommen unberührt.
Zweitens entlädt sich die in Abschnitt~\ref{subsec:spvbias} identifizierte Multikollinearität des \ac{EmALT}-Designs ($\ac{VIF} = 5{,}91$ für $\hat{\sym{beta}}_{22}$) im Zusammenspiel mit der \ac{MLE} in einer Umverteilung der relativen Schätzfehler.
Während die voll-orthogonalen Referenzdesigns bei den sensitiven Krümmungstermen moderat überschätzen ($+12{,}79\,\%$ bis $+23{,}17\,\%$), verzeichnen die Terme höherer Ordnung im \ac{EmALT} betragsmäßig höhere relative Abweichungen (beispielsweise $-53{,}26\,\%$ bei $\hat{\sym{beta}}_{22}$).
In der ingenieurwissenschaftlichen Bewertung ist diese Zahl jedoch zu relativieren:
Da der wahre Nominellwert des Krümmungseffekts kritisch auf $\sym{beta}_{22} = -0{,}40$ kalibriert ist, entspricht eine relative Abweichung von $-53\,\%$ einer absoluten Verschiebung von lediglich $\Delta \sym{beta}_{22} \approx -0{,}21$.
Wie die vergleichende Darstellung der absoluten Schätzwerte in Abbildung~\ref{fig:ma_abb5.4.3_betadev_wbl} verdeutlicht, verharrt die rekonstruierte Antwortfläche damit im physikalisch plausiblen Korridor und bildet die progressive Verschleißkinematik ohne Vorzeichenwechsel oder entartete Gradienten ab.
\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{5cm}
    \input{plots/ma_abb5.4.3_betadev_wbl.tex}
    \caption{Absolute Schätzwerte der \ac{MLE}-Koeffizienten im Weibull-Modell ($\sym{snr}=1{,}20$). Der Schätzwert der Basislebensdauer $\hat{\sym{beta}}_0$ bleibt exakt; die Terme höherer Ordnung spiegeln die durch die Multikollinearität induzierte Varianzumverteilung wider.}
    \label{fig:ma_abb5.4.3_betadev_wbl}
\end{figure}
Ein herausragender methodischer Vorteil des \ac{EmALT}-Designs offenbart sich schließlich bei der Identifikation des intrinsischen Weibull-Formparameters $\hat{\sym{b}}$ (wahrer Wert $\sym{b} = 3{,}03$).
Klassische, eng konzentrierte Versuchspläne überschätzen diesen Parameter bei kleinen Stichproben massiv: $\hat{\sym{b}} = 4{,}97$ im Basis-\ac{CCD} bzw. $\hat{\sym{b}} = 5{,}91$ im $\textnormal{\ac{CCD}}_{-\textnormal{\ac{AP}}}$.
Das \ac{EmALT}-Design hingegen stützt durch die weit in den Niedrigstressraum ausgreifenden Ankerpunkte den stochastischen Hebelarm der Likelihood-Funktion.
Mit $\hat{\sym{b}} = 3{,}37$ schätzt es den wahren Formparameter mit Abstand am präzisesten.
Das asymmetrische \ac{EmALT}-Design erkauft sich die Absicherung der Prädiktionsvarianz folglich nicht durch eine Entartung der Modellparameter, sondern liefert im Gegenteil die physikalisch konsistenteste Beschreibung der zugrundeliegenden Ausfalldynamik.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{5cm}
    \input{plots/ma_abb5.4.3_phisto_all_wbl.tex}
    \caption{Aggregierte $\sym{p-Wert}$ Histogramme der \ac{MLE}-Signifikanztests. Die stochastische Auswertung liefert artefaktfreie Verteilungen.}
    \label{fig:ma_abb5.4.3_phisto_all_wbl}
\end{figure}

Auf eine isolierte, parameterweise Auswertung der Schätzvarianz (\ac{MSE}) wird an dieser Stelle verzichtet, da sich die stochastische Unsicherheit des Modells durch die asymmetrische Stützstellenallokation direkt in der lokalen \ac{SPV} am fernen Prognosepunkt aggregiert und dort im Rahmen der ökonomischen Synthese über den \ac{EPI} ($\sym{eta}_{\textnormal{eco}}$) holistisch bewertet wird.

Hinsichtlich der $\sym{power}$ manifestiert sich die Asymmetrie des Designs unmittelbar in der \ac{MLE}-Gütefunktion, vgl. Abbildung~\ref{fig:ma_abb5.4.3_powerdev_wbl}.
Während das rein passiv ausgedünnte $\textnormal{\ac{CCD}}_{-\textnormal{\ac{AP}}}$ einen verhältnismäßig symmetrischen Detektionsverlust der Krümmungsterme auf ca. $93{,}9\,\%$ verzeichnet, forciert die Translation der \acp{AP} im \ac{EmALT}-Design ein stark polarisiertes Verhalten.
Die Detektionsgarantie des ungestörten quadratischen Effektes $\hat{\sym{beta}}_{11}$ erfährt eine signifikante Stabilisierung und übertrifft mit $99{,}01\,\%$ gar das makellose Niveau des Basis-\ac{CCD} ($98{,}33\,\%$).
Im Gegenzug verzeichnet der korrespondierende Krümmungseffekt der modifizierten Stressachse ($\hat{\sym{beta}}_{22}$) einen methodisch unvermeidbaren Trennschärfe-Rückgang auf $75{,}04\,\%$.
Auch $\hat{\sym{beta}}_{12}$ reagiert auf die topologische Verzerrung mit einem reduzierten Detektionsniveau von $64{,}53\,\%$.
In der Gesamtbewertung der Zuverlässigkeitstechnik ist dieses stochastische Opfer jedoch relativierend einzuordnen:
Eine \textit{a posteriori} $\sym{power}$ von rund $75\,\%$ stellt für Terme zweiter Ordnung in experimentellen Lebensdaueranalysen gemeinhin eine statistisch völlig hinreichende Nachweiswahrscheinlichkeit dar \cite{Cohen.1988}.
Das \ac{EmALT}-Design opfert folglich bewusst einen Teil der extremen, oftmals überdimensionierten statistischen Konfidenz lokaler Modellparameter, um im Gegenzug die entscheidende Informationsstruktur am fernen Prognosepunkt abzusichern, ohne am Prüfstand vollständig zu kollabieren.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{5cm}
    \input{plots/ma_abb5.4.3_powerdev_wbl.tex}
    \caption{Vergleichende Analyse der \ac{MLE}-$\sym{power}$. Das \ac{EmALT}-Design rettet die für die Modellierung essenziellen Terme vor dem vollständigen Zusammenbruch.}
    \label{fig:ma_abb5.4.3_powerdev_wbl}
\end{figure}

Die abschließende ökonomische und stochastische Synthese manifestiert die finale Entscheidungsoberfläche der Versuchsplanauswahl - vgl. Tabelle~\ref{tab:results_emalt_kpi_eco} sowie Abbildung~\ref{fig:ma_abb5.4.3_tradeoff_eco}.
In dieser Synthese kulminiert zugleich die fundamentale methodische Differenzierung zwischen \textbf{statistischer Effektivität} und \textbf{ökonomischer Effizienz}, welche den Kern dieser Arbeit prägt.
Während die vorangegangenen Abschnitte nachgewiesen haben, dass das \ac{EmALT}-Design prinzipiell statistisch effektiv agiert - also die methodische Fähigkeit besitzt, die Modellparameter erwartungstreu zu schätzen und die Prädiktionsvarianz im Einsatzbereich gezielt abzusichern -, bewertet der \ac{EPI} erstmals die übergeordnete Effizienz: das konkrete Verhältnis aus dieser statistischen Zielerreichung und den hierfür real aufzuwendenden Prüfstandsressourcen.

Der linke Teilplot in Abbildung~\ref{fig:ma_abb5.4.3_tradeoff_eco} veranschaulicht diese zwingende physikalisch-ökonomische Kopplung anhand einer exponentiellen Effizienzgrenze:
Die Beibehaltung des orthogonalen Basis-\ac{CCD} charakterisiert sich durch minimale \ac{EoL}-Ver\-suchs\-dauern und folglich geringe direkte Testkosten. Dennoch erweist sich dieser Ansatz als ineffizient, da die Vermeidung von Prüfkosten mit einer stochastisch inakzeptablen Prädiktionsunsicherheit im Feldbereich erkauft wird - das Design verfehlt somit bereits die Grundvoraussetzung der Effektivität.
Das theoretische Feld-\ac{CCD} markiert das konträre Extremum: Zwar generiert es prinzipbedingt die maximale Effektivität am Prognosepunkt (\ac{SPV}$ \approx 1{,}0$), provoziert jedoch durch unbeschleunigte Prüfzeiten eine massive Bilanzerhöhung des Kostenerwartungswerts $\sym{Er}(\sym{C}_{\sym{idx_cost_total}})$, was seine ökonomische Effizienz vollständig zunichtemacht.
Das \ac{EmALT}-Design positioniert sich auf dieser Pareto-Kurve als das maßgebliche physikalisch-ökonomische Optimum:
Es transformiert hohe statistische Effektivität durch die drastische Verkürzung der Versuchszeiten via \ac{ALT} in maximale ökonomische Effizienz.

Dieser Befund belegt die unzureichende Validität klassischer \ac{DoE}-Metriken im Rahmen der Zuverlässigkeitstechnik.
Während die konventionelle \ac{RSM} ein \textit{D}-op\-ti\-ma\-les Design (Maximierung von $\sym{eff_D}$) als universelle Zielgröße postuliert, widerlegt die vergleichende Analyse in Abbildung~\ref{fig:ma_abb5.4.3_tradeoff_eco} (rechts) diese Prämisse fundamental.
Die Gegenüberstellung von \textit{D}-Effizienz und dem holistischen \ac{EPI} - hier exemplarisch für den hochsensitiven Krümmungsterm $\sym{epi}({\sym{beta}_{22}})$ visualisiert - offenbart eine strikt inverse Kausalität: Das Basis-\ac{CCD} erreicht zwar nominell die höchste \textit{D}-Effizienz, disqualifiziert sich jedoch unter Berücksichtigung der gewichteten Extrapolationsgüte im Kontext der Lebensdauererprobung vollständig.
Demgegenüber dominiert das \ac{EmALT}-Design - ungeachtet seiner durch die geometrische Asymmetrie bedingten, klassisch geringsten \textit{D}-Effizienz - die tatsächliche \textbf{wirtschaftliche Effizienz} (\ac{EPI}) signifikant.
Wenngleich die quantitative Ausprägung des \ac{EPI} durch die spezifische Parametrisierung des Kostenmodells und die Kalibrierung des Arbeitsbereiches mit $\sym{snr} = 1{,}20$ determiniert wird, besitzt die identifizierte Pareto-Topologie universelle Gültigkeit für beschleunigte Lebensdauerprüfungen.
Das theoretische Feld-\ac{CCD} erzielt zwar durch \ac{SPV}$ = 2{,}60$ eine hohe Prognosegüte, verursacht jedoch durch unbeschleunigte Prüfzeiten prohibitive Erwartungskosten von $\sym{Er}(\sym{C}_{\sym{idx_cost_total}}) = 128{,}7\text{ kCU}$.
Die unbeschleunigte Erprobung unter Feldbedingungen erweist sich gegenüber der für das \ac{EmALT}-Design geschätzten Kostenumfänge demnach als um den Faktor $1{,}59$ kostenintensiver, vergleiche Tabelle~\ref{tab:results_emalt_kpi_eco}.
Das im \ac{ALT}-Bereich verortete Design unterbieten diesen Ressourcenaufwand drastisch, wobei das \ac{EmALT}-Design diese deutliche Ersparnis bei nahezu identischer Prädiktionsschärfe (\ac{SPV}$ = 2{,}39$) realisiert.
Obgleich das Basis-\ac{CCD} mit $74{,}4\text{ kCU}$ den absolut geringsten monetären Aufwand erfordert, kollabieren seine $\sym{epi}$-Werte aufgrund der massiven Extrapolationsvarianz ($\sym{pred_var} = 20{,}5$) nahezu vollständig.
Im Gegensatz dazu dominiert das \ac{EmALT}-Design die Metrik mit weitem Abstand -- und zeigt insbesondere bei den für die Krümmung essenziellen quadratischen Termen (wie $\hat{\sym{beta}}_{22}$) signifikant überlegene $\sym{epi}$-Werte.
Die numerische Bilanz belegt damit unmissverständlich, dass das \ac{EmALT}-Konzept das globale Optimum aus Kostendämpfung durch Beschleunigung und zwingend erforderlicher Prognosesicherheit im fernen Feldbereich darstellt.
Dass diese antagonistische Kausalität kein isoliertes Phänomen des quadratischen Terms darstellt, sondern für alle verbleibenden Modellkoeffizienten systematisch Bestand hat, wird durch die vollumfängliche Evaluation in Tabelle~\ref{tab:results_emalt_kpi_eco} und Anhang~\ref{app:c_effizienz} zweifelsfrei belegt.
Ein rein durch klassische Optimalitätskriterien motiviertes Festhalten an orthogonalen Versuchsplänen erweist sich somit im Rahmen der Lebensdauererprobung mittels \ac{DoE} als systematisch ineffizient, da vordergründig eingesparte Prüfstandsressourcen durch eine unzulässig hohe, potenziell sicherheitskritische Prognoseunsicherheit im Feldbetrieb kompensiert werden müssen.

\input{tables/tab_results_emalt_kpi_eco.tex}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{5cm}
    \input{plots/ma_abb5.4.3_tradeoff_eco.tex}
    \caption{Links: Exponentieller Zielkonflikt aus Prädiktionsvarianz und Erwartungskosten mit dem \ac{EmALT}-Design als ökonomischem Optimum. Rechts: Inverse Kausalität zwischen klassischer \textit{D}-Effizienz und \ac{EPI} am Beispiel von $\sym{beta}_{22}$ (vollständige Evaluation in Anhang~\ref{app:c_effizienz}).}
    \label{fig:ma_abb5.4.3_tradeoff_eco}
\end{figure}

\subsubsection{Konsequenzen für die Prognoseunsicherheit im Feldbetrieb} \label{subsubsec:ci_reliability}

Die übergeordnete Zielsetzung dieser Arbeit -- die \textbf{Entwicklung effizienter multivariater Lebensdauertests} -- erfüllt sich vor diesem Hintergrund in ihrer finalen ingenieurswissenschaftlichen Konsequenz: der Konstruktion belastbarer Prognosemodelle für Lebensdauern und nicht zuletzt für Zuverlässigkeiten.
Der ökonomische Mehrwert des \ac{EmALT}-Designs, holistisch quantifiziert durch den \ac{EPI}, verbleibt dabei nicht als abstrakte methodische Kennzahl, sondern übersetzt sich in der industriellen Praxis in eine höhere Präzision dieser Vorhersagemodelle bei effizienter Ressourcennutzung.
Wie flankierende Untersuchungen zu \acp{CI} in optimierten Versuchsplänen belegen \cite{Arndt.2025}, korreliert die topologische Kontrolle der \ac{SPV} mit präziseren Schätzungen der Vertrauensbereiche für die prognostizierten Ausfallwahrscheinlichkeiten im realen Einsatzraum.

Um den methodischen Transfer aufzuzeigen, wie aus den ermittelten Regressionsparametern in $\hat{\sym{theta}}$ nun ein finales Zuverlässigkeitsmodell generiert wird, visualisiert Abbildung~\ref{fig:ma_abb5.4.3_reliability_ci} den simulativen Verlauf der Überlebenswahrscheinlichkeit $\sym{R}(\sym{t})$ exakt am anvisierten Extrapolationspunkt $(-0{,}7 ; -1{,}9)$ im Feldbereich (vgl. Abschnitt~\ref{subsec:evg_plot}).
Dargestellt sind die Zuverlässigkeitsfunktionen auf Grundlage der Gleichungen~\ref{eq:reldef}, \ref{eq:glm_link} und \ref{eq:var_T_general} sowie die korrespondierenden Bänder der $90\,\%$-\acp{CI}, resultierend aus der Fisher-Informationsmatrix (\ac{FIM}) der jeweiligen \ac{MLE}-Modellierung.
Als objektive Referenz dient hierbei die Kurve des wahren Systemverhaltens nach bekannter Vorgabe aus Tabelle~\ref{tab:simulation_parameters_weibull} mit $\sym{b}=3$.

Während die mittleren $\sym{R}(\sym{t})$-Prognosen (Punktschätzer) beider Designs -- im Rahmen der verbleibenden Abweichungen der Koeffizientenschätzungen in $\hat{\sym{beta}}$ -- den wahren physikalischen Degradationsverlauf valide approximieren, manifestieren sich im direkten Vergleich der Konfidenzbänder feststellbare topologische zusätzliche Unterschiede.
Bedingt durch die massive Extrapolationsdistanz unterliegen naturgemäß beide Versuchspläne einer substanziellen Prognosevarianz, sodass ihre absoluten Streuungsamplituden auf einem vergleichbaren Grundniveau in $\sym{pred_var}$ verharren.
Dennoch induziert das voll-orthogonale Basis-\ac{CCD} die prägnanteste Aufweitung der Vertrauensgrenzen.
Demgegenüber gelingt es dem \ac{EmALT}-Design, diesen Unsicherheitsbereich durch die strategische Varianzdämpfung seiner Ankerpunkte sowohl kosteneffizienter als auch messbar zu verdichten.
Die optisch leicht wellige Ausprägung der simulierten Vertrauensgrenzen $[\hat{\sym{R}}(\sym{t})_{\sym{idx_u}}, \hat{\sym{R}}(\sym{t})_{\sym{idx_o}}]$ ist dabei methodisch begründet: Sie resultiert aus der multidimensionalen Natur der Stressoren in Kombination mit der empirischen Extraktion der Schranken mittels der Kovarianzmatrix $\hat{\sym{V}}$.
Die stochastischen Streuungen der $1\times 10^4$ Monte-Carlo-Schätzer transformieren sich stark nichtlinear durch die Weibull-Gleichung~(\ref{eq:reldef}), was bei der punktweisen Ermittlung der Perzentile über die fortlaufende Zeitachse $\sym{t}$ zu leichten lokalen Fluktuationen führt.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{6.5cm}
    \input{plots/ma_abb5.4.3_reliability_ci.tex}
    \caption{Vergleich der prognostizierten Überlebenswahrscheinlichkeit $\sym{R}(\sym{t})$ am Feldbetriebspunkt inklusive $90\,\%$-Konfidenzband. Aufgrund der Distanz weisen beide Designs hohe Prognoseunschärfen auf, das \ac{EmALT}-Design (schwarz) reproduziert das wahre Zuverlässigkeitsmodell jedoch mit stellenweise geringerer Varianz als das Basis-\ac{CCD} (grau).}
    \label{fig:ma_abb5.4.3_reliability_ci}
\end{figure}

In der industriellen Praxis und vor dem Kontext eines Prüflingsumfangs von $\sym{N}=13$ sind selbst derartige moderate Reduktionen der Vertrauensbereiche von Bedeutung, da normative Sicherheitsnachweise und Wartungsintervalle zumeist zwingend auf der unteren Vertrauensgrenze einer definierten $\sym{R}(\sym{t})$ basieren \cite{Meeker.2022,Bertsche.2022}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Methodische Synthese und Transfer} \label{sec:chap5_fazit}

Das vorliegende Kapitel erbringt damit den theoretischen und simulativen Nachweis, dass klassische Prinzipien der statistischen Versuchsplanung zwar ein methodisch robustes Fundament für die Lebensdauererprobung bilden, in ihrer starren Ausprägung jedoch für die Lebensdauererprobung ökonomisch ineffizient sind.
Die vollzogene Synthese liefert dabei drei zentrale Erkenntnisse für die Zuverlässigkeitstechnik:

\begin{enumerate}
    \item \textbf{Topologische Resilienz:} \acp{RSD} wie das \ac{CCD} reagieren auf periphere Modifikationen und Ausdünnungen erstaunlich robust, solange die Identifikation der Haupteffekte gesichert ist. Dies legitimiert die Abkehr vom Dogma der strengen Orthogonalität und eröffnet ein wertvolles geometrisches Gestaltungsbudget.
    \item \textbf{Extrapolationsabsicherung:} Die \ac{SPV} verhält sich in gestörten Designs hochgradig richtungssensitiv.
          Die entwickelte \ac{EmALT}-Positionierung nutzt das freigewordene Gestaltungsbudget proaktiv, um über asymmetrische \acp{AP} die Prognoseunsicherheit zielgerichtet entlang des Extrapolationsvektors zum fernen Feldbetriebspunkt zu minimieren.
          Der \ac{EVG} dient hierbei als analytisches Instrumentarium, um die topologische Sensitivität von $\sym{pred_var}$ zu quantifizieren und die Ankerpunktpositionierung systematisch zu optimieren.
    \item \textbf{Ökonomische Effizienz:} Die strenge Entkopplung von Design-Optimalität und Versuchsdauer verliert in der Lebensdauererprobung ihre Gültigkeit. Der neu eingeführte \ac{EPI} ($\sym{epi}$) weist nach, dass sich das \ac{EmALT}-Design im Zielkonflikt zwischen stochastischer Prädiktionsgüte und exponentiell skalierenden Prüfkosten als das überlegene physikalisch-ökonomische Optimum positioniert und klassisch \textit{D}-optimale Pläne signifikant dominiert.
          Auch entlang der in diesem Kapitel entwickelten Methodik ergeben sich zudem Freiräume alternativer, ökonomisch effizienter Versuchspläne, die in der industriellen Praxis zu einer signifikanten Reduktion der Prüfstandsressourcen bei gleichzeitiger Absicherung der Prognosegüte führen.
\end{enumerate}

Trotz dieser eindeutigen Befunde bedarf es einer kritischen Würdigung der Simulationsergebnisse.
Die numerische Evaluation basiert auf der Prämisse eines strukturell korrekten log-linearen \ac{AFT}-Modells sowie einer stetigen Degradationskinematik.
Treten in der physikalischen Realität abrupte Wechsel der Schädigungsmechanismen zwischen \ac{ALT}-Niveau und Feldbereich auf, geraten rein modellbasierte Extrapolationen unweigerlich an ihre methodischen Grenzen.
Gerade vor diesem Hintergrund entfaltet das \ac{EmALT}-Design jedoch einen weiteren, entscheidenden Praxisvorteil: Durch die strategische Verortung der \acp{AP} nahe dem realen Einsatzniveau fungieren diese Stützstellen nicht nur als stochastische Varianzdämpfer, sondern zugleich als physikalische Frühwarnsensoren.
Im Rahmen der post-experimentellen Datenanalyse lassen sich diese Ankerpunkte analog zu \textit{Confirmation Experiments} auswerten \cite{Stevens.2020}.
Entsprechende Metriken, wie etwa die \textit{Probability of Agreement}, können hierbei herangezogen werden, um empirisch zu validieren, ob die am Hochlastprüfstand extrahierten Degradationsgesetze im moderaten \textit{Use-Space} weiterhin Gültigkeit besitzen - eine Detektionsmöglichkeit, die durch reines \ac{ALT} vollständig verwehrt bliebe.
