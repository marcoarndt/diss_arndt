% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% %% Kapitel 5 - Entwurf %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Effizienz und Effektivität in der multivariaten Versuchsplanung} \label{chap:entwurf}

Nach der in Kapitel~\ref{chap:screening} beschriebenen qualitativen Vorselektion der Einflussgrößen ist nun die quantitative Planung durch die Entwicklung adäquater, effizienter und multivariater Versuchspläne erforderlich.
Ziel ist es, etablierte Standard-\acp{RSD} gezielt an die spezifischen Anforderungen der Lebensdauerprognose anzupassen, ohne deren statistische Integrität unzulässig zu kompromittieren.
In Anlehnung an die Ausführungen aus Abschnitt~\ref{sec:forschungsfragen} gilt dabei fortwährend die Prämisse, dass insbesondere das \ac{CCD} aufgrund seiner sequenziellen Augmentierbarkeit (Screening $\to$ lineares Modell $\to$ quadratische Modellterme, vgl. Abbildung~\ref{fig:abb2.08_strategy}) im Kontext kostenintensiver \ac{EoL}-Tests einen entscheidenden operativen Vorzug bietet und daher im Fokus der vorliegenden Untersuchung steht.

Dieses Kapitel widmet sich  in zwei Schritten zunächst der systematischen Analyse geometrischer Design-Abweichungen und deren Auswirkung auf die statistische Güte \colorbox{yellow}{(siehe Abschnitt~\ref{sec:abweichungen})}.
Unabhängig von einer möglichen Beschleunigung durch \ac{ALT} wird zuerst untersucht, inwieweit typische Manipulationen der Design-Geometrie wie variierende Koordinaten oder asymmetrische Replikationen der Versuchspunkte sowie mangelnde Robustheit in der Versuchssteuerung die Performance beeinflussen.
Um diese komplexen Abhängigkeiten generisch und losgelöst von spezifischen Produktdaten zu untersuchen, ist der Einsatz numerischer Simulationen immer dann erforderlich, wenn die Parameterschätzung betreffende Metriken erfasst werden sollen.
Mithilfe von \acp{MCS} und fiktiven physikalischen Modellen wird die Entwicklung dieser Verhältnismäßigkeiten somit systematisch aufgezeigt, siehe \colorbox{yellow}{Abschnitt~\ref{subsec:simulationsmethodik}}.
Erst so werden \textbf{Auswirkungen von Orthogonalitäts-Abweichungen} quantifizierbar und damit die Grundlage für die Auswahl von Design-Alternativen geschaffen.

In einem zweiten Schritt werden als signifikant und dominant erfasste Einflüsse durch Design-Modifikationen hinsichtlich ihrer generischen Auswirkung auf die Modellierung bewertet \colorbox{yellow}{(Abschnitt~\ref{subsec:sensitivitaetsanalyse})}.
Sie sollen im weiteren Verlauf der Untersuchung als Grundlage für die Auswahl vielversprechender Design-Alternativen dienen und im Anschluss ökonomisch bewertet.
Um hierbei valide Aussagen über die Effektivität einer Konfiguration zu treffen, wird neben der bloßen Versuchsplan-Performance oder Schätzgüte maßgeblich die \textbf{statistische Trennschärfe (\ac{power})} als übergeordnete Metrik herangezogen.
Sie fungiert als Indikator für die Erfolgsgarantie der Testagenda, indem sie die Wahrscheinlichkeit beziffert, mit der real existierende Effekte tatsächlich mithilfe des jeweiligen Versuchsplans als signifikant erkannt werden, vgl. \colorbox{yellow}{Abschnitt~\ref{subsec:optimal}}.
Was in der allgemeinen \ac{DoE}-Methodik oft als nachgeordnete Größe behandelt wird \cite{Rigdon.2022,Montgomery.2020}, avanciert bei kostenintensiven \ac{EoL}-Tests zum \ac{KPI}: Jede Manipulation des Designs muss zwingend gegen den potenziellen Verlust an der Detektionswahrscheinlichkeit hinsichtlich der Effekte auf die Lebensdauer innerhalb des \ac{GLM} abgewogen werden.

Darauf aufbauend führt \colorbox{yellow}{Abschnitt~\ref{sec:kostenmodell}} ein Kostenmodell ein, um die monetären Konsequenzen derartiger Modifikationen zu quantifizieren.
Die Bewertung der Versuchspläne erfolgt dabei streng nach der Kerndefinition von Effizienz als Maß für die Leistungsfähigkeit: Welche Information wird perspektivisch generiert und welcher monetäre Aufwand steht dem gegenüber?
Daraus leitet sich die zentrale ökonomische Leitfrage ab: \textit{Wie viel Trennschärfe ist finanzierbar?}
Erst so wird quantifiziert, inwiefern zufällige oder möglicherweise bewusste Design-Modifikationen die \textbf{Effizienz} des \ac{RSD} verändern und zu welchem Maß die \textbf{Effektivität} dabei beeinträchtigt wird.

Zur Bewertung der Prognosegüte wird abschließend eine Erweiterung bestehender Visualisierungsmethoden vorgestellt \colorbox{yellow}{(Abschnitt~\ref{sec:visualisierung})}, bevor die Gesamtmethodik in der numerischen Simulationsstudie anhand der Zuverlässigkeitsmodellierung mit Aussagesicherheit final bewertet wird \colorbox{yellow}{(Abschnitt~\ref{sec:zuv_vb})}.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Effektivität von Versuchsplänen mit Orthogonalitäts-Abweichungen} \label{sec:abweichungen}
Generell können sich Orthogonalitäts-Abweichungen durch physikalische Restriktionen der technischen Versuchsausführung, die spezifische Performance und Funktionalität des Systems oder Produkts prinzipiell oder bei \ac{ALT} durch möglicherweise bewusste Manipulation des Versuchsraums zur Steigerung der Extrapolationsgüte sein.
Insbesondere im Kontext der Lebensdauerprognose sind solche Eingriffe praxisrelevant und begründet: Die Verschiebung der Versuchspunkte in Richtung höherer Lastniveaus verkürzt die Prüfzeit und senkt die Kosten, während eine Verortung nahe dem Operationsbereich die Modellanpassung verbessert und die Extrapolationsunsicherheit reduziert.

Allerdings bergen solche Modifikationen das Risiko, die statistische Integrität des Designs zu beeinträchtigen.
Im Idealfall sind die Design-Matrix-Spalten eines \ac{CCD} orthogonal zueinander, was eine unabhängige Schätzung der Modellparameter ermöglicht - vgl. Abschnitt~\ref{subsubsec:rsm}.
Geometrische Verzerrungen führen zwangsläufig zu einem Verlust dieser Orthogonalität (vgl. Gleichung~\ref{eq:skalar}).
Besonders kritisch ist, dass die Auswirkungen peripherer Parameterstufen (Sternpunkte) im augmentierten Ansatz erst spät im sequenziellen Prozess sichtbar werden - nämlich dann, wenn das initiale faktorielle Design erweitert wird.
Daher ist es essenziell, die Robustheit des \ac{CCD} gegenüber typischen Design-Abweichungen systematisch zu quantifizieren.
Entgegen bereits erfolgter Untersuchungen durch \textcite{Box.1963}, \textcite{Ahn.2015}, \textcite{Donev.2004}, \textcite{Zhang.2021} sowie \textcite{Jang.2002} oder vergleichbarer liegt der Fokus hierbei allerdings nicht auf der reinen geometrischen Verzerrung und der Analyse sowie Bewertung durch geeignete Optimalitätskriterien (entsprechend Abschnitt~\ref{subsec:optimal}).
Die hier erwähnten Beiträge konzentrieren sich primär auf die geometrische Charakterisierung der Design-Abweichungen und deren Einfluss auf die Struktur der Design-Matrix bzw. geeigneter Optimalätskriterien.
Vielmehr erfolgt hier eine Quantifizierung der Auswirkungen auf die Schätzpräzision und insbesondere die Trennschärfe im Kontext von Lebensdauerprognosen.
Die Bewertung mittels Optimalitätskriterien wäre aus genannten Gründen ohnehin nicht zielführend, da im Umkehrschluss die Erfüllung der Kriterien im Planungsprozess erneut Unkenntnis über die tatsächlichen Auswirkungen auf die Schätzpräzision und Trennschärfe erzeugen würde.
Dies hat zur Folge, dass zunächst eine numerische Simulation erforderlich ist, um die Auswirkungen von Design-Abweichungen auf die statistische Güte quantifizieren zu können, bevor überhaupt eine Bewertung der Design-Alternativen erfolgen kann.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Methodik zur simulativen Ermittlung von Auswirkungen durch Orthogonalitäts-Abweichungen} \label{subsec:simulationsmethodik}
Um die Auswirkungen von Design-Abweichungen auf die statistische Güte quantifizieren zu können, bedarf es einer kontrollierten Untersuchungsumgebung, in der - im Gegensatz zu physischen Experimenten - die \textbf{wahre} Natur des Systems bekannt ist.
Durch den Vergleich von beiderseits Modellierungs- als auch Analyse-Ergebnissen mit einer vorab definierten Wahrheit (engl. \textit{Ground Truth}) lassen sich \acp{KPI} wie die Schätzpräzision und allen voran die \ac{power} bestimmen.
Zu diesem Zweck wird für die Untersuchungen eine allgemeingültige Simulationsumgebung geschaffen, die auf einem scheinbar-physikalischen Modell basiert \cite{Arndt.2023,Arndt.2022c}.\\

\textbf{Pseudo-physikalisches Simulationsmodell} \\
Das zugrundeliegende Systemverhalten im Sinne einer \textit{Ground Truth} wird durch ein polynomiales Regressionsmodell zweiter Ordnung approximiert.
Dies entspricht der Struktur, die auch als Ergebnis der \ac{RSM}-Untersuchung erwartet wird (vgl. Gleichung~\ref{eq:quadric_std_model}) und möglicherweise im Zuge einer Zuverlässigkeitsuntersuchung eines technischen Produktes resultieren kann.
Um die Anforderungen an eine multivariate Untersuchung ($\sym{k} \ge 2$) zu erfüllen und die Komplexität handhabbar zu halten, wird im Basisszenario von zwei Faktoren ($\sym{x}_1, \sym{x}_2$) ausgegangen.
Das Modell berücksichtigt neben den Haupteffekten explizit die für die Lebensdaueranalyse kritischen Wechselwirkungen ersten Grades sowie quadratische Terme zur Abbildung von Nichtlinearitäten:
\begin{equation} \label{eq:simulation_model}
    \sym{y}_{\sym{idx_sim}} = \sym{beta}_{0} + \sym{beta}_{1}\sym{x}_{1} + \sym{beta}_{2}\sym{x}_{2} + \sym{beta}_{12}\sym{x}_{1}\sym{x}_{2} + \sym{beta}_{11}\sym{x}_{1}^2 + \sym{beta}_{22}\sym{x}_{2}^2 + \sym{epsilon}.
\end{equation}

Um ein realistisches, stochastisches Verhalten abzubilden, sind die Modellparameter nicht deterministisch fixiert.
Den Regressionskoeffizienten $\symsub{beta}{idx_j}$ werden zwar nominale Startwerte zugewiesen (vgl. Tabelle~\ref{tab:simulation_parameters}), diese werden jedoch in jedem Simulationslauf mit einer definierten Streuung überlagert.
Zusätzlich wird die resultierende Systemantwort $\sym{y}_{\sym{idx_sim}}$ mit einer Reststreuung $\sym{epsilon}$ beaufschlagt, um die inhärente \textbf{aleatorische Unsicherheit} des Systems (Messfehler, unbekannte Einflüsse) abzubilden.
Mathematisch wird dies proportional durch normalverteilte Zufallsvariablen modelliert, sodass für den Fehlerterm sowie für die fluktuierenden Modellparameter um ihre nominalen Startwerte ($\sym{beta}_{\sym{idx_j}, \text{nom}}$) gilt:
\begin{equation} \label{eq:simulation_stochastics}
    \sym{epsilon} \sim \mathcal{N}(0, \symsub{sigma}{idx_sim}^2) \quad \text{und} \quad \symsub{beta}{idx_j} \sim \mathcal{N}(\sym{beta}_{\sym{idx_j}, \text{nom}}, \symsub{sigma}{idx_sim}^2).
\end{equation}

Die bewusste Wahl der Normalverteilung als zugrundeliegende Stochastik ist für diese isolierte Designuntersuchung methodisch zwingend und zweckdienlich:
Sie entspricht der elementaren Grundannahme klassischer Regressionsmodelle (Gauß-Markow-Theorem) und stellt sicher, dass die nachfolgende statistische Inferenz (Berechnung von $\sym{p-Wert}$ und \acp{CI}) im ungestörten Basis-Design ideal und erwartungstreu abläuft.
So wird zweifelsfrei gewährleistet, dass später beobachtete Schätzfehler oder $\sym{power}$-Verluste ausschließlich auf die induzierten geometrischen Orthogonalitäts-Abweichungen (Design-Penalties) zurückzuführen sind und nicht durch systematische, verteilungsbedingte Residuenfehler verfälscht werden.

Die Default-Werte für die Koeffizienten sowie die vorgegebene Standardabweichung $\sym{sigma}_{\sym{idx_sim}}$, welche als Streuparameter für das stochastische Basis-Verhalten dient, sind in Tabelle~\ref{tab:simulation_parameters} zusammengefasst.

\begin{table}[htbp]
    \centering
    \caption{Nominale Startwerte und Streuparameter des pseudo-physikalischen Simulationsmodells}
    \label{tab:simulation_parameters}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{@{}lr@{}}
        \toprule
        \textbf{Modellparameter}                                                           & \textbf{Default-Wert} \\
        \midrule
        Regressionskonstante $\sym{beta}_{0}$                                              & $30$                  \\
        Regressionskoeffizienten $\symsub{beta}{idx_j}$ (für $j \in \{1, 2, 12, 11, 22\}$) & $10$                  \\
        Standardabweichung der Systemantwort $\symsub{sigma}{idx_sim}$                     & $1$                   \\
        \bottomrule
    \end{tabular}
\end{table}

\textbf{Simulationsalgorithmus und Monte-Carlo-Ansatz} \\
Das Modell der \textit{Ground Truth} wird zunächst mit den zu untersuchenden Versuchsplänen gekoppelt.
Für jeden Stützpunkt des Versuchsplans (Versuchspunkte) werden die korrespondierenden Systemantworten simuliert.
Anschließend erfolgt die Rückrechnung des Regressionsmodells mittels \ac{MLE}, entsprechend der ausgeführten Methodik aus Abschnitt~\ref{subsubsec:mle}.
Um statistisch belastbare Aussagen zu treffen und \textbf{epistemische Unsicherheiten} auf einen minimalen numerischen Approximationsfehler zu reduzieren, wird das Verfahren in eine \ac{MCS} mit $\symsub{n}{idx_MC} = 10.000$ Iterationen eingebettet -- vergleiche Abschnitt~\ref{subsec:optimal} für entsprechende Hintergründe.
Dieser Ansatz erlaubt die Bewertung zweier zentraler Kriterien:
\begin{enumerate}
    \item \textbf{Präzision der Parameterschätzung:} Durch den (Mittelwert-) Vergleich der geschätzten Parameter $\hat{\sym{Beta}}$ mit den \textit{wahren} Koeffizienten $\sym{Beta}_{\text{true}}$ lassen sich systematischer Schätzfehler (\textbf{Bias}) und Varianz (\textbf{\ac{MSE}}) der Schätzung ermitteln.
    \item \textbf{Bestimmung der Trennschärfe $\sym{power}$:} Für jeden Simulationslauf wird geprüft, ob die signifikanten Effekte korrekt erkannt wurden ($\sym{p-Wert} < \sym{alpha}$). Das Verhältnis der Treffer zur Gesamtzahl der Simulationen ergibt die empirische \ac{power} (vgl. Gleichung~\ref{eq:power}).
\end{enumerate}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Sensitivitätsanalyse geometrischer und stochastischer Einflussgrößen} \label{subsec:sensitivitaetsanalyse}
Um zunächst die Robustheit des methodischen Ansatzes gegenüber den eingangs erwähnten Einflussgrößen zu bewerten, wird eine Sensitivitätsanalyse durchgeführt, welche methodische einem \textbf{\ac{DoE} on Experimental Design}-Ansatz folgt:
Die Eigenschaften des Versuchsplans sowie die der \textit{Ground Truth} werden als Faktoren eines übergeordneten Meta-Designs derart variiert, sodass deren Einfluss auf die Effektivität der Schätzung und damit insbesondere die Trennschärfe der Effekte im \ac{GLM} quantifiziert werden kann \cite{Arndt.2023}.
Infrage kommt dabei eine aus praktischer Überlegung erfasste Auswahl an sowohl geometrische Manipulationen als auch stochastische Einflüsse sowie methodische Parameter und deren Interaktionen:
\begin{enumerate}
    \setlength{\itemsep}{0pt}
    \setlength{\parskip}{0pt}
    \item Manipulation von Sternpunkten über deren axialen Abstand;
    \item Manipulation von Sternpunkten durch co-faktorielle Korrelation;
    \item Variation der Anzahl der Zentralpunkte;
    \item Variation der Anzahl der Sternpunkte;
    \item Variation der Standardabweichung der Effektstärken (System-Rauschen);
    \item Änderung des relativen Verhältnisses von Effektstärken untereinander;
    \item Variation der Standardabweichung der Systemantwort (Mess-Rauschen);
    \item Variation des Signifikanzniveaus.
\end{enumerate}
Insbesondere die Berücksichtigung der Verhältnismäßigkeit von Effektstärken sowie der Rauschparameter ist von zentraler Bedeutung, da sie die \ac{SNR} im Modell direkt beeinflussen und damit die Detektionswahrscheinlichkeit der Effekte maßgeblich steuern, gleichzeitig aber eine gegen unendlich tendierende Variationsmöglichkeit bieten, die in einem reinen Screening-Ansatz nicht abgedeckt werden könnte.
Zur formalen Beschreibung des Untersuchungsraums werden diese acht Einflussgrößen im Vektor $\sym{xi_vek} = [\symsub{xi_factor}{idx_fac_a},\dots, \symsub{xi_factor}{idx_fac_h}]^\top$ zusammengefasst und in Tabelle~\ref{tab:sensitivitaetsfaktoren} definiert.
Die Integration dieser Faktoren in das Meta-Design erfolgt dabei methodisch differenziert: Während die kontinuierlichen Stör- und Geometrieparameter ($\symsub{xi_factor}{idx_fac_a}, \symsub{xi_factor}{idx_fac_b}, \symsub{xi_factor}{idx_fac_e}, \symsub{xi_factor}{idx_fac_f}, \symsub{xi_factor}{idx_fac_g}, \symsub{xi_factor}{idx_fac_h}$) den Versuchsraum eines \ac{CCD} aufspannen, um stetige Sensitivitäten (Regressionskoeffizienten) zu schätzen, werden die strukturellen Designgrößen der Stützstellenanzahl ($\symsub{xi_factor}{idx_fac_c}, \symsub{xi_factor}{idx_fac_d}$) als kategoriale Blockfaktoren über drei diskrete Stufen behandelt.
Diese Trennung ist zwingend geboten, da Variationen in der Stützstellenanzahl die topologische Struktur der Design-Matrix $\sym{X}$ und deren Freiheitsgrade diskontinuierlich verändern.
Ein rein kontinuierliches Meta-Modell würde hier zu unzulässigen Interpolationen über strukturelle Singularitäten hinweg führen -- beispielsweise beim vollständigen Verlust der Schätzbarkeit quadratischer Terme bei extremer Ausdünnung.
Folglich werden die kontinuierlichen Sensitivitäten der Rauschparameter stets \textit{innerhalb} konsistenter topologischer Design-Strukturen ermittelt, während der Haupteffekt der Stützstellenreduktion separat über die diskreten Mittelwertverschiebungen (Level-Shifts) der \ac{power} zwischen den Blöcken quantifiziert wird.
Weiter erfüllt der \ac{DoE}-Ansatz mittels \ac{CCD} den Anspruch, für die Sensitivitätsanalyse eine effiziente und notwendigestatistische Inferenz und Kausalität zu identifizieren, wohingegen alternative Ansätze wie die reine Korrelationsanalyse, \textit{Space-Filling Designs} (wie \textit{Latin Hypercube Sampling} oder \textit{Sobol}-Sequenzen) oder die Anwendung von \ac{ML}-Modellen zur Schätzung von Sensitivitäten darüber hinaus Assoziationen für explorative Prädiktionen liefern würden \cite{Zio.2013,Kleppmann.2020}.

Etwaige Ergänzungen wie die Variation der Anzahl der Faktoren, die Manipulation der faktoriellen Versuchspunkte oder die Einführung von Lebensdauermodellen werden dabei bewusst ausgeschlossen, um die Komplexität des Meta-Designs handhabbar zu halten und die Interpretation der Ergebnisse durch eine pragmatische Auswahl und im Fokus der in Kapitel~\ref{chap:ansatz} postulierten Zielsetzung zu erleichtern.
\begin{table}[htbp]
    \centering
    \caption{Definition und Parametrisierung der Untersuchungsfaktoren für die Sensitivitätsanalyse}
    \label{tab:sensitivitaetsfaktoren}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{@{}l p{0.45\textwidth} l l@{}}
        \toprule
         & \textbf{Beschreibung}                                             & \textbf{Wertebereich} & \textbf{Charakteristik} \\
        \midrule

        $\symsub{xi_factor}{idx_fac_a}$
         & \textbf{Sternpunkt-Abstand ($\symsub{alpha}{idx_D}$)} \newline
        Variation des Abstands der Sternpunkte zum Zentrum (z.B. flächenzentriert, rotierbar und extendiert).
         & $[1,00;\, 2\cdot\symsub{alpha}{idx_D}]$
         & Kont. (\ac{CCD})                                                                                                    \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_b}$
         & \textbf{Sternpunkt-Shift} \newline
        Asymmetrische / Co-Faktorielle Verschiebung eines Sternpunkts zur Simulation geometrischer Verzerrungen und Faktoren-Korrelation.
         & $[0,10;\, \symsub{alpha}{idx_D}]$
         & Kont. (\ac{CCD})                                                                                                    \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_c}$
         & \textbf{Zentralpunkte ($\symsub{n}{idx_C}$)} \newline
        Variation der Anzahl der Zentralpunkte $\symsub{n}{idx_C}>0$.
         & $[1,00;\, 8,00]$
         & Diskret (Faktoriell)                                                                                                \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_d}$
         & \textbf{Sternpunkte ($\symsub{n}{idx_S}$)} \newline
        Variation durch Entfall von maximal $50\,\%$ der Sternpunkte (z.B. durch Zensierung oder Funktionalitätsverlust).
         & $[2,00;\, 4,00]$
         & Diskret (Faktoriell)                                                                                                \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_e}$
         & \textbf{System-Rauschen ($\sym{sigma}$)} \newline
        Verhältnis zwischen pseudo-physikalischem Modellkoeffizienten und Systemabweichung bzw. Effektstreuung.
         & $[0,80;\, 1,20]$
         & Kont. (\ac{CCD})                                                                                                    \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_f}$
         & \textbf{Effekt-Verhältnis ($\sym{beta}_1/\sym{beta}_2$)} \newline
        Verhältnis zweier Haupteffekte zueinander: zwischen gleichwertiger Effektstärke oder einem Anteil von $10\,\%$.
         & $[0,10;\, 1,00]$
         & Kont. (\ac{CCD})                                                                                                    \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_g}$
         & \textbf{Mess-Rauschen ($\sym{epsilon}$)} \newline
        Verhältnis zwischen den Modellkoeffizienten und dem Wert des Streuparameters der Systemantwort.
         & $[0,80;\, 1,20]$
         & Kont. (\ac{CCD})                                                                                                    \\
        \addlinespace

        $\symsub{xi_factor}{idx_fac_h}$
         & \textbf{Signifikanzniveau ($\sym{alpha}$)} \newline
        Das gewählte Signifikanzniveau.
         & $[0,02;\, 0,08]$
         & Kont. (\ac{CCD})                                                                                                    \\

        \bottomrule
    \end{tabular}
\end{table}

So werden schließlich Haupteffekte, Interaktionen sowie quadratische Effekte auf die Trennschärfe als wesentlicher Performance-\ac{KPI} und damit Metrik zur Effektivität ermittelt.

\subsection{Ergebnisse der Sensitivitätsanalyse und Quantifizierung der Trennschärfe} \label{subsec:ergebnisse_sensitivitaet}

Die simulative Untersuchung der Trennschärfe (\ac{power}) über den durch $\sym{xi_vek}$ definierten Untersuchungsraum offenbart eine signifikante Dichotomie der Einflussgrößen.
Es muss zwischen \textbf{dispositiven Designfaktoren} (Geometrie $\symsub{xi_factor}{idx_fac_a}$, Stützstellen $\symsub{xi_factor}{idx_fac_c}, \symsub{xi_factor}{idx_fac_d}$), die vom Versuchsplaner aktiv modifiziert werden können, und \textbf{externen Restriktionen} (Rauschen $\symsub{xi_factor}{idx_fac_e}, \symsub{xi_factor}{idx_fac_g}$, Signifikanzniveau $\symsub{xi_factor}{idx_fac_h}$), die durch das physikalische Testsystem oder normative Vorgaben als unveränderlich zu betrachten sind, unterschieden werden.

Tabelle~\ref{tab:summary_power_sensitivity} zeigt dazu entsprechende Ergebnisse der Sensitivitätsanalyse aus Abschnitt~\ref{subsec:sensitivitaetsanalyse} in Form von Mittelwerten, Wertebereichen sowie den dominanten Einflussfaktoren für die Trennschärfe der einzelnen Modellparameter.
Die Werte wurden durch eine \ac{ANOVA} des Meta-Designs ermittelt, wobei die Effektstärke $\sym{Eff}$ als Maß für die Dominanz eines Faktors auf die Trennschärfe herangezogen wird, sofern dieser als signifikant identifiziert und der im Model geschätzte Zusammenhang der Daten entsprechend erfolgreich auf Plausibilität (Residuenanalyse, Bestimmtheitsmaß, etc.) geprüft wurde.
Tabelle~\ref{tab:summary_power_sensitivity} bildet dabei eine zusammenfassende Übersicht über die Ergebnisse, wobei weiterführende Details zu den einzelnen Effekten und deren Interaktionen in Anhang~\ref{app:c} zu finden sind.

Betrachtet man zunächst die globale \textbf{Modell-Power} ($\sym{power}_{\sym{idx_M}}$), so zeigt sich eine extrem hohe Robustheit gegenüber allen Variationen mit einem Mittelwert von $\overline{\sym{power}} > 99,7\,\%$.
Diese pauschale Metrik verdeckt jedoch, dass das Verhalten spezifischer Einzel-Effekte - insbesondere die Identifikation der für die Lebensdauerprognose kritischen quadratischen Terme - deutlich sensitiver ausfällt.
Wie Tabelle~\ref{tab:summary_power_sensitivity} zeigt, liegt die mittlere Trennschärfe für quadratische Terme ($\sym{power}_{\sym{beta}_{11}}, \sym{power}_{\sym{beta}_{22}}$) nur noch im Bereich von $70\,\%$ bis $79\,\%$.

Die entscheidende Erkenntnis liefert jedoch der Blick auf die \textbf{Sensitivitäts-Indizes} (Spalte „Dominante Sensitivität“).
Für die Mehrheit der untersuchten Koeffizienten ($\sym{power}_{\sym{idx_M}}, \sym{power}_{\sym{beta}_1}, \sym{power}_{\sym{beta}_{12}}, \sym{power}_{\sym{beta}_{11}}$) ist nicht etwa eine Reduktion der Versuchspunkte der limitierende Faktor, sondern das \textbf{Messrauschen ($\symsub{xi_factor}{idx_fac_g}$)}.
Mit einem Sensitivitätsindex von bis zu $\sym{Eff}=1,99$ (bei $\sym{power}_{\sym{beta}_{11}}$) dominiert die Güte der Messtechnik die Erfolgswahrscheinlichkeit des Tests.
Da das Rauschen ($\symsub{xi_factor}{idx_fac_g}, \symsub{xi_factor}{idx_fac_e}$) sowie das Signifikanzniveau ($\symsub{xi_factor}{idx_fac_h}$) in der praktischen Anwendung bei \ac{EoL}-Tests an bestehenden Prüfständen meist feste Randbedingungen darstellen, definieren sie eine natürliche Obergrenze der erreichbaren Trennschärfe, die auch durch maximale Versuchspläne nicht durchbrochen werden kann.

\begin{table}[htbp]
    \centering
    \caption{Zusammenfassende Statistik der Trennschärfe und Identifikation der dominanten Einflussfaktoren. Die Ergebnisse demonstrieren, dass die Power maßgeblich durch unveränderliche Systemgrößen (Rauschen) und nur im Ausnahmefall durch modifizierbare Designgrößen limitiert wird.}
    \label{tab:summary_power_sensitivity}
    \renewcommand{\arraystretch}{1.2}
    \small
    \begin{tabular}{@{}l r r c r@{}}
        \toprule
        \textbf{Koeffizient}            & \textbf{Mittelwert}      & \textbf{Wertebereich}          & \multicolumn{2}{c}{\textbf{Dominante Sensitivität}}                                    \\
        \cmidrule(lr){2-3} \cmidrule(lr){4-5}
                                        & $\overline{\sym{power}}$ & $[\text{min}; \text{max}]\,\%$ & \textbf{Einflussfaktor $\symsub{xi_factor}{idx_i}$ (Kategorie)} & \textbf{$\sym{Eff}$} \\
        \midrule
        \multicolumn{5}{c}{\textit{Globale Modellgüte:}}                                                                                                                                     \\
        $\sym{power}_{\sym{idx_M}}$     & $99,7\,\%$               & $[94,5; 100,0]$                & $\symsub{xi_factor}{idx_fac_g}$ (System)                        & $0,10$               \\
        \addlinespace
        \midrule
        \multicolumn{5}{c}{\textit{Spezifische Modelleffekte:}}                                                                                                                              \\
        $\sym{power}_{\sym{beta}_0}$    & $97,9\,\%$               & $[76,9; 100,0]$                & $\symsub{xi_factor}{idx_fac_e}$ (System)                        & $0,52$               \\
        $\sym{power}_{\sym{beta}_1}$    & $78,6\,\%$               & $[60,0; \phantom{0}90,8]$      & $\symsub{xi_factor}{idx_fac_g}$ (System)                        & $1,50$               \\
        $\sym{power}_{\sym{beta}_2}$    & $66,0\,\%$               & $[37,3; \phantom{0}84,1]$      & $\symsub{xi_factor}{idx_fac_f}$ (System)                        & $2,53$               \\
        $\sym{power}_{\sym{beta}_{12}}$ & $72,6\,\%$               & $[52,5; \phantom{0}85,9]$      & $\symsub{xi_factor}{idx_fac_g}$ (System)                        & $1,93$               \\
        $\sym{power}_{\sym{beta}_{11}}$ & $70,9\,\%$               & $[32,6; \phantom{0}86,8]$      & $\symsub{xi_factor}{idx_fac_g}$ (System)                        & $1,99$               \\
        $\sym{power}_{\sym{beta}_{22}}$ & $79,3\,\%$               & $[33,4; 100,0]$                & $\symsub{xi_factor}{idx_fac_a}$ \textbf{(Design)}               & $3,72$               \\
        \bottomrule
        \multicolumn{5}{@{}l}{\footnotesize $\sym{Eff}$: Absoluter Effekt des Faktors auf die Power (aus ANOVA zum Meta-Design).}
    \end{tabular}
\end{table}

Dies birgt ein erhebliches ökonomisches Potenzial: Wenn die Trennschärfe ohnehin primär durch das System- und Messrauschen limitiert wird, ist zu prüfen, ob Design-Vereinfachungen (Reduktion von $\symsub{xi_factor}{idx_fac_c}$ und $\symsub{xi_factor}{idx_fac_d}$) überhaupt zu einer \textit{signifikanten} Effektivitätsreduktion führen.
Tabelle~\ref{tab:impact_cd} quantifiziert diesen „\textit{Design-Penalty}“:
Es zeigt sich, dass der Verzicht auf Zentralpunkte ($\symsub{xi_factor}{idx_fac_c}$) und Sternpunkte ($\symsub{xi_factor}{idx_fac_d}$) zwar die Trennschärfe reduziert (z.B. $\Delta \sym{power} \approx -13\,\%$ für $\sym{power}_{\sym{beta}_{11}}$), dieser Einfluss jedoch in der gleichen Größenordnung liegt wie die unvermeidbaren Rauscheffekte (\textit{Noise}).


\begin{table}[htbp]
    \centering
    \caption{Systematischer Trennschärfe-Verlust ($\Delta \sym{power}$) bei Reduktion der Versuchsstützpunkte. Während lineare Haupteffekte relativ robust gegenüber einer Design-Ausdünnung bleiben, zeigt sich der primäre \textit{Design-Penalty} in der Schätzung der quadratischen Terme.}
    \label{tab:impact_cd}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{@{}l rr@{}}
        \toprule
        \textbf{Koeffizient}            & \multicolumn{2}{c}{\textbf{Durchschnittlicher Trennschärfe-Verlust ($\Delta \sym{power}$)}}                                                                              \\
        \cmidrule(l){2-3}
                                        & \textbf{bei $\symsub{xi_factor}{idx_fac_c}$ ($\symsub{n}{idx_C}=8 \to 1$)}                  & \textbf{bei $\symsub{xi_factor}{idx_fac_d}$ ($\symsub{n}{idx_S}=4 \to 2$)} \\
        \midrule
        \multicolumn{3}{@{}c}{\textit{Konstante, Haupt- und Interaktionseffekte}}                                                                                                                                  \\
        $\sym{power}_{\sym{beta}_0}$    & $-4,5\,\%$                                                                                  & $-0,3\,\%$                                                                 \\
        $\sym{power}_{\sym{beta}_1}$    & $-2,4\,\%$                                                                                  & $-6,0\,\%$                                                                 \\
        $\sym{power}_{\sym{beta}_2}$    & $+1,1\,\%$\rlap{$^*$}                                                                       & $-8,0\,\%$                                                                 \\
        $\sym{power}_{\sym{beta}_{12}}$ & $-1,2\,\%$                                                                                  & $-0,9\,\%$                                                                 \\
        \addlinespace
        \midrule
        \multicolumn{3}{@{}c}{\textit{Quadratische Effekte}}                                                                                                                                                       \\
        $\sym{power}_{\sym{beta}_{11}}$ & $-13,9\,\%$                                                                                 & $-12,8\,\%$                                                                \\
        $\sym{power}_{\sym{beta}_{22}}$ & n.\,a.\rlap{$^{**}$}                                                                        & $-6,1\,\%$                                                                 \\
        \bottomrule
        \multicolumn{3}{l}{\footnotesize $^*$ \phantom{$^*$}Positiver Wert resultiert aus Varianzverschiebungen bei instabilen Designs.}                                                                           \\
        \multicolumn{3}{l}{\footnotesize $^{**}$ Nicht auswertbar, da Singularität in quadratischem Term bei $\symsub{n}{idx_C}$.}
    \end{tabular}
\end{table}
Einzig der quadratische Koeffizient $\sym{power}_{\sym{beta}_{22}}$ bildet eine Ausnahme:
Hier ist der geometrische Abstand der Sternpunkte ($\symsub{xi_factor}{idx_fac_a}$) mit einem extremen Index von $3,72$ der dominierende Faktor (vgl. Tabelle~\ref{tab:summary_power_sensitivity}).
Für alle anderen Terme gilt:
Da die $\sym{power}$ durch die physikalischen Randbedingungen ($\symsub{xi_factor}{idx_fac_e}, \symsub{xi_factor}{idx_fac_g}$) maßgeblich vorbestimmt ist, besteht Spielraum zur Anpassung der Faktoren $\symsub{xi_factor}{idx_fac_c}$ und $\symsub{xi_factor}{idx_fac_d}$ in Bereiche, die aus Aspekten der Versuchszeit und des Budgets attraktiv sein können - ohne die Effektivität des Tests unverhältnismäßig stärker zu kompromittieren, als es das Messsystem ohnehin schon tut.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Generische Auswirkungen relevanter Orthogonalitäts Abweichungen} \label{subsec:ergebnisse_generic}

Die Ergebnisse der vorangegangenen Sensitivitätsanalyse (siehe Abschnitt~\ref{subsec:ergebnisse_sensitivitaet}) haben gezeigt, dass das \ac{CCD} im Hinblick auf die statistische Trennschärfe ein robustes Verhalten gegenüber Modifikationen seiner Stützstellenanzahl aufweist, da im Regelfall das experimentelle System- und Messrauschen den limitierenden Faktor der Effektivität darstellt - vgl. Tabellen~\ref{tab:summary_power_sensitivity} und~\ref{tab:impact_cd} sowie Abschnitt~\ref{subsec:pläne}.
Für die praktische \ac{EoL}-Versuchsplanung greift dieser generalisierte, kombinatorische Ansatz jedoch noch zu kurz.
In der Realität am Prüfstand treten Abweichungen von der idealen Design-Matrix technisch bedingt oft nicht als geplante, systematische Ausdünnung auf, sondern als unvorhergesehene, asymmetrische und isolierte Einzelphänomene, erzwungen durch physikalische oder normative Restriktionen \cite{Arndt.2022,Mell.2022,Box.1963,Ahn.2015}.
Gleichzeitig eröffnet das Bewusstsein über die erstaunlich geringen \textit{Design-Penalties} aus Abschnitt~\ref{subsec:ergebnisse_sensitivitaet} die Chance für eine gezielte adaptive Planung:
Wenn Design-Modifikationen die Effektivität des Nachweises ohnehin nur moderat beeinträchtigen, ist zu prüfen, inwiefern geometrische Manipulationen zur Kostenoptimierung, Prüfzeitverkürzung oder zur Vermeidung irrelevanter Ausfallmechanismen nicht sogar bewusst (\textit{on purpose}) induziert werden sollten.

Um diese singulären Praxisphänomene systematisch bewerten zu können, ist die kombinatorische Betrachtung des Meta-Designs aus dem vorherigen Abschnitt nicht mehr zielführend.
Stattdessen werden basierend auf der Simulationsmethodik aus Abschnitt~\ref{subsec:simulationsmethodik} vier spezifische, voneinander isolierte Störszenarien definiert, welche typische Einzelrestriktionen in multivariaten Lebensdauerversuchen abbilden \cite{Arndt.2022,Mell.2022}:
\begin{itemize}
    \item \textbf{Fall I (Axialer Shift):} Sternpunkte ($\symsub{n}{idx_S}\geq 1$) können aufgrund von Anlagenrestriktionen nicht auf dem nominellen Abstand $\symsub{alpha}{idx_D}$ gefahren werden oder würden einen durch \ac{ALT} induzierten Wechsel des Ausfallmechanismus provozieren.
          Zur methodischen Einordnung wird dieser Fall in drei Stufen simuliert: eine axiale Stauchung auf $80\,\%$, eine Streckung auf $120\,\%$ sowie der Rückzug auf die Nennlasten des rein faktoriellen Würfels (\textit{\ac{FC}}, $\symsub{alpha}{idx_D}=1$).
    \item \textbf{Fall II (Co-faktorielle Korrelation):} Ein Sternpunkt ist nicht mehr achsenrein isoliert realisierbar.
          Spezifische, physikalische Systemfunktionalitäten erzwingen die simultane (parasitäre) Aussteuerung eines zweiten Faktors, wodurch eine direkte geometrische Korrelation im ansonsten orthogonalen Raum entsteht.
          Dieser Fall wird in einer leichten und einer starken Ausprägung untersucht.
    \item \textbf{Fall III (Stellgrößen-Scatter):} Die Aktorik des Prüfstands erlaubt keine exakte Ansteuerung der Zielkoordinaten, sodass die Sternpunkte einer unbeabsichtigten stochastischen Streuung (uniformes Rauschen von $\pm 10\,\%$ auf der Eingangsvariablen $\symsub{x}{idx_i}$) unterliegen.
    \item \textbf{Fall IV (Früh-Ausfall / Zensierung):} Ein extremer Versuchspunkt (Sternpunkt) führt zu einem Frühausfall, einem irrelevanten Durchläufer oder kann aus Zeitgründen nicht abgeschlossen werden.
          Er entfällt somit vollständig und asymmetrisch aus der finalen Design-Matrix -- hier simuliert am Beispiel der Eliminierung des oberen Sternpunktes auf der Achse von Faktor $\sym{x}_1$.
\end{itemize}

Während auch bei dieser isolierten Betrachtung der Erhalt der Trennschärfe die übergeordnete Rolle spielt, rückt hier zusätzlich die Schätzgüte der Modellparameter in den Fokus.
Es gilt zu quantifizieren, inwiefern der Verlust der Orthogonalität - messbar über den globalen $\sym{A2}$-Indikator - zu Multikollinearität und folglich zu einer schädlichen Varianzinflation (\ac{VIF} beziehungsweise \ac{MSE}) oder gar zu systematischen Verzerrungen der Koeffizientenschätzung (\textit{Bias}) führt - vgl. Abschnitt~\ref{subsec:optimal}.
Die analytische Differenzierung dieser Fehlerarten ist von entscheidender Bedeutung, da insbesondere ein signifikanter Bias die Validität der gesamten Lebensdauerprognose unbemerkt kompromittieren würde, während eine reine Varianzinflation primär die Konfidenzintervalle aufweitet.

\subsubsection{Kalibrierung der Signal-to-Noise Ratio und analytische Justierung des Arbeitspunkts}

Wie in der vorangegangenen Sensitivitätsanalyse (vgl. Abschnitt~\ref{subsec:ergebnisse_sensitivitaet}) dargelegt, konvergiert die Trennschärfe bei dominierenden physikalischen Effekten (hohes $\sym{snr} \gg 1$) in einen Sättigungsbereich nahe $100\,\%$.
Unter solchen Idealbedingungen ist das Regressionsmodell derart robust, dass selbst signifikante geometrische Verzerrungen des Versuchsplans den Nachweis eines Effekts $\sym{Eff}$ kaum gefährden.
In der Praxis der Lebensdauerprognose stellen diese dominanten Einflussgrößen jedoch selten die primäre Herausforderung dar. Vielmehr gilt es, subtile Effekte und schwache Degradationserscheinungen zu identifizieren, die sich nur knapp vom Systemrauschen abheben.

Um den isolierten Einfluss von Orthogonalitäts-Abweichungen - den spezifischen \textit{Design-Penalty} - überhaupt quantifizierbar zu machen, muss die nachfolgende Simulation daher in einen kritischen Arbeitsbereich überführt werden.
Hierfür wird das \textit{Ground-Truth}-Modell gezielt kalibriert: Die Koeffizientenstärken der untersuchten Effekte werden auf $\symsub{beta}{idx_i} = 10$ fixiert und mit einer erhöhten Standardabweichung der Systemantwort von $\sym{sigma} = 7{,}5$ beaufschlagt. Dies entspricht einem moderaten Signal-Rausch-Verhältnis von $\sym{snr} \approx 1{,}33$.

Diese Parametrisierung macht sich die mathematische Gesetzmäßigkeit zunutze, dass die Trennschärfe-Funktion - basierend auf den Nichtzentralitätsparametern $\symsub{delta}{idx_nc}$ der zugrundeliegenden Prüfverteilungen - einen S-förmigen (sigmoidalen) Verlauf aufweist \cite{Cohen.1988,Montgomery.2020}.
Während die Kurve am unteren Rand für ein $\sym{snr} \approx 0$ gegen das gewählte Signifikanzniveau $\sym{alpha}$ (Erwartungswert des Fehlers 1. Art) und am oberen Rand asymptotisch gegen $\sym{power}=100\,\%$ strebt, besitzt sie in der Übergangszone ihre steilste Flanke.
Die definierte Kalibrierung zwingt das ideale, orthogonale Referenz-Design exakt auf diesen Wendepunkt maximaler Sensitivität, in dem die Basis-Trennschärfe für die betrachteten Modelleffekte bei rund $\sym{power}=88\,\%$ liegt.

Dieser künstliche Arbeitspunkt fungiert folglich als analytische Lupe:
Er eliminiert jeglichen Sättigungseffekt und stellt sicher, dass die infolge von Orthogonalitäts-Verlust und Multikollinearität auftretende Varianzinflation (\ac{VIF}) unmittelbar als messbarer Trennschärfe-Verlust $\Delta \sym{power}$ sichtbar wird.
Um hierbei rein stochastische Schwankungen zwischen den verschiedenen Design-Szenarien auszuschließen und eine strikte \textbf{Reproduzierbarkeit} der Ergebnisse zu garantieren, wird der Monte-Carlo-Algorithmus deterministisch initialisiert (\textit{Mersenne-Twister}-Generator mit \textit{Seed}-Fixierung) \cite{Zio.2013}.
Nur durch dieses methodische Setup lässt sich valide bewerten, ab welchem Grad eine hypothetische Praxisrestriktion am Prüfstand bzw. generelle Orthogonalitäts-Abweichung gemäß \textit{Fall~I-IV} die statistische Integrität der Effekt-Identifikation real gefährdet.

\subsubsection{Quantitative Auswertung und explorative Optimalitäts-Analyse}

Die simulative Auswertung der definierten Praxis-Szenarien bei einem kritischen Signal-Rausch-Verhältnis ($\sym{snr} = 1{,}33$) offenbart eine hochgradig differenzierte Reaktion der klassischen Design-Metriken (Tabelle~\ref{tab:results_ortho_metrics}) und der algorithmischen Schätzgüte.
Zur detaillierten Evaluation wird diese Schätzgüte nachfolgend nach dem systematischen Schätzfehler (\textit{Bias} in Tabelle~\ref{tab:results_ortho_bias}), der Schätzvarianz (\textit{\ac{MSE}} in Tabelle~\ref{tab:results_ortho_mse}) sowie dem resultierenden Trennschärfe-Verlust ($\Delta \sym{power}$ in Tabelle~\ref{tab:results_ortho_power}) aufgeschlüsselt.

\input{tables/tab_results_ortho_metrics.tex}

Zunächst belegt die Auswertung die fundamentale Erwartungstreue des angewandten OLS-Algorithmus:
Trotz gravierender struktureller Abweichungen im Versuchsraum verharrt der systematische Schätzfehler für alle Szenarien und über alle Koeffizienten hinweg im marginalen Promillebereich nahe Null (Tabelle~\ref{tab:results_ortho_bias}).
Eine geometrische Verzerrung im \ac{CCD} führt demnach nicht zu einer inhärent falschen Lebensdauerprognose. Dass die gewählte Anzahl von $\symsub{n}{idx_MC} = 10.000$ Simulationsläufen dabei ausreichend dimensioniert ist, um das stochastische Rauschen der numerischen Approximation zu eliminieren und eine strikt reproduzierbare Konvergenz gegen diese wahren Parameter zu gewährleisten, wird exemplarisch für den Koeffizienten $\hat{\sym{beta}}_2$ in Abbildung~\ref{fig:ma_abb5.1.4_betakonv} visuell belegt.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_betakonv.tex}
    \caption{Numerische Konvergenz am Beispiel von Schätzwert zu $\hat{\sym{beta}}_2$ über die \ac{MCS}-Iterationen.}
    \label{fig:ma_abb5.1.4_betakonv}
\end{figure}

\input{tables/tab_results_ortho_bias.tex}

Stattdessen entlädt sich der durch Asymmetrien und Korrelationen erzeugte Fehler theoriekonform fast vollständig in der Schätzvarianz (\ac{MSE}, vgl. Tabelle~\ref{tab:results_ortho_mse}).
Dies spiegelt sich prägnant in den stark ansteigenden Werten der absoluten \ac{MSE}-Auswertung in Abbildung~\ref{fig:ma_abb5.1.4_msedev} wider.
In der Praxis der Zuverlässigkeitstechnik führt diese Varianzinflation zu signifikant aufgeweiteten Vertrauensbereichen der Punktschätzungen, was der unmittelbare Abgleich der Fehlerbalken (Konfidenzintervalle) in Abbildung~\ref{fig:ma_abb5.1.4_betadev} eindrucksvoll verdeutlicht.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_msedev.tex}
    \caption{Güte der Schätzer (\ac{MSE}). Geometrische Verzerrungen entladen sich primär in der Schätzvarianz, insbesondere beim Wegfall von Freiheitsgraden durch asymmetrische Ausfälle (\textit{Fall IV}).}
    \label{fig:ma_abb5.1.4_msedev}
\end{figure}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_betadev.tex}
    \caption{Absolute Schätzwerte der Modellkoeffizienten inklusive der $95\,\%$-Konfidenzintervalle. Die durch Nicht-Orthogonalität induzierte Varianzinflation (\ac{VIF}) führt zu stark aufgeweiteten Vertrauensbereichen.}
    \label{fig:ma_abb5.1.4_betadev}
\end{figure}

\input{tables/tab_results_ortho_mse.tex}

Die explorative Analyse der Resultate korreliert diese Varianzeffekte direkt mit den klassischen Design-Metriken (Abbildung~\ref{fig:ma_abb5.1.4_korrelation}).
Auffällig ist hierbei, dass der rein topologische $\sym{A2}$-Indikator kaum linear mit dem tatsächlichen Trennschärfeverlust korreliert (Abschnitt~\ref{subsec:optimal}: Pearson-Korrelation $\sym{corr} \approx 0{,}38$).
Dies ist darauf zurückzuführen, dass Trennschärfe-Verluste in Folge von Multikollinearität nicht linear, sondern sigmoidal verlaufen, weshalb reine Matrix-Topologie-Indikatoren (wie $A_2$) sich primär als Schwellenwert-Warnungen, nicht aber als lineare Prädiktoren der $\sym{power}$ eignen.
Auch der \ac{VIF} zeigt mit $\sym{corr} \approx 0{,}69$ nur eine begrenzte Aussagekraft für die Trennschärfe.
Hingegen fungiert die simulierte Schätzvarianz (\ac{MSE}) als nahezu perfekter, inverser Prädiktor für den Trennschärfe-Erhalt ($\sym{corr} \approx 0{,}99$ zwischen \ac{MSE} und $\Delta \sym{power}$ für den Term $\hat{\sym{beta}}_{11}$).

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{4cm}
    \input{plots/ma_abb5.1.4_korrelation.tex}
    \caption{Korrelation zwischen Design-Metriken und dem resultierenden Trennschärfe-Verlust.}
    \label{fig:ma_abb5.1.4_korrelation}
\end{figure}

Gleichzeitig erweist sich im Bereich der theoretischen Optimalitäten die I-Effizienz ($\sym{eff_I}$) als verlässlichster Indikator ($\sym{corr} \approx -0{,}97$).
Volumengetriebene Kriterien wie $\sym{eff_I}$ und $\sym{eff_D}$ skalieren proportional mit der absoluten Ausdehnung des Versuchsraums. So erzielt der nach außen gestreckte Shift (Fall I, $+20\,\%$) Bestwerte in der integrierten Prädiktionsvarianz ($\sym{eff_I} = 85{,}1\,\%$), da die Hebelwirkung der Stützpunkte im Raum steigt.
Die \textit{G}-Optimalität ($\sym{eff_G}$), welche die Vorhersagegüte am ungünstigsten Ort (\textit{Worst-Case}) absichert, erweist sich hingegen als extrem sensitiv gegenüber geometrischen Asymmetrien.
In Fall II (starke Korrelation) fällt sie drastisch von $17{,}6\,\%$ auf $13{,}9\,\%$ ab, obwohl das globale Design-Volumen ($\sym{eff_D}$) scheinbar stabil bleibt.
Dies illustriert den statistischen blinden Fleck multikollinearer Designs (sichtbar am höchsten $\sym{A2}$-Indikator von $0{,}352$):
Durch parasitäre Abhängigkeiten entstehen schlecht abgetastete Lücken im Parameterraum, welche die Fehlervarianz in diesen Ecken lokal ansteigen lassen.

Der resultierende \textbf{Einfluss auf die Trennschärfe} -- in Tabelle~\ref{tab:results_ortho_power} quantifiziert und in Abbildung~\ref{fig:ma_abb5.1.4_powerdev} detailliert veranschaulicht -- verhält sich in der Konsequenz wie folgt:

\input{tables/tab_results_ortho_power.tex}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.9\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_powerdev.tex}
    \caption{Trennschärfe mit $95\,\%$-Konfidenzintervallen in Abhängigkeit der Störszenarien. Trotz kritischer Parameterkonstellation ($\sym{snr}=1{,}33$) bleiben die \sym{power}-Verluste bei Erhalt der Symmetrie gering.}
    \label{fig:ma_abb5.1.4_powerdev}
\end{figure}

\begin{enumerate}
    \item \textbf{Gutmütige Restriktionen (Scatter und \ac{FC}):} Eine ungenaue, stochastisch verzerrte Ansteuerung der Nenn-Koordinaten (\textit{Fall III}) hat mit einem $\sym{A2} = 0{,}007$ kaum messbare Auswirkungen. Der Trennschärfeverlust liegt bei $\le 1\,\%$. Selbst das bewusste Zurückziehen der Sternpunkte auf das strikte physikalische Randniveau der Faktoren (\textit{\ac{FC}}, \textit{Fall I}) generiert lediglich einen vertretbaren Detektionsverlust von $3{,}6\,\%$ in den quadratischen Termen.
    \item \textbf{Kritische Restriktionen (Ausfall):} Der vollständige Entfall eines Randpunktes (\textit{Fall IV}) schlägt sich im Kollaps nahezu aller globalen Optimalitäten nieder (z.\,B. $\sym{eff_I}$ fällt auf $69{,}0\,\%$). Der Verlust von Freiheitsgraden an den Rändern treibt das \ac{VIF} auf über $1{,}2$ und den \ac{MSE} auf massive $5{,}46$ - ein Anstieg der Schätzvarianz um über $70\,\%$. Dies resultiert im stärksten Einbruch der Trennschärfe von fast $7\,\%$.
\end{enumerate}

Zur Sicherstellung der methodischen Transparenz und Reproduzierbarkeit ist das numerische Konvergenzverhalten der empirischen Trennschärfe über die zunehmende Anzahl an \ac{MCS}-Iterationen exemplarisch für $\sym{beta}_2$ in Abbildung~\ref{fig:ma_abb5.1.4_powerkonv} dokumentiert.
Es zeigt, dass die angesetzte Iterationstiefe ausreicht, um den numerischen Approximationsfehler zu minimieren und stabile Power-Kennwerte abzuleiten.
Flankierend sind die resultierenden Wahrscheinlichkeitsdichten der Signifikanztests (\sym{p-Wert}) für alle Fälle in Abbildung~\ref{fig:ma_abb5.1.4_phisto} grafisch aufbereitet.
Weiterführende Sensitivitäts- und Strukturmatrizen sowie die Konvergenzplots der restlichen Koeffizienten können bei Bedarf im Anhang~\ref{app:c} eingesehen werden.

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_powerkonv.tex}
    \caption{Numerische Konvergenz der Trennschärfe über die Simulationsläufe am Beispiel von $\sym{beta}_2$.}
    \label{fig:ma_abb5.1.4_powerkonv}
\end{figure}

\begin{figure}[htbp]
    \centering
    \setlength\figurewidth{0.8\textwidth}
    \setlength\figureheight{6cm}
    \input{plots/ma_abb5.1.4_phisto.tex}
    \caption{$\sym{p-Wert}$ Histogramm für den Signifikanztest am Beispiel von $\sym{beta}_2$. Die relative Häufigkeit der Werte unterhalb von $\sym{alpha}=0{,}05$ entspricht der berechneten $\sym{power}$.}
    \label{fig:ma_abb5.1.4_phisto}
\end{figure}


\subsubsection{Synthese: Adaptive Planung und arbiträre Modifikationen}

Aus diesen Resultaten lässt sich für die methodische Auslegung von \ac{EoL} Lebensdauerprognosen eine klare handlungsleitende Synthese ableiten.
Das \ac{CCD} reagiert auf symmetrische, aber suboptimale Hebelweiten (\textit{Fall I}) sowie auf verrauschte Stellgrößen (\textit{Fall III}) extrem verzeihend.
Dieser Umstand legitimiert eine \textbf{gezielte, adaptive Versuchsplanung}:
Anstatt bei Prüfständen mit stark eingeschränkten physikalischen Stellbereichen, extremen Kostendrücken oder unpräziser Aktorik kompromisslos auf starren geometrischen Lehrbuchvorgaben (wie der strengen Rotierbarkeit mit exaktem $\symsub{alpha}{idx_D}$) zu beharren, kann und sollte das Design bewusst in den technisch sicheren, \textit{Face-Centered}-Bereich komprimiert werden.
Die Studie belegt unmissverständlich, dass der resultierende \textit{Design-Penalty} in der statistischen Trennschärfe im niedrigen einstelligen Prozentbereich verbleibt.
Umgekehrt ermöglicht eine bewusste axiale Streckung sogar eine künstliche Steigerung der \sym{power}, sofern das Testobjekt diese Lasten toleriert.

Gleichzeitig liefert der asymmetrische Ausfall (\textit{Fall IV}) die harte methodische Grenze für diese Flexibilität:
Der ungeplante Entfall von Extrempunkten zerstört die Stabilität der Informationsmatrix signifikant.
Daraus folgt die ökonomisch-strategische Konsequenz für die Testplanung: Modifikationen der Design-Geometrie sind methodisch vertretbar und hochgradig ratsam, wenn sie perspektivisch Prüfzeit und Kosten sparen.
Der asymmetrische, ersatzlose Ausfall von Randstützstellen - sei es durch Fehlplanung oder zu hohe Belastungen (Zensierung) - muss operativ jedoch unter allen Umständen verhindert oder durch sofortige Augmentierung kompensiert werden.
Dieser Paradigmenwechsel vom starren Lehrbuch-Design hin zu einer kosteneffizienten, risikoadaptierten Versuchsraumgestaltung bildet das notwendige Fundament für das nachfolgend vorgestellte ökonomische Bewertungsmodell.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Fazit: Statistische Effektivität und ökonomische Relevanz} \label{subsec:fazit_sensitivitaet}

Aus den numerischen Analysen lässt sich ein zentrales Fazit für die Lebensdauererprobung ableiten: Das \ac{CCD} erweist sich als äußerst robust gegenüber moderaten geometrischen Modifikationen.
Die statistische Effektivität, repräsentiert durch die globale und modellspezifische Trennschärfe, wird primär durch nicht beeinflussbare, exogene System- und Messrauschen-Parameter limitiert.
Eine gezielte Reduktion der Versuchsumfänge - wie der partielle Wegfall von Zentral- oder Sternpunkten - bewirkt zwar einen messbaren isolierten Verlust an Trennschärfe, diesen \textit{Design-Penalty} gilt es jedoch stets in Relation zur ohnehin limitierenden Wirkung des experimentellen Rauschens zu setzen.

Für die praktische \ac{EoL}-Versuchsplanung bedeutet dies einen Paradigmenwechsel: Anstatt \acp{RSD} um jeden Preis auf strenge geometrische Orthogonalität und eine maximale Stützstellenanzahl zu trimmen, können und sollten Versuchspläne flexibel an betriebliche Limitierungen adaptiert werden.
Der bewusste Tausch einer geringfügig reduzierten statistischen Trennschärfe gegen eine potenziell massive Einsparung an Versuchszeit und finanziellen Ressourcen stellt folglich einen validen Optimierungsansatz dar.
Damit ist die methodische Grundlage geschaffen, um Modifikationen am Versuchsplan im folgenden Abschnitt in einem dedizierten Kostenmodell monetär zu bewerten und der statistischen Leistungsfähigkeit gegenüberzustellen. Das Ziel ist eine holistische Bewertungsmetrik, die den Informationsgewinn direkt mit dem wirtschaftlichen Aufwand verknüpft.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Ökonomische Bewertung: Das Kostenmodell} \label{sec:kostenmodell}
Eine rein statistische Bewertung greift in der industriellen Praxis zu kurz.
Eine Design-Modifikation, die die Prädiktionsvarianz um 5\% senkt, aber die Versuchskosten verdoppelt, ist ineffizient.
Daher wird ein Kostenmodell eingeführt, das die monetären Aufwände eines Lebensdauerversuchs in Abhängigkeit der Design-Parameter abbildet.
Die Gesamtkosten $\symsub{C}{idx_total}$ setzen sich additiv aus fixen und variablen Anteilen zusammen:
\begin{equation} \label{eq:kostenmodell}
    \symsub{C}{idx_total} = \symsub{C}{idx_fix} + \left( \sym{n} \cdot \symsub{C}{idx_spec} \right) + \left( \sum_{\sym{idx_i}=1}^{\sym{n}} \symsub{t}{idx_test}_{\sym{idx_i}}(\symsub{x}{idx_i}) \cdot \symsub{C}{idx_time} \right) + \symsub{C}{idx_fail}.
\end{equation}
Hierbei beschreibt $\symsub{C}{idx_fix}$ die Rüstkosten, $\symsub{C}{idx_spec}$ die Herstellkosten pro Prüfling und $\symsub{C}{idx_time}$ die laufenden Betriebskosten des Prüfstands pro Zeiteinheit.
Der Term $\symsub{t}{idx_test}_{\sym{idx_i}}(\symsub{x}{idx_i})$ ist die entscheidende, vom Versuchsplan abhängige Größe: Durch \ac{ALT} (hohe Lastniveaus $\sym{x}$) sinkt die Prüfzeit, was $\symsub{C}{idx_total}$ reduziert.
Gleichzeitig steigt jedoch durch aggressive Lasten potenziell der Energiebedarf oder das Risiko nicht-repräsentativer Ausfallmechanismen ($\symsub{C}{idx_fail}$).
Dieses Modell ermöglicht es, Design-Alternativen nicht nur nach ihrer statistischen Effizienz (D-, G-Effizienz), sondern nach ihrer \textit{ökonomischen Effizienz} (Information pro Euro) zu bewerten.
Dieses Modell ermöglicht es, Design-Alternativen nicht nur nach ihrer statistischen Effizienz (D-, G-Effizienz), sondern nach ihrer \textit{ökonomischen Effizienz} (Information pro Euro) zu bewerten.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Orthogonalitäts-Abweichungen in der Zuverlässigkeitsprognose} \label{sec:design_alternativen}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Extrapolation Variance Graph (EVG)} \label{sec:visualisierung}
Zur Beurteilung der Vorhersagegüte im gesamten Versuchsraum haben sich der \textbf{\ac{VDG}} und der \textbf{\ac{FDS}-Plot} etabliert.
Diese Metriken bewerten die Verteilung der skalierten Prädiktionsvarianz (\ac{SPV}) jedoch oft global oder über sphärische Distanzen gemittelt.
Für die Lebensdauerprognose, bei der oft eine Extrapolation in einen spezifischen, eng begrenzten Anwendungsbereich ("Corner Case") von Interesse ist, sind diese globalen Maße oft unzureichend.

Daher wird in dieser Arbeit eine neue, erweiterte Auswertungsmethode vorgestellt: Das \textbf{Extrapolations-spezifische SPV-Mapping}.
Im Gegensatz zum klassischen VDG, der die Varianz über Radien mittelt, visualisiert diese Methode die Isolinien der Prädiktionsvarianz explizit in Richtung der relevanten Belastungsvektoren.
Dies ermöglicht die gezielte Bewertung der Design-Güte exakt in jenen Bereichen des Parameterraums, in denen die spätere Feldprognose erfolgen soll, und deckt Schwachstellen auf, die in globalen FDS-Plots verborgen blieben.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Zuverlässigkeitsmodellierung mit Vertrauensbereichen} \label{sec:zuv_vb}


\section{Simulative Validierung und Methodennachweis} \label{sec:simulation}
Aufgrund der unendlichen Kombinationsmöglichkeiten von Einflussfaktoren, Lastniveaus und physikalischen Modellen ist eine generische Sensitivitätsanalyse für alle denkbaren Anwendungsfälle nicht zielführend.
Stattdessen wird die Anwendbarkeit und Leistungsfähigkeit der vorgeschlagenen Methodik am Beispiel eines generischen Referenzsystems mittels Monte-Carlo-Simulation (MC) nachgewiesen.

\subsection{Simulationssetup}
Als Referenzsystem dient ein Weibull-verteiltes System ($\sym{b}=2, \sym{T}=1000\,\text{h}$), dessen Lebensdauerverhalten durch ein log-lineares Modell mit zwei Hauptfaktoren und einer Wechselwirkung beschrieben wird.
Im Rahmen der MC-Simulation werden $\symsub{n}{idx_MC} = 10.000$ virtuelle Versuchsreihen erzeugt, wobei für jeden Lauf:
1.  Lebensdauerdaten basierend auf dem definierten Modell und den Versuchsplan-Stützstellen (modifiziertes CCD vs. Benchmark-CCD) generiert werden.
2.  Eine stochastische Streuung sowie Zensierung (Typ I/II) aufgeprägt wird.
3.  Die Modellparameter mittels \ac{MLE} zurückgeschätzt werden.

\subsection{Ergebnisse und Effizienzbewertung}
Die Auswertung erfolgt multidimensional anhand der in den vorangegangenen Abschnitten definierten Metriken:
\begin{itemize}
    \item \textbf{Power:} Wie hoch ist die Wahrscheinlichkeit, die definierten Effekte trotz Design-Manipulation korrekt zu detektieren?
    \item \textbf{Parameterschätzgüte:} Wie verhalten sich Offset und Varianz der Schätzer ($\hat{\sym{b}}, \hat{\sym{T}}$) bei Verlust der Orthogonalität?
    \item \textbf{Ökonomische Bilanz:} Welches Design liefert die geforderte Präzision bei minimalen Gesamtkosten gemäß Gleichung~\ref{eq:kostenmodell}?
\end{itemize}
Durch den Vergleich der gesampleten Versuchszeiten (Time-to-Failure) kann valide auf die Effizienz im \ac{ALT}-Kontext geschlossen werden.
Es wird gezeigt, dass ein gezielt manipuliertes CCD, bei dem Sternpunkte in Richtung der Hauptbelastung verschoben werden, die Prognoseunsicherheit im Anwendungsbereich signifikant senken kann, ohne die maximale Versuchsanzahl oder das Budget des konventionellen Benchmarks zu überschreiten.
Damit wird der Nachweis erbracht, dass die entwickelte Methodik zur Bewertung und Anpassung von Versuchsplänen (Kostenmodell + SPV-Visualisierung + Orthogonalitäts-Check) ein robustes Werkzeug zur Steigerung der Testeffizienz darstellt.